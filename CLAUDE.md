# CLAUDE.md

2026-09-24目录整理：保留根train1/train2/predict1/predict2入口；YOLO在src/yolo/aic，公共浮点处理src/modalities.py，D-FINE项目适配src/dfine/{training,runtime}.py，上游核心/配置src/D-FINE；工具统一tools/dfine及既有tools，历史源码tools/archive/run_snapshots（不入Git）。删除空common、重复aic/aic和迁空src/tools；runs审计、模型及日志保留。当前官方标签1700/300，不恢复历史清洗。此次不改变训练参数，不测试或运行模型；已有未提交内存修复及用户预测配置保留本地，目录变动与引用单独提交推送。

2026-09-24 v24最终复盘：v24完成72轮，best42 AP95=0.4008757256/AP50=0.6279537053，末轮AP95=0.38451；它是1709/291同协议最高训练峰值，但best后30轮未刷新且训练损失继续下降，判定存在后期过拟合，不从last盲目续训。现场活动进程实际为v26_no_tone，v25已经完成。后续每个新版本代码完成后、启动训练前必须先创建独立Git提交；有推送授权时同步GitHub，且排除数据、权重、runs和用户无关文件。

2026-09-24最新授权：v25已完成64轮、best34 AP95=0.3851467445，实际FP32/batch1。用户批准YOLO v26_no_tone，关闭新增IR Gamma/局部对比度/RGB曝光，保留其余v25配方及batch1/nbs16；patience改50，200轮上限。先提交代码文档再启动一次正式训练；不追加测试或独立评估、不自动重试。train2保持v25配置不启动，predict1用户改动保留不夹带。下面“当前v24/下一轮v25”为历史。

## 训练版本编号规则（2026-09-24，最高优先）

- 版本号按实际启动的训练迭代管理，不按代码修改、方案讨论或Git提交次数递增。
- 新方案尚未开始训练时，所有新增增强、参数和代码调整继续归入同一待训练版本，不得提前创建下一版本。
- 用户当前正在训练v24；下一轮统一为v25，包含连续FP32、红外Gamma/局部对比度、RGB曝光与深度增强。此前未开训草案所称v26已合并回v25，不代表存在已训练的v26模型。
- 只有当前版本已实际开始训练，且用户决定安排下一轮新训练时，才分配下一版本号；断点续训仍沿用原版本。不得仅凭文件名推定已经训练。
- 当前两个入口运行名均以v25_illumination结尾。不要改名、移动或覆盖已经运行的v24及历史产物；代码协议标识与训练轮次版本号分别管理。


2026-09-23 v25追加（最新）：用户授权借鉴昼夜增强，train1/train2运行名改v25_illumination，SensorAugment显式开启IR Gamma0.25、受限浮点局部对比度0.25、RGB曝光扰动0.25。其余v25配方和预处理协议不变；新增增强默认概率0、只在训练启用，不重写图片为16位，不改标签，不宣称三分支光照门控或夜间效果已证实。未测试/训练/推理，本次不自动提交推送。详见docs/浮点三模态实施方案.md。

2026-09-23 v25连续浮点实施（最新优先）：用户批准两框架连续FP32深度、IR增益/偏置/噪声、有效深度扰动/稀疏缺失，旧权重仅推理兼容，不恢复旧训练。train1.py为YOLO v25_illumination（1280、batch2/nbs16、amp=False、主5e-5/骨干1e-5）；train2.py已接src/D-FINE/aic_training.py真实五通道Objects365训练（1280、batch2/有效16、主1e-4/骨干1e-5、60轮）。共享src/modalities.py处理连续浮点；内部0–255不取整，网络仅除255一次；关闭TF32，权重FP32保存。predict2在CLI后按最终权重自动识别尺寸，缺失不猜测；旧默认v4/v6不改，旧输入协议仍用于旧权重。数据/标签/划分未改，未自动测试、静态执行检查、训练或推理。用户授权提交推送，排除数据/权重/runs/PDF和未跟踪tests、validate_local.py。实施与未运行限制见docs/浮点三模态实施方案.md；下方v20/v24及缺口未修复均为历史状态。

2026-09-23 文档整合与复赛材料（最新优先）：docs现行手册、参考、历史合订本和官方资料分开，原文记录完整保留在归档，不以旧“当前”覆盖现状。main.py仍为v20 RGB诊断（200轮日程/20轮预算，非提交模型），本轮未开训。predict.py默认复赛测试源，保留用户新选v5 best.pt及复赛predict_v5输出；不自动换权重。新增独立审阅材料导出与materials-only补材料，所选运行训练快照缺失则待补，不用v20源码冒充。技术方案先用docs/技术方案.md，MD草稿不代表PDF要求已完成；队长及队伍信息待填。源码/模型/结果有摘要追溯，原数据/权重/运行产物不入GitHub。只核对文本和Git差异，不测试/静态执行检查/训练/推理/复评；按用户授权提交推送本轮源码文档，保留未跟踪tests和validate_local不提交。详见docs/项目知识索引.md及docs/预测与赛事提交.md。


2026-09-22 v20最新：v19完成75轮，best62 AP95=0.3672142710，阶段75门槛0.38停止，未超过v14。用户批准先做RGB诊断，main.py改v20_diagnostic：v14五通道增强完成后只给网络RGB，三通道首层，1280/batch4/nbs16/val_batch1，保留200轮日程与分层lr，独立budget_epochs=20，停用AP门槛。诊断不是三模态赛事候选；predict.py及原清洗1709/291不改。不自动测试、语法执行检查、训练、推理、复评、连续搜索或Git操作；本轮仅配置和文档，用户开训。详见docs/v19复盘与v20模态诊断.md，下方v19尚无结果为历史。

2026-09-22 提交与成绩最新：用户授权更新文档并推送GitHub；v19源码及实施记录已归档2e89a1f，16条初赛截图成绩另记docs/历次训练与赛事分数对比.md#初赛提交原始记录。v4两次55.6000，新增v5标为多标签50.8550（单标签51.0830）；53.1270/54.0500不擅自对应模型。截图全部初赛DONE，不证明复赛分数、逐类AP或v19效果。只更新文档和Git，不修改配方、不测试或运行模型；数据、权重、runs、截图原件、会话、PDF及本地未跟踪测试/评估脚本不入库。下方未提交是历史状态，推送完成以Git结果为准。

2026-09-22 v19实施最新：用户确认后已改main.py为v19_quality_fusion。官方YOLO26l/1280/清洗1709/291不变；保留RGB主干、复制预训练IR浅层、P3局部3×3对应、Depth原始支持掩码及小门控残差。六通道张量含派生支持比例，磁盘channels5不改；三模态与支持共用几何，深度支持加权插值、辅助填0，仅适用于新版本，旧权重保持原处理。batch2/nbs16、val_batch2、200轮、骨干及IR2e-5/新增模块及检测器1e-4，保留v14增强/早停/阶段预算，不采样重洗。predict.py保留用户默认，新模型显式复赛source/1280/height1280；未测试/语法执行检查/模型运行，未提交推送，由用户开训；不得称已涨分或保证0.60。v14仍为同协议基线，详细初始化/接口/命令见docs/归档/03_v15至v19实验.md#record-8。下方main仍v18、v19尚为草案属于上一阶段。

2026-09-22 v18完成后最新：55轮、best15、AP95=0.3752101829/AP50=0.6040608488，未超过v14；后期退化且阶段门槛均通过，不延长重跑。用户授权执行下一步审计并更新文档提交GitHub；已完成1709组训练IR/Depth文件读数（3418文件、每4行/列采样），PNG/JPG深度零值27.6491%/80.9212%，PNG采样超20米为0；5张JPG经全像素确认全零，新增4组RGB/IR目视。官方深度单位毫米/20米上限已核对，不再猜测量程错误。数据、标签、划分和训练/预测代码未改；不测试或运行模型。main.py仍为已完成v18，不重复启动，predict默认不动，保留v14候选。质量感知/局部容错融合只是待审核设计，不是已实现v19。只提交文档，不夹带runs、图像、权重、会话、PDF、未跟踪tests与validate_local.py。详见docs/归档/03_v15至v19实验.md#record-2；下方“v18尚无效果”等为历史。

2026-09-22：用户授权记录并提交GitHub，仅提交当前v18及依赖源码、相关复盘；不变更配方或执行任何模型任务/测试。数据、权重、运行产物、会话导出、赛事PDF、本地未跟踪测试/评估脚本保留本地。v18尚无已确认效果；最终提交/推送状态以Git为准，下方未提交描述属于历史。

2026-09-21 v18最新：用户完成v17共55轮，best15轮AP95=0.3851431884/AP50=0.6172845371，采样执行正确但未超过v14。现入口v18_split_stem，从官方RGB重新迁移，关闭受限重复，保留v14的1709/291清洗、1280/batch4/nbs16及200轮日程。RGB/IR/Depth首层卷积独立参数、共享原BN/激活；RGB首层/颈部/头1e-4，骨干2e-5，辅助首层真实AdamW学习率5e-4。模型版本区分旧整块首层，旧权重仍能预测，不能恢复旧last；predict.py默认不改，新模型使用显式复赛命令。新增正式训练首批响应RMS及优化步更新范数记录，不额外前向；范数不等于模态贡献因果。未运行测试/静态执行检查/模型任务，未提交推送，由用户启动。完整方案见docs/归档/03_v15至v19实验.md#record-6；以下待训练/入口描述属于历史阶段。

2026-09-21 v17配置与采样实现（最新）：用户自行开训，禁止自动测试/训练/推理/复评或提交推送。main.py改v17_bounded_repeat，官方YOLO26l重迁移，保留清洗1709/291及1280/batch4/nbs16、主lr1e-4/骨干2e-5，恢复v14的200轮余弦和第101轮关Mosaic、patience40/阶段筛选；cls_pw0。全12类按训练含图频率计算，阈值0.10、倍率≤2、含球提议倍率≤1.25、额外主样本≤10%、每序列及已审阅场景额外≤2。每图≤2指主索引，不含Mosaic附加图。固定轮长与有限加载器保证计划不跨轮预取，新增三份采样审计。aic/model.py、data.py及predict.py本轮不改，现有脏改动保留。v16已跑完60轮，best第36轮AP95=0.3676631044，比v14训练峰值0.3921497554低2.449个百分点，不能称v16未训练或继续延长就会改善。v17未运行，不承诺60分，详细计划见docs/归档/03_v15至v19实验.md#record-5。下文为历史阶段。

2026-09-21 v16配置交付（最新）：用户选择继续v16，自行开训；v4已由用户提交复赛，分数尚未提供。main.py为官方YOLO26l重迁移、五通道early_v10/main、1280/batch4/nbs16、主lr1e-4/骨干2e-5、60轮余弦/lrf0.01/warmup5、前20轮mosaic0.5/第21轮关闭，scale0.3/translate0.1不变、HSV0。恢复cls_remapTrue、patience40/min_delta0/最低20轮，10/20/50轮沿用v14门槛0.32/0.34/0.36，val_batch2、save_period5；不续训v15。固定当前清洗1709/291，不重洗，不动predict.py默认；预测显式填写复赛source和v16权重。未测试/静态执行检查/训练/推理/复评或提交推送，不自动启动。计划见docs/归档/03_v15至v19实验.md#record-4；v14本地基线0.3917876103保留，不能承诺60分。

2026-09-21 v15已完成（最高优先）：9轮、最佳第1轮，5轮筛选通过、9轮patience8早停，非10轮门槛误停。一次同291张FP32/batch2复评AP95=0.3849390289/AP50=0.6317789394，比v14 AP95低0.68486个百分点；状态completed/retain_v14，两进程退出码0。ball改善不能抵消tricycle/uav/boat等回退。保留v14 best为当前同口径候选，不自动重训、换模型或降门槛；main.py仍为已完成v15配方，不要重复运行。predict用户默认不改，v14显式传权重。下一项训练标签/模态对齐审阅另行开展，验证集不动；本次仅读取最终产物、更新文档，无新增模型运行、测试或提交推送。见v15计划最终验收和历次训练对比，下方启动状态为历史。

2026-09-21 v15启动（最新）：用户确认后实施20轮短收尾，从v14第15轮best严格继承全部五通道状态，新建优化器/日程/EMA计数；SHA256及同划分检查不放宽。主2e-5/骨干4e-6、warmup1、lrf0.1、mosaic0、scale0.1/translate0.025、patience8、最低5轮，阶段5/10轮AP95门槛0.385/0.393，val_batch2。18回归及2训练/2验证批通过。10:55:59已启动一次，结束后只自动复评best一次，不自动重试。运行状态在runs/checks_20260921/launch_v15/status.json；别重复运行main.py、别改运行中Python源码。v14基线0.3917876103保留，结果未完成不称提分；predict默认不改、数据不动、无提交推送。执行记录见docs/归档/03_v15至v19实验.md#record-3。

2026-09-21 v14已完成（最新）：55轮，最佳第15轮训练AP95=0.39215，之后patience40正常早停；阶段10/20/50均通过。同291张FP32/batch2复评best.pt为AP95=0.3917876103/AP50=0.6293876168，v13为0.3565488837/0.5577577474，提高3.524/7.163个百分点。10类改善，boat/ball退步，后期仍过拟合；首轮仅0.24671不妨碍后续提高。未达到0.60，不直接比较旧划分v4；下一短周期无Mosaic候选尚未启动。main.py仍为可复现v14；预测原v13默认保留，v14显式传权重。无正式训练进程继续运行，未提交推送，详见docs/归档/02_v9至v14复盘.md#record-7。

2026-09-21 最高优先：用户本轮授权相关修复、测试/训练/推理，仍禁止上传赛事数据，不自动提交推送。固定1709/291；新协议fp32_single_label_content_clip_v2统一预处理、FP32单标签NMS和内容裁框。v14保留v4真实首层融合，仅改骨干2e-5、其余1e-4；polish_*为None不改增强，显式值才有效。10项回归、两批训练/验证、检查点重载和两张验证图预测打包通过。最佳轮图存best_validation；tools/validate_local.py显式复评，不读官方测试集。实际基线、运行状态和限制见docs/归档/02_v9至v14复盘.md#record-7。下方“未测试”和禁测均为此前状态，不覆盖本轮授权；不承诺60分。

2026-09-20 最新优先：v12线上47.3270；v13完成151轮，最佳51轮AP95=0.35681，末轮0.31912，后期过拟合。确认native_square验证五通道补114、旧预测IR/Depth补0的不一致；predict.py已按权重geometry分流，默认v13 best与predict_v13_preprocess_fix，用户先重提交无需重训。main.py准备v14：与v13相同数据/几何/增强，仅骨干第1–10层lr2e-5、首层/颈部/头1e-4；预算patience40，10/20/50/75/100/125/150/175/200轮历史最佳AP95门槛0.32/0.34/0.36/0.38/0.40/0.45/0.50/0.55/0.60，记录screening_history.csv。门槛不保证提分；下方把v12停止原因当性能根因的说法已更正。未测试、静态执行检查、训练、推理、复评或重洗，未提交推送。见docs/归档/02_v9至v14复盘.md#record-6。

2026-09-20 当前最高优先：v11线上44.4710。v12第10轮AP95=0.33257且为运行新高，mAP50=0.55075、ball AP95=0.45121，却因0.34阶段门槛停止；该门槛来自不同的v4旧256张验证集，不能筛当前291张验证集。v13只移除阶段门槛，其他v12参数和代码不变，依靠best AP95与patience100自然收敛；默认预测v13/1280/predict_v13。未加入弱类采样或RGB路线，未执行测试、训练、推理或复评，未提交推送。见docs/归档/02_v9至v14复盘.md#record-5。

2026-09-20 当前最高优先：v11在第20轮阶段筛选停止，最佳第5轮AP95=0.32668；ball最佳轮AP95=0.41520，证明关闭cls_pw改善ball但未改善总体。当前main.py为v12：恢复v4原生1280方形数据几何，保留当前清洗1709/291、五通道早期融合、cls_pw0和ball迁移；200轮，前100轮Mosaic0.5，batch4/nbs16，AdamW 1e-4，阶段门槛第10/20轮历史最佳0.34/0.36。预测默认v12/1280×1280/predict_v12。未叠加弱类采样，未执行测试、训练、推理或复评，未提交推送。见docs/归档/02_v9至v14复盘.md#record-4。

2026-09-20 当前最高优先：v10完成49轮，最佳第19轮AP95=0.33592，线上50.9240；验证退化后正常早停，不是训练不足。v11保持v10全尺寸早期融合、1709/291清洗数据、增强和优化日程，只关闭cls_pw以隔离v10 ball从首轮AP95=0.35996退化至最佳轮0.03541的问题。第10/20轮分别按历史最佳AP95 0.31/0.34筛选，未达标停止；首轮仅诊断，不以0.4硬切，因为v4首轮也只有0.29976。predict.py默认v11 best/predict_v11。无测试、训练、推理或复评，未提交推送，不保证60分。见docs/归档/02_v9至v14复盘.md#record-3。

2026-09-20 同日后续优先：用户要求v10全尺寸和经验增强，替代下面1280草案。当前五通道首层融合，统一1920×1080内容/1920×1088张量；120轮、batch2/nbs16、全模型1e-4、patience30。前40轮Mosaic0.25/scale0.3/translate0.1，第41轮关闭拼图，保留scale0.1/translate0.025；RGB专用HSV0.01/0.15/0.15，三模态几何同步。保留ball语义迁移、cls_pw0.25、清洗审计、双最佳与FP32单标签。预测同尺寸，自动识别early_rect_v10_2，批量共用矩形/NMS路径，不再使用1280逐图路径。输出predict保留。无测试/训练/推理/复评、无重洗或新增人工复核、无提交推送、不保证60分。见docs/归档/02_v9至v14复盘.md#record-2。

2026-09-20 最新状态：v9用户线上53.4750（最佳63轮AP95=0.34952），低于v4的55.6000；新旧验证划分不同，不能简单归因推理或某一层。已读取Git 86411da、CPU检查点、预测记录及曲线并实施v10：官方YOLO26l五通道早期融合、1280、batch4/nbs16、全模型AdamW1e-4、200轮余弦、mosaic0.5/scale0.3/translate0.1/HSV0、cls_pw0.25、固定双头0.8/0.2、首轮计patience40。数据审计/1709与291划分不变，保留ball同义迁移和FP32单标签验证；预测默认v10 best/1280/v4原生路径，用户输出predict保持。没有测试、静态执行检查、模型前向/复评/训练/推理，不保证60分，未提交推送。源码归并结构不变，旧模型推理兼容。详情见docs/归档/02_v9至v14复盘.md#record-2；下方v9当前入口描述均为历史。

2026-09-19 最新状态：按用户要求保留完整配置，main.py覆盖本机default.yaml全部键，补model/mode/dis并修正dfl的L1权重注释。本轮明确授权的固定种子短流程已通过，runs/smoke_main/entry_smoke-2/smoke_report.json记录1训练批、1优化器调用、1验证批，无权重输出；没有验证多次运行严格确定性或全程训练。测试包装的model形参冲突已修正后复跑。正式v9核心数值不变，仍不自动扩展为调参搜索或全量训练。下方“修后尚未复跑”为历史状态。

2026-09-19 接口修正：曾按用户明确授权执行一次main.py冒烟测试，在构建模型时失败，训练/验证0批、无权重输出。现按用户修复要求，将aic/model.py的_predict_once与框架(x, profile, embed)接口对齐，移除visualize多余参数，未改结构/配方；修后尚未复跑，未提交。下方未测试记录为历史，默认不自动新增测试。

2026-09-19 报错修正追加（优先于下文未修正记录）：用户提供真实尺寸校验报错后，已将main.py的imgsz改为IMAGE_HW[0]=1920，常量按宽、高注释；由入口路径生成绝对PROJECT_PATH，解决全局runs_dir重复拼接，不修改全局设置。其余v9配方、清洗数据及预测默认值不变，不删除或移动旧输出。只核对代码与差异，未运行测试、AST/compile/import、训练或推理；本次修改尚未提交推送。

2026-09-19 提交同步：用户授权文档及本轮代码整理提交GitHub，保持当前用户参数。main.py存在IMAGE_HW=(1920,1080)但imgsz取第二项1080的尺寸冲突，尚未代改；开训前须与image_width=1920统一，不能以“图片1K”视为无问题。数据清洗/固定划分/开训审计/融合训练/提交链路保留；57文件修订已有审计，新增约200张人工复核仍是待办。未运行测试、训练、推理或新一轮清洗，详细现状见docs/归档/02_v9至v14复盘.md#record-1。

2026-09-19 入口收敛（优先于下方历史）：根目录仅保留main.py与predict.py。预测、后处理、图片/TXT保存、ZIP打包及旧YOLO/D-FINE推理均集中predict.py；aic/model.py保存可序列化模型及共用几何/NMS，aic/data.py提供读图/类别/指纹，aic/training.py集中当前训练器与数据集。tools/prepare_dataset.py保留数据审计清洗，以uv run python -m tools.prepare_dataset运行。删除原根目录中转文件及不用的旧D-FINE训练循环、旧YOLO训练器，不删除数据或权重；原文件可从08a4180恢复。aic.model注册旧三模态融合模块名供旧v9权重反序列化；源码指纹已变，整理前中断训练须用原代码快照恢复，不绕过校验。默认参数不变，未运行测试、AST/compile/import、训练或推理。说明见docs/代码结构与运行.md。

2026-09-19 清理追加：已删除当前训练/预测无引用的旧v5训练优化.py，Git提交08a4180保留原文件。三模态融合.py、融合训练.py、三模态训练.py、三模态预测.py、准备三模态数据集.py均仍被当前流程直接或间接使用，保留；D细化训练.py用于历史D-FINE预测。历史文字里的“保留训练优化.py”不再代表当前文件状态。此次仅阅读引用与差异，没有执行测试、AST/compile/import或模型任务。

2026-09-19 最新执行：用户批准门控融合并指定1920×1080，main.py现为原生YOLO风格v9入口，官方YOLO26l RGB主干+IR/Depth分支、张量1920×1088、120轮、batch=2/nbs=16、固定双头0.8/0.2，第81轮收尾。predict.py同步v9 best.pt及1920/1080、batch=4；保留旧后端但不回退默认值。datasets仍1709/291及57文件修订；没有再次清洗或启动模型任务。两个before的images和code_cleanup备份已按此前授权删除，不再是完整备份。此前只执行过一次AST静态语法解析和git diff格式检查，没有执行pytest、模型前向、评估、训练、推理或显存探测，不能据此宣称代码运行正确。以后默认不再做AST/compile/import等静态执行检查；提交前只看git状态、文件清单和实际暂存差异。用户自行训练，不能承诺60分。详见docs/归档/02_v9至v14复盘.md#record-1，优先于下方历史状态。

2026-09-18 晚间历史：用户已完成v8并反馈53.5740，v7 best_map50.pth为52.4010。官方下载刷新已落位1709/291，全部图像及新版标签新旧内容相同；截图经验与下一轮候选见docs/归档/01_早期训练与清洗.md#record-6。不实现多权重投票。

知识入口：docs/项目知识索引.md。文档中的旧“当前”按历史阶段解释，官方原文与项目导航分列。用户predict.py当前选择v7 best_map50.pth、1536；本轮提交保留此修改，不将其当成已获得新线上分数的模型。不运行测试、独立评估或推理。

2026-09-18 最新状态已覆盖为 v8：main.py 使用 D-FINE-L、1280、batch=4/有效16、最多60轮、lr=5e-5/骨干5e-6、patience=10；关闭v7重采样、裁剪与低清模拟。数据已按审阅清单落位 datasets 的1709/291，保留全部2000组、既有清洗修订，新增标签修订0；旧完整数据及缓存可从 runs/dataset_cleaning/v8_20260918_162439/before 恢复。不得用下文历史配置覆盖v8，不恢复v7断点，不自动测试、评估或训练。predict.py默认由用户维护，v8需显式传新权重与1280尺寸。以 docs/归档/01_早期训练与清洗.md#record-7 和 AGENTS.md 顶部为准；下文是历史。

2026-09-18 最新入口为 D-FINE v6：main.py → D细化训练.py，五通道、Objects365 E25、1280、60轮、3轮预热、主学习率1e-4/骨干1e-5。训练尚未运行，用户自行启动，不自动测试。v5 best.pt线上51.0830，v4 55.6000保留为正式基线。predict.py兼容YOLO .pt与D-FINE .pth；新权重需显式指定。当前断点文件为last.pth，不是下文历史last.pt。完整状态以docs/归档/01_早期训练与清洗.md#record-1为准，下文v5与v4参数均为历史。

用户优先要求：不自动运行测试、评估、额外推理或训练，不新建测试脚本；只分析现有产物与必要差异。评估模型.py、tests/、runs/config_checks 已移入 runs/code_cleanup/ 可恢复备份。v5_full 已完成，见 docs/归档/01_早期训练与清洗.md#record-3；下方旧进度保留为历史，不能据此再次启动训练。

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概览

最新实施状态：main.py 训练入口已切到 v5_full，辅助模块训练优化.py。线上 v4=55.6000、epoch50=53.2540，v4 保留为基线；v5 已实现弱类采样、原尺寸同步目标裁剪、RGB 增强、模态随机缺失、骨干 0.2 倍学习率、关闭 cls_pw、单标签验证对齐及双指标留存，尚未执行训练。当前 mosaic=0.25、scale=0.2、translate=0.05、patience=200，第 161 轮关闭 Mosaic/目标裁剪/模态缺失。预测新增 --multi-label 可选开关，默认关闭。目标 60 分以上，不承诺实际结果；细节以 docs/归档/01_早期训练与清洗.md#record-2 为准。下方 v4 复盘属于历史状态。

2026 第八届 AIC 全球校园 AI 算法精英大赛参赛项目：**面向城市场景的视觉多模态目标检测**（算法挑战赛道）。

任务：对空间对齐的三模态图像（RGB 可见光 / 红外 / 深度）做 12 类目标检测，输出每张测试图的检测框 TXT。排行榜指标为 `mAP@50-95`。赛题权威参考：`docs/比赛资料/2026 AIC 视觉多模态目标检测参赛手册.md`。

**当前进度**：YOLO与D-FINE已隔离：`train1.py/predict1.py`及`src/yolo/`负责YOLO，`train2.py/predict2.py`及`src/D-FINE/`负责D-FINE。复赛源为`datasets/test`，预测结果统一进入`历史产出/`；详见`docs/代码结构与运行.md`和`docs/预测与赛事提交.md`。

最新复盘：v4_clean_lr1e4 已完成 200 轮，CSV 最高 mAP50=0.60632（42 轮）、mAP50-95=0.40370（100 轮），高于旧运行与 v3 的记录。当前配方 l/1280/batch=4、workers=4、AdamW、lr0=0.0001、nbs=16；上限 5000，学习率前 200 轮衰减、第 101 轮关闭 Mosaic，epochs 仍影响双头损失日程。已实施定向清洗并保留完整审计。predict.py 默认 v4 best.pt，mAP50 候选 epoch50.pt 实际为第 51 轮（0.59183）；第 42 轮未留存。当前先使用现有单个权重，不立即重训。弱类采样、多标签后处理、双指标留存和不同预训练基底尚未启用。详见 `docs/归档/04_数据与调参经验.md#record-2`。

## 常用命令

```bash
# 训练（三模态五通道融合入口；命令变体见 main.py 顶部文档字符串）
uv run python train1.py

# 断点续训：把 main.py 顶部 RESUME_PATH 设为
# 同配方中断运行的实际 weights/last.pt；完成后剥离优化器的权重不能原状态恢复
uv run python train1.py

# 默认生成清洗审阅包；落位需指定已确认记录的 --apply-review
uv run python 准备三模态数据集.py
```

五通道模型无法用只给 visible 图的通用 `yolo val/predict` 命令，验证与提交推理需经自定义加载器。

环境：Windows 11 + RTX 5080 16G，ultralytics 8.4.x（YOLO26），torch 2.14.0+cu132，`device=0`。依赖由 `uv` 管理（`pyproject.toml` + `uv.lock`），完整机器配置见 `docs/归档/05_旧运行与环境记录.md#record-1`。

## 数据布局

```text
数据集/                        # 官方数据（.gitignore 已忽略）
├── 训练集/
│   ├── AIC2026_Train_2000/     # 2000 组训练数据（有标注）
│   │   ├── visible/            # RGB 8-bit，png 1851（1920×1080）+ jpg 149（640×360）
│   │   ├── infrared/           # 红外（存为三通道，本质同一热度灰度图复制三份）
│   │   ├── depth/              # 深度 16-bit（I;16），单位毫米；0 或极小值 = 无效深度
│   │   └── labels/             # 原始标注（2026-04-15 下载）
│   └── new_labels_2000/        # 更新版标注（2026-09-02，三模态训练使用此版）：382/2000 文件有差异
└── 测试集/AIC2026_PHASE_1_1000/  # 1000 组初赛测试数据（无标注），visible/infrared/depth
                                  #   png 845（1920×1080）+ jpg 155（640×360）

datasets/                       # 落位后的训练数据（不入库，仅 data.yaml 入库）
├── train/                       # 1744 组：images、infrared、depth、labels
├── val/                         # 256 组：images、infrared、depth、labels
└── data.yaml                    # RGB / 红外 / 深度五通道配置
```

数据已落位：datasets/{train,val}/{images,infrared,depth,labels}，1744/256 划分保留；图像硬链接官方源，标签为新版独立清洗副本。57 个标签文件变化，train 12628 框、val 2566 框；审计与备份在 runs/dataset_cleaning/20260917_012026。日常训练无需运行准备脚本，其默认命令只生成审阅包；历史重建函数不能覆盖清洗标签。划分依据与剩余局限见 docs/归档/04_数据与调参经验.md#record-1。

- 12 类：0=person, 1=boat, 2=animal, 3=seat, 4=sign, 5=bicycle, 6=car, 7=ball, 8=light, 9=garbage can, 10=uav, 11=tricycle。**索引一经训练即固定，禁止调换或增删**。
- 深度 png 必须按原始 16-bit 位深读取（`I;16`），不能当 8-bit 灰度图；但 `depth/` 里另有 149 张 jpg 是 **8-bit RGB**（640×360），不含 16-bit 数据，读深度要按扩展名分支。
- **各模态目录都混着两种分辨率**：png 1920×1080 与 jpg 640×360，两种词干不重叠、各自独立成样本（不是缩略图）。读数据不能假设统一尺寸。
- `datasets/data.yaml` **不写 `path:` 键**：写了会被原样使用、不相对 yaml 解析，`path: .` 会落到当前工作目录导致 `images not found`（2026-09-15 已修）。细节见 `docs/归档/04_数据与调参经验.md#record-4`。
- `datasets/{train,val}` 使用更新版 `new_labels_2000`；原始版仅保留在 `数据集/训练集/AIC2026_Train_2000/labels`。准备脚本会原子替换落位标签，避免经硬链接改动官方旧标注。

## 提交格式与比赛硬性约束

- 预测结果：每张测试图一个**同名** TXT（`00000004.jpg` → `00000004.txt`），每行 `class_id cx cy w h confidence`（比训练标签多一个置信度字段）。无目标也要交空 TXT；每图最多 100 框；打包 zip 上传。
- 禁止：使用官方 2000 组以外的训练数据；用测试集训练/标注；调用在线服务或 API；多模型投票/平均式集成。
- 允许：ImageNet / COCO / Objects365 等公开预训练权重。
- 三模态几何增强必须同步（同一变换应用于三张图与标签，保持空间对齐）。
- 成绩低于官方基线会被判无效。

## 代码与注释风格

风格基准：**仓库内 `main.py`**（2026-09-15 用户钦定，代码和注释照它的样子写）；`AGENTS.md` 已按此更新为完整书面规范。要点：

- PEP 8 通用原则 + 四空格缩进；不机械拆行，语义完整的短表达式优先一行；避免过度抽象与一行式压缩。
- 模块开头：中文三引号文档字符串，首行一句话概括职责，随后按需写初始化/生命周期约束；训练入口在文档字符串内收纳执行命令与 CLI 命令变体（训练/断点续训/推理）。导入按 `# 内置库` / `# 三方库` / `# 自己的模块` 分组标记排列，无对应导入时省略该组；影响导入顺序的副作用设置（如环境变量）写在内置库组并注明原因。
- 模块级常量全大写 + 显式类型标注，每个常量上方一行中文注释写明取值依据。
- 训练代码必须包在 `if __name__ == "__main__":` 内（Windows DataLoader 子进程会重新导入脚本），上方注释说明原因。
- `model.train(...)` 全参数显式写出，按「一、模型、数据与训练时长」至「八、其他任务参数」分节；**每个参数行尾注释必须写明取值依据**（本机实测显存/耗时、硬件规格、数据集约束、与框架默认值的差异）；detect 任务不用的参数集中在末节并注明不使用。
- 函数/类：Google 风格中文文档字符串（首行概括，按需 `Args` / `Returns` / `Raises` / `Note` 区块；不重复类型提示、不加空区块）；**所有函数显式声明返回类型**，无返回值写 `-> None`；仅确实无法确定时才用 `Any`。
- 命名：函数/变量 `snake_case`、类 `PascalCase`、模块级常量全大写；配置值、函数参数、返回值、重要状态必须显式类型提示。
- 行注释：简洁中文、紧邻所解释代码（通常在上方）；只写代码本身表达不了的内容（业务目的与固定值依据、前置条件与顺序、平台兼容 workaround、实测数据）；不复述代码，删掉后含义仍明确的注释不要写。
- 动手前先看同模块/相邻模块既有写法（`三模态训练.py`、`准备三模态数据集.py` 已按此风格），不引入另一套模式；不顺手整理无关代码。
- 日志/打印必须具体到对象和结果（如 `预标注完成，共产出 N 个框`），不写干巴巴的"成功/失败"。
- 提交信息：简短、祈使式中文摘要（如 `补充忽略规则`），每个提交只处理一个明确目标。

## 代码与仓库约定

- `.gitignore` 已显式排除数据集目录（`数据集/`、`datasets/`）、训练输出（`runs/`）、`*.cache` 及图片/txt/权重等——仓库只跟踪代码和文档，**严禁 git add 数据集或大文件**（GitHub 单文件上限 100MB；历史上曾因暂存数据集使 .git 膨胀到 17GB，2026-09-04 已清理重写）。唯一例外：`datasets/data.yaml`（12 类配置，训练必需）。
- `docs/比赛资料/视频讲解.mp4`、`docs/比赛资料/参赛选手承诺书.pdf` 仅存于磁盘，不入库。
- 当前完成运行在 `runs/detect/AIC_RGBIRDepth_yolo26l_1280_v4_clean_lr1e4/`；旧目录与 v3 保留历史运行。
- 远程仓库：`origin = github.com/crazyzzpdt/AICOMP`（主分支 `main`，普通 push 即可，勿用 force）。
- 团队文档与提交信息使用中文，新增文档、注释请保持中文。

## Windows 训练注意事项（实战沉淀，详见 docs/归档/04_数据与调参经验.md#record-3）

- 训练代码必须包在 `if __name__ == '__main__':` 里，否则 DataLoader 多进程递归建进程直接报错。
- Ctrl+C 中断的退出码 `0xC000013A` 属正常现象；加载 last.pt + `resume=True` 可无缝续训。
- `workers` 取 0~6；爆 OOM 就把 batch 减半回退。
- 修改数据集后删掉 `labels.cache`，下次训练自动重建。

## 参考文档

- `docs/归档/05_旧运行与环境记录.md#record-1` —— 本机训练环境记录（CPU/GPU/内存/SSD、CUDA 与 torch 版本、uv 依赖解析结果）
- `docs/归档/05_旧运行与环境记录.md#record-2` —— 开训前释放 CPU/内存/显存的操作清单与实测记录（进程、服务、自启项、计划任务、暂停 Windows 更新，含一键恢复；重启后 D/E/F 段仍生效，A/B 段需重跑）
- `docs/归档/04_数据与调参经验.md#record-1` —— 数据集成分、划分方法与类别分布统计（两套标注差异、重建方式）
- `docs/归档/04_数据与调参经验.md#record-2` —— 三模态融合训练的配置依据与数据集复核结论（同名三模态尺寸一致：1851 组 PNG 为 1920×1080、149 组 JPG 为 640×360；五通道烟雾训练实测）
- `docs/归档/04_数据与调参经验.md#record-3` —— 训练/标注/复盘全流程实战经验与脚本模板（配对清洗、预标注、断点续训、results.csv 复盘等）
- `docs/归档/04_数据与调参经验.md#record-4` —— YOLO 数据集目录与 yaml 规范
- `docs/参考/Ultralytics训练参数参考.md` —— 训练与增强参数速查（`main.py` 的注释即逐项对应此表）
- `docs/归档/04_数据与调参经验.md#record-5` —— 数据采集网站、数据集结构与划分规则
- `docs/归档/05_旧运行与环境记录.md#record-3` —— 官方赛题 PDF 任务要求摘录（输入输出格式、mAP 计算口径、约束条件）
- `docs/比赛资料/2026 AIC 视觉多模态目标检测参赛手册.md` —— 赛题手册（任务定义、类别表、评分规则、赛程、材料要求）
- `docs/比赛资料/官方赛题规则.md` —— 官方规则页正文**原文**（2026-09-15 抓取自 https://www.aicomp.cn/tracks/tracks-1/3700.html ，未改写；含数据格式细节、mAP 计算口径、各阶段提交清单、参考文献、红线约束）
- `docs/比赛资料/视频讲解-完整内容.md` —— 官方赛题视频逐字稿（数据集设计、三阶段难度递增说明）
