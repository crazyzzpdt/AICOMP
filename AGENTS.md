# 仓库协作规范

## 当前优先约束

2026-09-20 v12复盘与v13实施（最高优先）：用户补充v11线上44.4710。v12在第10轮按门槛停止，但第10轮本身仍创新高：mAP50=0.55075、mAP50-95=0.33257，ball AP95=0.45121；它只比0.34门槛低0.00743。根因是用v4旧256张验证集轨迹标定当前291张验证集，跨划分门槛没有可比性并误停上升模型。v13保持v12全部训练变量不变，仅移除screening_thresholds，继续由AP95 best和patience100控制；运行名AIC_RGBIRDepth_yolo26l_1280_v13_native_full，预测默认v13 best/1280×1280/predict_v13。不得在本轮同时加入弱类采样、RGB架构或其他参数，否则无法验证硬门槛根因。未执行测试、训练、推理或复评，未提交推送。详见docs/v12复盘与v13训练方案.md。

2026-09-20 v11复盘与v12实施（最高优先）：v11按阶段筛选在20轮正常停止，历史最佳为第5轮mAP50-95=0.32668；关闭cls_pw使最佳轮ball AP95升至0.41520，但总体低于v10的0.33592，说明类别权重不是主要瓶颈。v12不续训v11，回到唯一有55.6000线上证据的v4原生1280方形几何：官方YOLO26l五通道首层融合、清洗版1709/291、batch4/nbs16、AdamW 1e-4、200轮余弦、前100轮Mosaic0.5/scale0.3/translate0.1、后100轮关闭Mosaic、HSV全0、cls_pw0、patience100。保留固定0.8/0.2双头、ball语义迁移、FP32单标签验证和现有审计；第10/20轮按历史最佳AP95 0.34/0.36筛选。此次不叠加弱类采样，避免把几何恢复与采样混成不可解释的实验。predict.py默认v12 best、1280×1280与predict_v12。未执行测试、训练、推理或复评，未提交推送。详见docs/v11复盘与v12训练方案.md。

2026-09-20 v10复盘与v11实施（最高优先）：v10完成49轮，最佳第19轮mAP50-95=0.33592，用户线上50.9240；训练损失继续下降而验证损失恶化，30轮无AP95新高后正常早停，不是“轮数不足”。v10首轮总体AP95=0.25831，v4首轮=0.29976，因此禁止使用“首轮必须0.4”的硬门槛。main.py现为v11单变量诊断：保持v10全尺寸早期融合、数据、增强、优化器和推理一致，只将cls_pw从0.25改为0；以历史最佳AP95在第10轮≥0.31、第20轮≥0.34作为阶段筛选，未达标写出当轮结果后停止。该门槛用于节省弱配方训练时间，不保证线上60分；不得将首轮波动误当最终上限。predict.py默认v11 best与predict_v11。未执行测试、静态执行检查、训练、推理或复评，未提交推送。详见docs/v10复盘与v11筛选方案.md。

2026-09-20 同日后续优先：用户要求不仅恢复v4，还要落实经验增强及按多数1920×1080原图统一输入。v10已替换尚未训练的1280草案：五通道首层早期融合不变；内容1920×1080/张量1920×1088，batch2/nbs16、AdamW全模型1e-4、120轮余弦、patience30从首轮计数。前40轮矩形Mosaic0.25/scale0.3/translate0.1，第41轮关拼图但保留scale0.1/translate0.025；RGB专用HSV0.01/0.15/0.15，IR/Depth不改颜色，fliplr0.5。保留cls_pw0.25、球类语义迁移、固定双头、既有清洗1709/291和审计，不重洗。检查点版本yolo26l_early_rect_v10_2；预测自动走共用矩形/NMS批量后端，1920/height1080、FP32、batch4，用户OUTPUT_PATH=predict保留。历史v4/v9/D-FINE预测兼容；禁止将下方1280草案恢复为当前。未执行测试/静态执行检查/训练/推理或复评，未承诺60分，未提交推送。具体说明见docs/v9复盘与v10训练方案.md。

2026-09-20 v9复盘与v10实施（优先于下方历史）：v9已完成102轮，最佳第63轮mAP50-95=0.34952，用户线上53.4750，未超过v4的55.6000。v4旧验证256张，v9为291张，不直接按AP差认定架构因果。读取Git 86411da及CPU检查点确认v4为五通道首层早期融合，并非门控结构；v9本地prediction.json与best.pt哈希匹配，未发现漏模态或输出压缩影响输入的证据。main.py已改v10 early_fusion：官方COCO重迁移、1280、batch4/nbs16、AdamW全模型1e-4、200轮余弦、原生Mosaic0.5/scale0.3/translate0.1/HSV0、cls_pw0.25、固定双头0.8/0.2、patience40从首轮计数，第101轮仅关Mosaic。保留球类同义迁移、审计1709/291、FP32单标签验证及双指标权重；predict.py默认v10 best/1280/v4路径，用户OUTPUT_PATH=predict不变。只读取产物/可信权重与源码并修改代码文档；未运行测试、AST/compile/import检查、模型前向、复评、训练或推理，不能声称v10已验证运行或达到60分。详见docs/v9复盘与v10训练方案.md。未新增数据清洗或人工200张复核，不自动提交推送。

2026-09-19 完整配置与短流程追加：用户明确要求保留完整YOLO配置并授权本轮测试。main.py已显式覆盖本机default.yaml全部键，补model/mode/dis、修正dfl为L1权重注释，核心v9数值不变。固定种子短流程在runs/smoke_main/entry_smoke-2完成1训练批/1优化器调用/1验证批，报告passed、输入[2,5,1088,1920]、无权重输出；未验证跨运行确定性、稳态累积、全程收敛或保存恢复。第一次复跑因测试包装model形参冲突失败，修包装后通过。此次授权不扩展为自动搜索超参数或全量训练；后续仍按用户授权控制测试范围，详情见v9文档。

2026-09-19 模型接口修正：用户授权过一次main.py冒烟测试，构建模型即因_predict_once参数不匹配失败，训练/验证均0批，未保存权重。用户随后要求修复，已同步aic/model.py签名到本机Ultralytics的(x, profile, embed)，删除visualize形参及多余实参，父类调用使用关键字；其余融合结构/配方不变。修正后未复跑测试，不能宣称训练流程已通过。默认禁测仍有效；上述一次授权不代表可持续运行额外测试。修改尚未提交。

2026-09-19 报错修正追加（优先于下文未修正记录）：用户提供真实尺寸校验报错后，已将main.py的imgsz改为IMAGE_HW[0]=1920，常量按宽、高注释；由入口路径生成绝对PROJECT_PATH，解决全局runs_dir重复拼接，不修改全局设置。其余v9配方、清洗数据及预测默认值不变，不删除或移动旧输出。只核对代码与差异，未运行测试、AST/compile/import、训练或推理；本次修改尚未提交推送。

2026-09-19 文档与提交追加：用户授权同步本轮代码整理及文档并推送GitHub，不授权据此更改训练参数。当前main.py为IMAGE_HW=(1920,1080)、image_width=IMAGE_HW[0]，但imgsz仍为IMAGE_HW[1]，存在尺寸校验阻塞；建议imgsz改第一项与宽高注释同步，尚未代改，不得宣称可直接开训。v9完整数据流程保留，现有审计57文件修订（64裁剪/1去重/1改类）、1709/291；额外约200张人工复核未完成。本轮只读取代码及已有审计，不重做清洗、不执行测试或模型任务。提交包含明确的代码迁移与删除、相关文档，不夹带数据/权重/运行产物。

2026-09-19 入口收敛（优先于下方历史）：根目录仅保留main.py与predict.py。预测、后处理、图片/TXT保存、ZIP打包及旧YOLO/D-FINE推理均集中predict.py；aic/model.py保存可序列化模型及共用几何/NMS，aic/data.py提供读图/类别/指纹，aic/training.py集中当前训练器与数据集。tools/prepare_dataset.py保留数据审计清洗，以uv run python -m tools.prepare_dataset运行。删除原根目录中转文件及不用的旧D-FINE训练循环、旧YOLO训练器，不删除数据或权重；原文件可从08a4180恢复。aic.model注册旧三模态融合模块名供旧v9权重反序列化；源码指纹已变，整理前中断训练须用原代码快照恢复，不绕过校验。默认参数不变，未运行测试、AST/compile/import、训练或推理。说明见docs/代码结构与运行.md。

2026-09-19 代码清理：按用户授权删除当前入口无引用的旧v5组件训练优化.py，可从Git提交08a4180恢复。其余五个文件均保留当前依赖：融合训练.py负责v9训练，三模态融合.py负责可序列化模型和共用预处理，三模态训练.py提供共享读图/数据集/加载器，三模态预测.py提供预测与打包，准备三模态数据集.py提供类别常量/指纹/清洗审计。D细化训练.py仍供历史D-FINE权重预测。历史段落提及保留训练优化.py是当时状态，不应因此重新添加。仅阅读引用和Git差异，未执行任何测试或静态执行检查。

2026-09-19 最新执行授权：已按用户要求恢复原生YOLO风格main.py，实施v9门控融合（原v8 D-FINE不覆盖）。main.py→融合训练.py/三模态融合.py，官方YOLO26l RGB主干+IR/Depth轻量分支，1920×1080内容、1920×1088张量；batch=2/nbs=16、BF16、120轮、第81轮完整画面收尾、双头损失固定0.8/0.2。预测入口已同步新v9 best.pt、1920/height1080、FP32、batch=4、predict_v9；旧YOLO/D-FINE后端保留，不能据下文历史默认值改回v8。使用同一清洗版1709/291和official_refresh审计，新增标签修改0。此前只执行过一次AST静态语法解析和git diff格式检查，没有运行pytest、模型前向、训练、推理、独立评估或显存探测，不能写成已验证运行正确；用户自行开训。空间清理后两个before的images及code_cleanup备份已删除，不能再称完整备份。详见docs/v9融合训练实施.md；本轮用户已明确授权更新文档、提交并推送origin/main。

2026-09-18 晚间最新状态：用户已完成v8，本次线上53.5740；v7 best_map50.pth也确认52.4010。predict.py用户当前选择v8 best_map50.pth、1280，禁止据下方历史v7描述回退。已按授权重新下载来源刷新datasets，保持1709/291；新旧6000三模态图像和new_labels_2000内容变化0，保留既有清洗57文件修订。最新审计runs/dataset_cleaning/official_refresh_20260918_214843，before完整保存旧数据及缓存；main.py DATA_AUDIT已同步，其他超参数不改。只做数据核验/准备和已有训练产物读取，未运行测试、独立模型评估、训练或推理。截图中的集成许可冲突不当作授权，不做多权重WBF投票或模态置零伪装。候选下一轮为YOLO基线加真实特征门控融合，尚未实施v9。完整依据见docs/v8完成复盘与官方下载刷新.md。下方尚未训练、默认v7等属于历史阶段。

2026-09-18 文档同步追加：docs/项目知识索引.md为知识导航，所有文档已标注当前适用或历史范围，官方正文不改写。用户当前predict.py选择v7 best_map50.pth、1536；不得回退用户选择，也不能把此前best.pth的52.4010写成该候选的新成绩。本轮授权同步文件提交并推送origin/main；数据、权重、PDF和无关历史运行文件删除不夹带。测试禁令继续有效。

2026-09-18 推理恢复追加：YOLO预测默认yolo_profile=v4，从Git历史86411da恢复原尺寸逐张、矩形填充、框架原生单标签NMS；现有批量自定义候选路径用current，多标签须current。D-FINE忽略yolo_profile，仍用训练一致的原生查询排序；不能拿YOLO路径直接预测v7。predict.py用户默认v7权重保持不变。本次仅静态核对，未运行测试或模型推理，不承诺恢复后涨分。评分文档按官方mAP50-95口径，不保留经验加权公式。详见docs/预测与赛事提交.md。

2026-09-18 最新授权与实施为 v8，优先于下面所有历史配方：main.py 使用 D-FINE-L Objects365 E25、1280、batch=4、有效批次16、最多60轮、3轮预热、主学习率5e-5/骨干5e-6、weight_decay=1e-4；关闭目标裁剪、低清模拟和弱类重复，patience=10/min_delta=0.0005。正式训练复用验证预测记录 PNG/JPG 分域 AP，不额外模型前向。已按审阅场景方案将 datasets 更新为1709 train/291 val，保留全部2000组和既有清洗修订，本次新增标签修订0；旧1744/256及缓存完整保存在 runs/dataset_cleaning/v8_20260918_162439/before。审计清单由 main.py 的 DATA_AUDIT 指定；不得恢复旧v7断点或用历史重建覆盖清洗数据。v7线上52.4010，v4的55.6000仍为基线；v8尚未训练，不保证提分。predict.py保留用户v7默认值，预测v8须显式权重和imgsz=1280。没有自动测试、评估、推理或训练；下一步用户自行运行 main.py。详见 docs/v8训练方案与数据清洗.md。下方“当前”和“不改划分”等均是对应历史阶段约束，禁止据此撤回用户本轮授权的v8。

v7后续授权追加：仅boat/garbage can受限重复，倍率1.5/1.25为限额前期望；单图最多2次、额外总量≤10%、文件名近似组额外≤4张，第31轮关闭。学习率用optimizer_step按采样计划的真实优化步计数，累积尾组按当轮样本数归一化；weight_decay=0.000125；D-FINE原生损失权重在main.py显式配置，沿用官方默认。sampling_history.csv只在用户正式训练时写出；不运行测试或额外评估。不能恢复新增配方前的旧v7断点。

2026-09-18 后续已授权 v7：main.py 仍使用 D-FINE-L Objects365 E25，1536、batch=2、有效批次16、40轮、3轮预热，第31轮关闭尺度/完整目标裁剪/低清模拟；新增训练增强位于 D细化训练.py。v6两份最佳权重线上55.1490/55.1360，未超v4的55.6000。2000个当前标签匹配清洗审计，当前数据签名匹配v6记录，不能声称v6未用清洗数据。不自动测试、评估或训练，不自动改数据划分；下一步用户运行main.py。新模型预测须显式1536，用户predict.py默认v6权重不改。详见docs/v7训练方案与清洗核验.md。下面v6/v5入口配置为历史，禁止据此覆盖v7。

2026-09-18 当前入口已切换为 v6 D-FINE-L：main.py → D细化训练.py，官方 Objects365 E25 预训练、RGBIRDepth 五通道、1280、batch=2、有效批次16、60轮、3轮预热、AdamW主1e-4/骨干1e-5。源码固定在 vendor/D-FINE（不入库），依赖与预训练权重已准备。predict.py 兼容旧 YOLO .pt 与新 D-FINE .pth，用户原有默认权重路径保留。v5 best.pt 已确认线上51.0830，v4 55.6000仍为正式基线。没有执行模型前向、测试或训练，下一步由用户运行 main.py；禁止以旧段落再次改回 v5。详见 docs/D-FINE三模态实施方案.md。下文 v5 参数为历史实施记录。

用户明确要求控制工具调用与 token：默认不运行 pytest、冒烟测试、AST/compile/import检查、模型前向、模型评估、额外推理、训练或显存探测；不为调用技能新建测试与评估脚本。只读取已有训练产物，必要时检查引用和实际差异。提交前仅检查git状态、文件清单和实际暂存差异，不因技能要求扩大验证范围。只有用户明确要求才执行任何测试或评估；必须如实说明未测试，不宣称已验证运行正确性。

v5_full 已完成 234 轮，最佳 mAP50-95=0.40625（34 轮），最高 mAP50=0.59835（41 轮）；后期过拟合，不能描述为全面胜过 v4。ball 最终 best.pt AP50=0.423。详见 docs/v5训练完成复盘.md。下方训练设置是已完成配方，不代表新的重训授权。

最新实施状态：main.py 已改为 v5_full，由训练优化.py 的 OptimizedMultimodalTrainer 提供弱类受限重采样、原尺寸同步裁剪、RGB 增强、模态随机缺失与骨干 0.2 倍学习率。主学习率 0.0001、cls_pw=0、mosaic=0.25、scale=0.2、translate=0.05、patience=200，第 161 轮关闭 Mosaic/目标裁剪/模态缺失；额外保存两种 AP 最佳权重与逐类指标。predict.py 新增可选 --multi-label，默认关闭且保留用户权重路径。线上 v4=55.6000、epoch50=53.2540，保留 v4 为正式基线。目标 60 分以上，不保证提分；v5 仅静态检查，用户自行开训。详见 docs/v5训练方案与球类诊断.md，后面 v4 复盘保留为历史。

代码与注释风格以仓库内 `main.py` 为基准（2026-09-15 定）：新写或修改代码前先通读 `main.py` 和本次修改涉及的 `aic/`、`predict.py` 或 `tools/` 模块，模仿既有写法。

## 项目结构与模块组织

- `main.py`：唯一训练入口，保留原生YOLO分节参数，调用`aic.training`。
- `predict.py`：唯一预测入口和预测实现，负责推理、坐标恢复、图片/TXT保存与提交ZIP打包；已有结果不覆盖。
- `aic/model.py`：模型结构及训练/预测共用几何处理，保持旧v9模型模块名兼容。
- `aic/data.py`：类别常量、共享三模态读取与文件指纹，不依赖训练器或清洗工具。
- `aic/training.py`：当前融合训练器、矩形数据集和正式轮末验证；不单独启动。
- `tools/prepare_dataset.py`：保留有记录的审计清洗能力，日常训练无需执行。
- 评估模型.py、tests/ 与 runs/config_checks 已移出工作位置，其code_cleanup备份随后经授权删除；不是训练或预测依赖。
- `datasets/`：仅包含 `train/`、`val/` 与必需的 `data.yaml`；仅 `datasets/data.yaml` 入库。
- `数据集/`（官方原始数据）与 `runs/`（训练产物）不入库；`orgin_models/` 存放本地权重（不入库）。
- `docs/`：资料、复盘与经验文档；官方赛题资料在 `docs/比赛资料/`。

## 构建、测试与开发命令

- `uv sync`：根据 `pyproject.toml` 和 `uv.lock` 创建或更新环境。
- `uv run python main.py`：开始训练（在项目根目录执行）。
- `uv run python predict.py`：预测官方初赛测试集并打包；默认权重由用户维护。新模型显式使用 `--weights runs/detect/AIC_RGBIRDepth_dfine_l_1280_v6/weights/best.pth --output predict_v6`，不自动选择最新运行。
- `uv run python -m tools.prepare_dataset`：使用官方新版标注更新 `datasets/train`、`datasets/val` 和五通道配置。
- 断点续训：当前 RESUME_PATH 仅填写同配方中断运行的实际 last.pt，恢复写入新目录；完成/早停检查点拒绝恢复，更换配方保持 None。last.pth 属于历史D-FINE。
- 已完成 v5 配方：AdamW、lr0=0.0001（骨干 0.2 倍）、nbs=16、batch=4、patience=200；MAX_EPOCHS=5000，学习率前 200 轮衰减，第 161 轮关闭 Mosaic/目标裁剪/模态缺失。训练/验证各 4 个加载进程、预取 1 批、关闭锁页；epochs 仍影响双头损失日程。
- 初始化澄清：v5 从 orgin_models/yolo26l.pt 官方 COCO 预训练重新迁移，resume=false，未续训 v4；重新初始化不保证消除过拟合。弱类采样、双指标留存已启用，多标签预测为默认关闭的可选开关，不同基底未启用。不自动重训，详见 docs/v5训练完成复盘.md。

## 代码风格与命名规范

遵循四空格缩进和 PEP 8 通用原则，但不机械拆分代码；语义完整的短表达式优先一行。

- 模块以中文三引号文档字符串开头：首行一句话概括职责，随后按需写初始化/生命周期约束；训练入口的文档字符串内收纳执行命令与 CLI 命令变体（训练/断点续训/推理）。
- 导入按 `# 内置库` / `# 三方库` / `# 自己的模块` 分组标记排列，无对应导入时省略该组；影响导入顺序的副作用设置（如环境变量）放在内置库组，并注释"必须在导入 XX 前设置"。
- 模块级常量全大写 + 显式类型标注，每个常量上方一行中文注释写明取值依据。
- 命名：函数/变量 `snake_case`、类 `PascalCase`；配置值、函数参数、返回值、重要状态必须显式类型提示；所有函数显式声明返回类型，无返回值写 `-> None`；仅确实无法确定时才用 `Any`。
- 训练代码必须包在 `if __name__ == "__main__":` 内，上方注释说明原因（Windows DataLoader 子进程会重新导入脚本）。
- `model.train(...)` 全参数显式写出，按「一、模型、数据与训练时长」至「八、其他任务参数」分节；每个参数行尾注释写明取值依据——本机实测（显存/耗时）、硬件规格、数据集约束或与框架默认值的差异；任务不涉及的参数集中列在末节并注明不使用。
- 入口内可用简短标记注释划分职责（如 `# 实例化模型类`、`# 训练函数`）。
- 动手前先看同模块/相邻模块既有写法，已有模式能覆盖就不引入另一套；不顺手整理无关代码。

## Google 风格文档字符串

函数与类的文档字符串统一 Google 风格中文：首行概括职责；参数、返回值、异常、前置条件或副作用确实存在时，按需使用 `Args` / `Returns` / `Raises` / `Note` 区块，不加空区块、不重复类型提示已表达的信息。

```python
def read_image(path: Path, flags: int) -> np.ndarray:
    """读取单张图像，并兼容 Windows 中文路径。

    Args:
        path: 待读取的图像文件。
        flags: OpenCV 图像读取模式。

    Returns:
        成功解码的图像数组。

    Raises:
        FileNotFoundError: 图像不存在或无法解码。
    """
```

## 行注释风格

行注释使用简洁中文，紧邻其解释的代码。只写代码本身表达不了的内容：

- 业务目的与固定值的取值依据；
- 操作的前置条件与执行顺序；
- 本机实测数据（显存、耗时、阈值）；
- 平台兼容 workaround 及原因（如 Windows 中文路径、子进程重导入）。

不要逐字复述代码，也不为显而易见的赋值堆注释；删掉注释后含义仍明确的注释不要写。

日志必须具体描述对象和结果，如 `训练数据复制完成，共 N 张`，避免只写 `成功`、`失败`。

## 检查边界

不自动进行测试、静态执行检查或额外模型评估，遵守上方当前优先约束。框架正式训练内部的 val=True 用于选权重和早停，与额外 pytest、独立复评不同，不因清理 tests 而关闭。

## 提交与 Pull Request 规范

提交信息使用简短、祈使式的中文摘要，例如 `补充忽略规则`。每个提交只处理一个明确目标，并描述用户可感知的结果。

提交或创建 Pull Request 前必须检查实际差异和暂存内容，确认没有夹带无关修改、运行产物或敏感数据。

## 安全与数据红线

- 禁止提交凭据、Cookie 或本地环境路径；机器相关配置保存在已忽略的运行时文件中。
- 严禁入库：`数据集/`（官方数据）、`runs/`（训练产物）、权重（`*.pt`）、视频/PDF（`docs/比赛资料/视频讲解.mp4`、`docs/比赛资料/参赛选手承诺书.pdf` 仅存磁盘）。唯一例外：`datasets/data.yaml`。
- 比赛红线：仅可使用官方训练集；禁止用测试集训练/标注；禁止调用在线服务或 API；禁止多模型投票/平均式集成；赛事数据禁止泄露或上传公开平台。
