# 仓库协作规范

## 项目结构与模块组织

`CBEA.py` 是应用程序入口。浏览器页面对象位于 `pages/`，业务编排与 API 代码位于 `services/CBEA/`，配置模型位于 `data_models/`，共享工具位于 `utils/`，日志能力统一放在 `logger/`。

前端 HTML、CSS 和 JavaScript 按功能组织在 `templates/CBEA/dianxiaomi/`。运行配置与图标放在 `config/`；生成的表格和浏览器运行产物放在 `outputs/`、`debug_data/` 或 `user_data/`；项目文档放在 `文档/`。

`跨境电商自动化/`、`build/` 和 `dist/` 属于生成的发行产物，不是源代码。除非任务明确要求处理构建结果，否则不要直接修改这些目录。

## 构建、测试与开发命令

- `uv sync`：根据 `pyproject.toml` 和 `uv.lock` 创建或更新 Python 3.14 环境。
- `uv run playwright install chromium`：安装自动化流程使用的 Chromium 浏览器。
- `uv run python CBEA.py`：在本地启动 FastAPI 与桌面自动化应用。
- `uv run python _test_CBEA.py`：运行当前端到端回填烟雾流程。该流程依赖真实浏览器状态，不属于单元测试。
- `uv run pyinstaller CBEA.spec`：在 `dist/` 中构建 Windows 发行包。

## 代码风格与命名规范

遵循四空格缩进和 PEP 8 的通用原则，但不要为了满足固定行宽而机械拆分代码。短函数签名、短条件表达式以及语义完整的 Playwright 链式调用优先写在一行；只有表达式明显过长、存在多个独立步骤，或分行后能显著提升结构辨识度时才换行。

新增或实质修改代码前，先检查同一模块及相邻模块的既有结构、术语、日志和异常处理方式。已有局部模式能够覆盖当前需求时，不要引入另一套写法，也不要顺手整理无关代码。

Python 模块应以简洁的中文三引号文档字符串开头，说明模块职责以及必要的初始化或生命周期约束。导入按以下顺序分组，并使用项目约定的分组标记：

```python
# 内置库

# 三方库

# 自己的模块
```

没有对应导入时可以省略该分组。保持导入有序，不进行与当前任务无关的导入清理。

模块、函数和变量使用 `snake_case`；类和 Pydantic 模型使用 `PascalCase`；模块级常量使用 `HOST`、`PORT` 这类大写命名。配置值、函数参数、返回值和重要应用状态必须显式添加类型提示。

所有函数和方法都必须显式声明返回类型。没有业务返回值的函数写成 `-> None`，不要省略返回注解；协程声明其 `await` 后得到的实际结果类型，生成器使用 `Iterator[T]`，异步生成器使用 `AsyncIterator[T]`，永不正常返回的函数使用 `NoReturn`。只有确实无法确定类型时才使用 `Any`，不得用 `Any` 代替正常的类型设计。

优先使用直接、清晰的控制流程，避免不必要的抽象、过度封装和晦涩的一行式写法。单行优先不等于压缩逻辑：一行代码应表达一个完整且容易理解的操作。

异步操作必须显式使用 `async` 和 `await`。管理 Browser、Context、Page、日志处理器、队列或其他资源时，使用生命周期安全的 `try`、`except` 和 `finally`。将底层异常转换为业务异常时，使用 `raise ... from e` 保留异常原因。

共享应用状态必须显式初始化并添加必要的类型提示。不得并发执行的工作流应使用锁保护。生命周期变化和可恢复故障应通过项目日志记录，日志内容使用清晰、具体的中文。

## Google 风格文档字符串

Python 文档字符串统一使用 Google 风格，不再使用 Sphinx reStructuredText 的 `:param:`、`:return:` 或 `:raises:` 字段指令。

文档字符串的第一行应使用简洁中文概括职责。参数、返回值、异常、前置条件或副作用确实存在时，再按需使用 `Args`、`Returns`、`Raises`、`Note` 和 `Examples` 区块；不要为了形式完整而添加空区块。

类型已经由 Python 类型提示表达，文档字符串不要重复书写参数类型。参数说明应解释业务含义、允许值或调用约束；返回值说明应解释返回结果在业务中的含义，而不是重复 `str`、`bool` 等类型名称。

普通方法通常使用以下结构：

```python
async def change_parent_sku(self, parent_sku: str) -> str:
    """填写 Parent SKU。

    Args:
        parent_sku: 工作流生成的 Parent SKU。

    Returns:
        已填写的 Parent SKU。
    """
```

复杂方法只补充真实存在且对调用方有价值的信息：

```python
async def add_product_imgs(self, img_urls: list[str]) -> list[str]:
    """通过网络链接上传产品图片。

    Args:
        img_urls: 要上传的产品图片链接列表。

    Returns:
        上传完成的产品图片链接列表。

    Raises:
        TypeError: 图片链接不是列表。
        ValueError: 图片列表为空或包含无效链接。
        TimeoutError: 重试后仍未检测到有效图片。

    Note:
        该方法只修改页面状态，不负责保存商品。
    """
```

仅在调用方式不直观、容易误用或公共接口确实需要演示时添加 `Examples`。不要给简单的属性赋值或一眼即可理解的私有辅助方法编写冗长示例。

## 行注释风格

行注释使用简洁中文，并紧邻其解释的代码，通常放在操作上方。注释应说明代码本身无法充分表达的内容，包括：

- 业务目的和业务固定值；
- 操作的前置条件与执行顺序；
- 页面生命周期和资源释放要求；
- 并发边界与锁的作用；
- 动态选择器、脆弱定位和规避动态 ID 的原因；
- 可选字段的兼容处理；
- 等待、重试、超时和失败边界；
- 数据校验规则和关键副作用。

关键页面操作可以采用“定位意图 → 页面操作”的注释方式：

```python
# 根据品牌标签定位对应的表单项，避免依赖动态生成的选择框ID
brand_label = self.page.locator("label").filter(has_text="品牌")
brand_form_item = self.page.locator(".ant-form-item").filter(has=brand_label)
# 在品牌表单项中填写品牌名称
await brand_form_item.locator("input[role='combobox']").fill(brand)
```

不要逐字复述代码，也不要为日志调用、普通 `return`、显而易见的赋值或简单条件堆砌注释。注释数量以帮助维护者理解关键意图为准；如果删除注释后代码含义仍完全明确，则该注释通常没有必要。

可以使用 `"""初始化"""`、`"""路由管理"""`、`"""系统接口"""` 这类简短三引号标记划分有意义的顶层职责。用户可见日志和注释使用中文，同时保留 FastAPI、SSE、Playwright、Context、Browser 等约定俗成的英文技术词。

日志必须具体描述对象和结果，例如 `采集任务已完成`、`Playwright工厂已关闭` 或 `制造商输入框不存在，跳过填写`，避免只写 `成功`、`失败`、`出错` 等缺少上下文的信息。

## 页面对象与业务边界

页面选择器和单个 UI 操作必须封装在页面对象中；多步骤业务流程放在 `services/`；路由主要负责校验输入、协调任务状态和返回 API 响应。

页面对象应按实际页面和业务品类划分。某个品类验证过的字段、选择器和操作流程不能默认视为其他品类的通用能力。复用前必须确认页面结构、字段含义和业务顺序一致；不一致时应建立独立页面对象或独立工作流。

前端功能资源应放在同一目录中，例如 `index.html`、`feature.css` 和 `feature.js`。修改前端时优先复用共享主题变量、布局外壳、`API`、`ThemeManager`、`LogStream` 和任务状态约定，不要复制另一套实现。

仓库没有统一格式化器或代码检查器时，应以本规范和当前模块的既有结构为准。避免无关重排、全文件格式化或仅为个人偏好产生的大面积差异。

## 测试规范

仓库当前以烟雾脚本为主，没有统一配置的 pytest 测试套件。新增自动化测试应使用 `pytest`，放在 `tests/` 中，并按 `test_<behavior>.py` 命名。

单元测试应模拟网络和浏览器边界。依赖店小秘账号、已保存浏览器状态或外部服务的测试必须明确标记。提交前运行与修改内容对应的烟雾流程，并确认失败场景下 Browser、Context、Page 和相关资源能够正确关闭。

修改功能或修复缺陷时，优先先写能够复现目标行为的失败测试，再完成最小实现并验证测试通过。若任务受修改范围限制而不能新增测试文件，可以使用临时定向测试验证行为，但不得把临时测试代码写入生产模块。

## 提交与 Pull Request 规范

提交信息使用简短、祈使式的中文摘要，例如 `统一前端系统名称`。每个提交只处理一个明确目标，并描述用户可感知的结果。

Pull Request 应包含简洁的问题与解决方案摘要、受影响流程、验证命令和关联 Issue。修改模板或界面时添加截图，并说明配置或打包影响。

提交或创建 Pull Request 前必须检查实际差异和暂存内容，确认没有夹带无关修改、运行产物或敏感数据。

## 安全与配置

禁止提交凭据、Cookie、浏览器 storage-state JSON、日志或客户表格。机器相关配置应保存在已忽略的运行时文件中。

编写配置文档时只提供脱敏示例。每次提交前检查暂存差异，确保没有泄露账号信息、客户数据、真实业务文件或本地环境路径。
