# v18后模态审计与文档提交计划

> 执行方式：主会话按已批准的只读审计方案完成，不派子代理，不新增测试或评估脚本。此计划记录已执行工作，不授权重复运行。

**Goal:** 排查v18退步后仍未确认的模态数值与质量问题，更新结果和下一候选设计，提交GitHub。

**Architecture:** 只读取既有模型产物及训练图像，CPU统计IR/Depth，不加载模型；保留全部现有训练/预测代码和数据。审计结论与未证实假设分开。

**Tech Stack:** PowerShell、NumPy/OpenCV本地图像读数、Git、Markdown。

## Global Constraints

- 固定1709/291；无数据/标签修改，不读取复赛测试图像来调参。
- 不运行pytest、AST/compile/import检查、模型前向、训练、推理或独立复评。
- 原图、逐图输出、脚本执行产物留`runs/`，不上传赛事数据。
- 不改`main.py`和`predict.py`默认值，不把已完成v18自动重跑。
- 仅提交本次文档，保留既有未跟踪会话/PDF/tests/validate_local.py。

## Task 1：确认v18结果与执行链

- [x] 读取最佳指标、CSV、停止记录与分支诊断；确认55轮、best15、AP95=0.3752101829。
- [x] 对比v14/v17，注明v14轮末val_batch1与已有batch2复评的区别。
- [x] 比对三份`aic`核心源码与运行记录SHA256，全部一致；不加载权重。

## Task 2：读取模态数值与观察样本

- [x] 从训练RGB目录建立1709组文件名清单，只解码对应IR/Depth共3418文件。
- [x] 使用`[::4, ::4]`网格统计类型、尺寸、零值、通道差、深度分位数及20米截断；保留采样口径。
- [x] 完整像素确认5张JPG深度全零，不修改或过滤这些图。
- [x] 新增4组RGB/IR目视，记录局部证据，不推断全局错位率。
- [x] 保存本地`runs/modality_audit/v18_20260922/summary.json`与执行读数代码，不入库。

## Task 3：文档与下一决策

- [x] 新增`docs/v18复盘与三模态数值审计.md`，归档证据与覆盖限制。
- [x] 更新历次模型/类别表、README、知识索引、AGENTS/CLAUDE及数据/代码状态说明。
- [x] 写出质量感知局部容错融合设计草案，尚未实现、待用户审核；不预填v19成绩。
- [x] 查看实际差异与暂存文件，范围仅本轮12份Markdown，不夹带代码、数据或运行产物。

提交与推送作为本计划的交付动作；成功状态及提交号以本轮Git记录和最终回复为准，不在提交前预写远程成功。

上述勾选仅表示对应文档/审计完成，不表示新模型通过测试或能达到0.60。模型实现和新训练不在本轮已完成范围内。
