# v19质量感知局部融合实施记录

状态：用户在v18审计后批准实施；已修改源码与入口，尚未运行测试、语法执行检查、模型前向、训练、预测或复评。由用户启动正式训练。本记录不意味着运行正确性或提分已经验证。

## 目标与边界

v18最佳AP95=0.3752101829，没有超过v14同协议batch2重载结果0.3917876103。下一步不再增加首层辅助学习率，而是实现[已批准设计](../specs/2026-09-22-quality-aware-fusion-design.md)：RGB定位主路径、IR有界局部对应、Depth显式质量支持。0.60仍是目标，不是预测。

不更换YOLO26l家族、不改1280、不重洗、不过滤样本、不改标签、不改变1709/291划分；无外部数据、测试集伪标签、多权重集成或额外预测头投票。v14仍保留为近期同协议候选，v4保留历史初赛55.6000记录。

## 一、输入及同步几何

- 磁盘`datasets/data.yaml`仍为五个传感器通道；v19内存张量为`R,G,B,IR,Depth,Depth_support`六通道。掩码是深度派生信息，不是第四种传感器。
- 原始uint16深度在量化前计算`300≤毫米值≤20000`支持。300毫米是本轮显式过滤近零噪声的设计阈值，不是由模型实验找到的最优值；JPG无毫米语义，只以灰度大于零作为支持代理。
- 深度值仍按20米量程转换到uint8；JPG仍使用灰度。IR仍按官方灰度语义读取。全零深度保留RGB/IR学习，不删除整张样本。
- 每次缩放/仿射对`Depth×Support`和`Support`用同一双线性变换，再除以支持恢复Depth。支持通道表示0–255量化后的覆盖比例，不重新当作二值标签；量化到零的位置深度也置零。防止缺测0与有效距离直接平均成伪近距离，但不能修复原始错误深度。
- 训练长边ceil缩放、四图Mosaic布局、仿射随机矩阵和框筛选沿用本机原生实现，只替换图像插值和填充值。水平翻转对全部通道同步执行；无独立随机模态变换。
- RGB填114，IR/Depth/Support填0。验证与预测共用`QualityLetterBox`、`resize_native_quality`。旧v4–v18保持原先五通道读取和各自补边，不能给旧模型套新处理。
- 版本：结构`yolo26l_quality_p3_v19_1`，预处理`rgbirdepth_support_weighted_uint8_v19_1`。不写新图像缓存；缺模态/尺寸不符立即报告。

## 二、结构及初始化

1. 官方RGB YOLO26l从`orgin_models/yolo26l.pt`重新迁移，保留`model.*`参数路径及sports ball→ball语义映射。不是从已过拟合v18续训。
2. IR复制**迁移后的**RGB第0–4层（到P3，512通道）。`0.conv.weight`对RGB输入通道求和，成为单通道卷积；其余卷积、BN参数和BN缓冲逐项继承。对应三路相同灰度的首层响应，不把随机建模参数声称为预训练。
3. Depth输入距离×支持及支持两个通道，三层stride2的Conv/GroupNorm/SiLU，输出64通道P3。仅首层前32个滤波器从RGB首层通道和继承；支持通道卷积权重初始化为零但可训练。后两层随机初始化，不声称整条深度分支都预训练。
4. IR在P3共享8维上下文投影，比较3×3邻域余弦相似性、softmax软对应；中心加2.0先验，边界屏蔽。候选仅±1个P3格（输入±8像素），不是传感器标定，无法补偿任意大错位。值特征压到64通道并逐偏移累加，避免展开64×9大张量。
5. IR和Depth残差投影用std=0.001非零小初始化，门控偏置−2保护初始RGB；不是把输入置零。Depth残差再乘P3支持比例，全缺测区域没有深度残差贡献。
6. RGB主干正常计算到P5后，仅替换颈部读取的P3缓存。RGB主干P4/P5仍由RGB计算；单个YOLO颈部/检测器整合多模态。双头训练0.8/0.2保持，正式输出仅一对多头+单标签NMS。

## 三、训练配方

| 项目 | v19设置 |
|---|---|
| 输入/数据 | 1280原生方形、清洗1709/291、无额外重复采样 |
| 资源 | batch2/nbs16、BF16训练、FP32 val_batch2、workers4/预取1、关闭锁页/缓存 |
| 优化器 | AdamW；RGB第1–10层及IR复制层2e-5；RGB首层/颈部/检测头1e-4；Depth/融合模块1e-4 |
| 日程 | 最多200轮、warmup5、余弦lrf0.01、weight_decay0.0005，原生偏置/归一化不衰减 |
| 增强 | Mosaic0.5，第101轮关闭；scale0.3、translate0.1、fliplr0.5、HSV0；不叠加MixUp/CutMix/CopyPaste |
| 选择 | AP95选best、另存best_map50；patience40，最低耐心停止20轮；阶段门槛沿用v14 |
| 比较口径 | 291张、FP32、单标签NMS、conf0.001/iou0.7/max_det100、真实内容裁框 |

新增分支后不能沿用v4的batch4显存结论。batch2是未实测的起点，用户按本机资源调整，不自动探测。有效批次16不能消除物理批次/BN统计差异，所以这不是严格单变量因果实验。辅助lr不再用v18的5e-4。

保留阶段历史最佳AP95门槛：10/.32、20/.34、50/.36、75/.38、100/.40、125/.45、150/.50、175/.55、200/.60。门槛用于预算，后期是目标而非保证；首轮未达0.4不停止。不自动放宽门槛或重试新配方。

## 四、记录及兼容

- `optimization_recipe.json`及权重签名记录预处理版本、通道顺序、深度范围、支持语义、P3位置、邻域大小和实际优化组；保存源码快照和数据审计。
- `fusion_diagnostics.csv`复用正式训练最后批次的IR门控、中心权重、Depth门控和支持均值，以及最近优化步辅助梯度范数。不是全轮平均或额外消融，不用这些数值直接证明模态有益。
- `results.csv`、`per_class_metrics.csv`、`domain_metrics.csv`、`best_validation`仍来自随训练验证。优先看同口径总体AP95，再看PNG/JPG及逐类是否退化，不能只看ball。
- 新配方只允许同结构、源码、审计、预处理签名的未完成断点恢复；v18或v14的last不能resume v19。v19恢复严格加载全部张量，不重新初始化辅助分支。
- 预测根据模型类别和签名走质量处理，拒绝缺失/不一致的v19元数据；历史YOLO/D-FINE路径保留。`predict.py`已有用户默认值不改，不自动选择最新权重。

## 五、用户执行

在项目根目录启动：

```powershell
uv run python main.py
```

训练结束先读取最佳轮与v14比较；如选择v19再显式预测复赛：

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v19_quality_fusion/weights/best.pt --source "数据集/复赛测试集" --imgsz 1280 --height 1280 --batch 2 --output predict_v19_round2
```

若框架因重名产生带后缀运行目录，以实际路径为准。预测不覆盖已存在输出；提交包仍只含六列TXT，支持掩码不输出为比赛标签。

本轮只核对源码和文本差异，没有运行测试、AST/compile/import或任何模型任务；不能将既往v14的检查结果当作v19运行验证。尚无v19训练AP或赛事分数，不替换已有较好模型。
