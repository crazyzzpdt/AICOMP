# 2026AIC 数据集成分与划分记录

> 记录时间：2026-09-05。统计口径与 `prepare_dataset.py` 的分组逻辑一致。
> 类别索引：0=person, 1=boat, 2=animal, 3=seat, 4=sign, 5=bicycle, 6=car, 7=ball, 8=light, 9=garbage can, 10=uav, 11=tricycle。

## 1. 原始数据构成

官方 2000 组训练集，目录 `数据集/训练集/AIC2026_Train_2000/`：

| 模态 | 数量 | 说明 |
|---|---|---|
| visible/ | 2000 | RGB 8-bit；jpg 149 张 + png 1611 张 |
| infrared/ | 2000 | 热红外（三通道热度灰度堆叠）|
| depth/ | 2000 | 16-bit 深度，单位毫米；0/极小值 = 无效深度 |
| labels/ | 2000 | YOLO txt（`cls cx cy w h`，0-1 归一化）|

文件命名构成：

- **带场景前缀 948 张**，共 764 种前缀（形如 `000006_010_帧号`、`shuming_985_帧号`）。同前缀 = 同一采集序列。
- **纯数字 ID 1052 张**（如 `00000004`、`001148`），按 ID 间隔 >50 视为不同序列，巨型连续段在段内间隔最大处切开。

测试集 `数据集/测试集/AIC2026_PHASE_1_1000/`：visible/infrared/depth 各 1000 张，无标注。

## 2. 两套训练标注（重要）

| 目录 | 时间戳 | 状态 |
|---|---|---|
| `AIC2026_Train_2000/labels/` | 2026-04-15 | 原始下载版（**当前 `datasets/` 落位使用此版**）|
| `new_labels_2000/` | 2026-09-02 | 更新版；文件名与 labels/ 完全一致 |

两套差异：**382 / 2000 个文件内容不同**——201 个文件增框、68 个减框、113 个同框数改位（坐标微调）。新版主要增量：

| 类别 | 旧版总框 | 新版总框 | 变化 |
|---|---:|---:|---:|
| 6 car | 1571 | 1949 | +378 |
| 8 light | 1455 | 1835 | +380 |
| 0 person | 5499 | 5549 | +50 |
| 2 animal | 3061 | 3020 | -41 |
| 其余 | — | — | 小幅增减 |

切换标注源：改 `prepare_dataset.py` 顶部 `LABEL_SUBDIR` 为 `"new_labels_2000"` 后重跑。

## 3. 划分结果（datasets/）

| 划分 | 图片 | 标注 | 占比 |
|---|---:|---:|---:|
| train | 1760 | 1760 | 88% |
| val | 240 | 240 | 12% |

- 落位方式：**硬链接**到 `数据集/训练集/`（同盘零拷贝，删任一侧不丢数据）。
- 每图框数：train 平均 6.8（1~50，中位 5）；val 平均 10.1（1~66，中位 6）——val 场景密度更高（含人员密集的马拉松类场景）。
- 负样本（空标注图）：0 张。数据本身不含纯背景图。

## 4. 划分方法（防泄漏设计）

按参赛手册要求"相邻/相同场景样本不得分跨两侧"：

1. **场景组为原子单位**：前缀组 + 纯数字连续段，共 **776 组**（组大小 1~363），整组进 train 或 val，绝不拆组。
2. 每类"最小场景组"预留给 val，保证 **12 类双侧覆盖**（稀有类 boat/uav 在 val 各 3 框）。
3. 其余组按 9:1 进度贪心配平；巨型段在间隔最大处切开后再参与（最大间隔最可能是真实场景分界）。

划分结果：val 14 组 / train 762 组。

## 5. 类别分布（当前落位 = 旧版标注）

| 类别 | 中文 | train 框 | val 框 | 合计 | 占比 |
|---:|---|---:|---:|---:|---:|
| 0 | person | 4019 | 1480 | 5499 | 38.1% |
| 1 | boat | 132 | 3 | 135 | 0.9% |
| 2 | animal | 2518 | 543 | 3061 | 21.2% |
| 3 | seat | 546 | 75 | 621 | 4.3% |
| 4 | sign | 736 | 73 | 809 | 5.6% |
| 5 | bicycle | 574 | 92 | 666 | 4.6% |
| 6 | car | 1506 | 65 | 1571 | 10.9% |
| 7 | ball | 80 | 9 | 89 | 0.6% |
| 8 | light | 1397 | 58 | 1455 | 10.1% |
| 9 | garbage can | 257 | 25 | 282 | 2.0% |
| 10 | uav | 201 | 3 | 204 | 1.4% |
| 11 | tricycle | 23 | 2 | 25 | 0.2% |
| **合计** | | **11989** | **2428** | **14417** | 100% |

长尾显著：person+animal 占近六成；tricycle/ball/boat/uav 为稀有类（合计 <3%）。

## 6. 划分脚本说明

划分由 `prepare_dataset.py` 完成（确定性：同数据重跑结果一致；`VAL_FRACTION` 控制比例、`RUN_MAX_LEN` 控制巨型段切分粒度、`LABEL_SUBDIR` 切换标注版本）。2026-09-15 数据划分定稿后该脚本已按需从工作区删除，如需重建划分或切换标注版本（labels / new_labels_2000），可从 git 历史恢复：

```bash
git show ebf0b47:prepare_dataset.py > prepare_dataset.py
```

首次训练时 Ultralytics 会自动生成 `labels.cache`。
