# 数据采集网站

2026-09-18 知识更新：本赛题仅使用官方训练数据。当前datasets为1709/291，全部2000组保留，已有57个标签文件修订沿用，本轮新增确定修订0；旧完整数据和缓存保存在runs/dataset_cleaning/v8_20260918_162439/before。外部站点和下方旧1744/256统计仅作其他项目收藏或历史记录，不能当作本轮增补数据方案。最新依据见 [知识索引](项目知识索引.md)。

当前状态：v4 已使用官方新版标注的定向清洗副本完成训练，lr0=0.0001、batch=4；先考虑现有权重候选。详细核验见 [数据集成分与划分记录](数据集成分与划分记录.md)，三次结果与优化建议见 [训练配置与数据集复核](训练配置与数据集复核.md)。本页外部站点仅为其他项目资料，不是本赛题补充训练数据来源。下方旧统计为清洗前历史，当前 train 12628 框、person=4028、car=1885。

比赛网址：https://www.aicomp.cn/tracks/tracks-1/3700.html

CVAT数据导入：https://docs.cvat.ai/docs/dataset_management/import-datasets/
CVAT数据导出：https://docs.cvat.ai/docs/dataset_management/export-datasets/

**其他项目的数据集网站收藏（本赛题不接入）：**
- Roboflow Universe（最重要！）：https://universe.roboflow.com/ —— 搜索你需要的场景，直接下载 YOLO 格式
- Roboflow 公开数据集：https://public.roboflow.com/
- Kaggle 数据集：https://www.kaggle.com/datasets
- Google Dataset Search：https://datasetsearch.research.google.com/

# 数据集结构和分配

负样本：一般用 5%-10%，按易混淆负场景的量来定，负样本留够了才压得住误检

校验集：一般 10%-20% 就够

数据集体检：拿到数据先看负样本/正常场景是否足够多样，只有极少量非目标场景画面的数据集要尽早发现，及时补数据或换源

## 本赛题数据集（官方下发，不可增补）

比赛禁止使用官方 2000 组以外的训练数据，所以这份数据只能拆、不能补。目录结构：

```text
数据集/
├── 训练集/
│   ├── AIC2026_Train_2000/          # 2000 组，有标注
│   │   ├── visible/                 # 可见光
│   │   ├── infrared/                # 红外
│   │   ├── depth/                   # 深度
│   │   └── labels/                  # 2000 个 txt（YOLO 格式 cls cx cy w h，0-1 归一化）
│   └── new_labels_2000/             # 更新版标注，2000 个 txt，当前已启用
└── 测试集/
    └── AIC2026_PHASE_1_1000/        # 1000 组，无标注，只有 visible/infrared/depth
```

三模态同一词干、同一场景，必须一起读。全量核验 2000 组训练样本：每组的三种模态尺寸一致，1851 组 PNG 均为 1920×1080，149 组 JPG 均为 640×360。加载时检查同名和同尺寸，再同步缩放与增强：

| 模态 | 数量 | 格式 |
|---|---|---|
| visible | 训练 2000 / 测试 1000 | RGB 8-bit；PNG 1920×1080 / JPG 640×360 |
| infrared | 训练 2000 / 测试 1000 | 三通道 uint8，本质是热度灰度复制三份 |
| depth | 训练 2000 / 测试 1000 | PNG 为 16-bit；JPG 为 8-bit RGB，不含原始毫米值 |

**每个模态都混着两种分辨率**：训练集 1851 张 1920×1080 的 png + 149 张 640×360 的 jpg；测试集 845 张 png + 155 张 jpg。两种词干不重叠，是各自独立的样本，不是同一张图的缩略图。读数据时不能假设统一尺寸。

PNG 深度图须按原始 16-bit 位深读取，再按距离范围归一化；JPG 仅转单通道，不能将其数值当作毫米。详见 `三模态训练.py`。

## 划分结果（datasets/，训练用）

| 划分 | 图片 | 占比 | 每图框数 |
|---|---:|---:|---|
| train | 1744 | 87.2% | 新版 12629 行，平均约 7.24；含 1 行重复、1 张空标注图 |
| val | 256 | 12.8% | 新版 2566 行，平均约 10.02 |

三种模态已通过硬链接落到 datasets 对应划分内，标签为新版独立清洗副本；原地修改硬链接图像会影响原图，不能把硬链接当独立备份。

12 类框数分布（train / val）：

| 类 | 名称 | train | val | 类 | 名称 | train | val |
|---:|---|---:|---:|---:|---|---:|---:|
| 0 | person | 4030 | 1519 | 6 | car | 1884 | 65 |
| 1 | boat | 128 | 7 | 7 | ball | 83 | 12 |
| 2 | animal | 2397 | 623 | 8 | light | 1778 | 57 |
| 3 | seat | 560 | 81 | 9 | garbage can | 252 | 30 |
| 4 | sign | 716 | 72 | 10 | uav | 205 | 3 |
| 5 | bicycle | 571 | 95 | 11 | tricycle | 25 | 2 |

类别分布不均：person + animal 占全部标注约 56.4%，tricycle / boat / ball / uav 合计约 3.1%，各类在 val 只有 2~12 框。需同时记录分类别 AP，不能把单次总体 mAP 当充分证据。

## 划分规则

1. **同场景必须整组进同一侧**。划分单位不是单张图，是"场景组"：文件名前缀组（`000006_010_帧号`、`shuming_985_帧号`）+ 纯数字连续段（ID 间隔 >50 视为断界）。同场景跨集会直接让 val 分数虚高，参赛手册明令禁止。
2. **每类最小场景组预先留给 val**，保证 12 类双侧都有框。否则稀有类在 val 归零，那一类的 mAP 就永远测不出来。
3. **判同场景必须看内容，不能只看文件名**。本数据集踩过的坑：纯数字 ID 里混着带前缀场景的**重发版**，同一场景两种命名，按文件名分组会把它们切到两侧。初次划分 240 张 val 里有 6 张与 train 同场景（余弦相似度 0.92~0.99、框坐标重合），已按整段采集迁回 val 侧修正。
4. **判据用双指标**：96×96 灰度余弦相似度 >0.85，且同类框中位距离 <0.03。实测同场景对的距离是 0.004~0.026，不同场景对是 0.06~0.51，分开得很干净。只靠相似度会被相似的城市场景骗（0.85~0.89 那一带全是不同场景）。
5. **校验集比例**取 ~13%，落在通用经验 10%~20% 内。宁可 val 稍大：这份数据 scene 差异大，val 太小则指标抖动盖过改进幅度。

## 与通用经验的对照

- 新版有 1 个空标注，尚未逐图确认是否纯背景；不能为凑负样本比例删除真实目标框。
- 三模态与标签来源一致、类别齐全，不等于无标注瑕疵。已记录 5 行原始值略超 1、1 行重复，以及排除舍入误差后的 64 个边缘越界框；边缘框需逐图复核，不能直接批量删除。
- ball 验证 12 个框已逐框检查位置；AP=0 尚不能归因于唯一因素。当前验证集缺少全部 149 张低分辨率 JPG，稀有类场景少，需要后续固定更有代表性的验证方案。
- 增强、类别加权与采样须分别做对照；提高推理 conf 不保证提高 mAP，过高会丢失候选框。
