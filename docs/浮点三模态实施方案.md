# 连续浮点三模态实施方案

## 2026-09-26 恢复YOLO检查点后的BN诊断与修复（最新）

用户已从回收站恢复`runs/detect/AIC_RGBIRDepth_yolo26x_1536_v27`，last/best均为第3轮。此前“检查点删除无法继续”的描述为当时状态。D-FINE-X另一个进程仍在训练，本轮未占用GPU、修改D-FINE/共享源码或中断进程。

### 同权重、同图的有界CPU证据

使用验证图`000002_082_00000061.png`，按正式连续FP32/1536预处理，输入[1,5,1536,1536]、各通道位于0–1。读取可信检查点，禁止GPU，仅在独立CPU内存副本上诊断，不写回任何权重。

| 对照 | 分类logits最大值 | 高于0.5的候选位置 | 解码框坐标/尺寸绝对最大值 |
|---|---:|---:|---:|
| 第3轮EMA原BN运行统计，正常eval | 128.846 | 940 | 7753.32 |
| 同权重，BN临时使用当前图像统计，仅用于诊断 | 2.334 | 18 | 1722.42 |
| 同权重，只恢复官方基底BN均值/方差，正常eval | 1.259 | 15 | 1591.53 |

原eval深层特征绝对最大值1273.25；仅BN用当前图像统计后为22.54。官方RGB基底同图eval最高置信度0.8274、框值最大1534.96，未出现异常饱和。对同一已训练权重仅替换BN运行统计即可消除该图的极端输出，明确定位该图异常的直接因素是运行统计失配。尚未分别比较实时训练模型与EMA缓冲（原保存仅EMA），不能进一步断言全由EMA算法或某一特定BN层引起；单图也不等于完整100张AP验证。

### 训练端处理

FusionRecipe新增`freeze_bn_stats=False`，历史默认不变；train1明确True。每轮`_model_train`先执行父类，再只将BatchNorm2d设为eval，使训练和验证使用一致的预训练运行均值/方差；BN weight/bias继续优化，RGB/IR/Depth及其他参数也不冻结。模型验证仍为正常eval，不使用验证图片更新统计，不以降低阈值掩盖异常。batch1累积到有效16并不会把BN的物理批次变为16，因此处理统计来源而不是继续增加累积。

在修复方法上的同图CPU前向核对：178个BN层统计未变，仿射参数仍requires_grad=True，训练/验证分类原始输出最大差值0，输出有限。合计6次单图CPU前向（含官方RGB对照），无反向、优化器更新、GPU测试、全量验证或正式训练。证据见`runs/diagnostics/v27_cpu_bn_20260926/{report,original_bn,fix_check}.json`。这验证控制逻辑及局部症状，不保证新训练AP或赛事成绩。

train1/predict1指向`AIC_RGBIRDepth_yolo26x_1536_v27_bn_fix`，仍属于v27故障修复，原故障运行完整保留。不resume第三轮异常last，不直接保存CPU实验中替换统计的权重当成正式模型；新训练从orgin_models/yolo26x.pt开始。保留用户300轮、1536、FP32/batch1/nbs16、50轮耐心以及官方1900/100；第41轮关Mosaic由`MAX_EPOCHS-POLISH_START_EPOCH+1=260`计算。验证首批诊断继续留存。新数据和旧验证成绩不可直接比较，尚未宣称全程修复或提分成功。未自动开训或提交推送Git。

## 2026-09-26 YOLO-X前三轮验证异常排查（未锁定根因）

用户实际将YOLO v27总日程与预算改为300轮。前三轮训练分类损失3.28071/1.82695/1.40773，而验证分类损失12254.1/3173.93/1568.78；第三轮用户日志AP95约3.03e-6，CSV五位小数写成0，不能误称绝对没有任何命中。该轨迹需要排查，不能保证等待预热结束会恢复，也不能按首轮低分判定最终上限。

已只读检查训练/验证预处理、双头损失解析、NMS、类别映射、优化器分组及EMA代码：网络为X五通道，归一化均为除255一次，验证FP32/max_det100。此前读取到的第三轮last.pt浮点状态均有限，无NaN/Inf；BN状态变化存在但不足以认定根因，EMA整型num_batches_tracked未累积来自其仅更新浮点缓冲的实现，不据此断言训练BN未更新。没有额外模型前向或评估，不能声称运行等价性已验证。

已修复确定的日程耦合：train1.py保留用户300轮，新增POLISH_START_EPOCH=41，close_mosaic按MAX_EPOCHS-POLISH_START_EPOCH+1计算为260；旧固定80会推迟到221轮。修复只影响下一次YOLO启动，不是验证全零修复，不会改变已运行进程。前几轮预热仍为5轮，总轮数改变对最初几轮余弦因子的差异很小，没有证据将验证异常归因于300轮。

续查时现场只见train2.py/D-FINE-X运行，已完成41轮；用户已确认删除原YOLO v27运行目录及检查点，不再要求找回；此前读取的张量统计是删除前证据。D-FINE训练、共用源码、数据与权重未改动。旧现场缺失，不能重现原始故障或声称已锁定根因。不盲目冻结BN、改精度或提高阈值掩盖异常。YOLO验证器新增validation_probe.jsonl：每轮仅记录原验证首批的输入通道范围、有限性、输出分数/框范围、NMS与内容区裁框前后数量，无额外模型前向、不改预测结果；仅下次YOLO运行生效，不能保证该诊断本身修复异常。未改变D-FINE及共享模块。本轮未启动训练、推理、测试或提交Git。

## 2026-09-26 D-FINE官方标签边界兼容修复

用户执行`python train2.py`时，在训练集初始化处因`000003_026_00000001`报“标签越界”，尚未进入训练循环。该图第一行官方标签的上边界为`0.336574 - 0.678704 / 2 = -0.002778`，本地`datasets/train/labels`与`数据集/训练集/new_labels_2000`内容一致；这是原始贴边框与适配层严格边界检查不兼容，不应通过重洗官方标签处理。

`src/dfine/training.py`现对训练与验证共用的加载路径作内存几何处理：将有效框与原图边界求交，由同一交集生成训练用归一化`cxcywh`及验证用COCO标注；未越界框保留原始训练数值。类别、列数、非有限值、非正尺寸检查保留，完全位于图外或裁后无面积的框仍明确报错，不静默删除目标。原始标签文件、类别、目标数量和数据划分不变，来源哈希继续依据原文件核对，不将内存裁边冒充标签原文。

加载完成时打印每个划分发生裁边的图片数与框数；`optimization_recipe.json`及检查点中的`label_geometry`记录`clip_to_image_bounds_v1`策略和统计。该处理定义了本地验证框几何，不代表已经核实赛事线上评测器对越界真值的处理完全相同。

修复实施时仅修改适配代码及本文，保留现有v27配置与其他未提交改动；未新增测试脚本，未执行测试、AST/compile/import检查、训练、预测或独立评估。当时用户启动在数据加载阶段失败，修复后未由助手复跑完整训练；本节不覆盖其他章节记录的后续用户运行状态。2026-09-26用户授权将本次边界兼容代码与本节说明分两次提交并推送GitHub，实际提交与推送结果以Git记录为准。

## 2026-09-25 v27双X模型方案（当前，尚未开训）

用户授权保留稀有类、验证100张，提高输入，并分别训练YOLO26x和D-FINE-X。此方案替代上一轮从v26-L短微调的建议：两个X都从各自官方RGB基底重新迁移五通道，不能继承L权重当作X。版本统一v27，两个架构分别记录；不因尚未开训的配置修改新增v28。

| 配置 | train1.py | train2.py |
|---|---|---|
| 模型 | 官方YOLO26x COCO | 官方D-FINE-X Objects365 |
| 运行名 | AIC_RGBIRDepth_yolo26x_1536_v27 | AIC_RGBIRDepth_dfine_x_1536_v27 |
| 输入/精度 | 1536方形等比填充、FP32、TF32关闭 | 同左 |
| 物理/有效批次 | 1/16，训练workers2、验证workers0 | 1/16，workers2 |
| 主/骨干学习率 | 5e-5/1e-5 | 5e-5/1e-5 |
| 轮数/耐心 | 120轮/50轮无提升 | 60轮/50轮无提升 |
| 预热/余弦末端 | 5轮/0.01 | 5轮/0.01 |
| 收尾 | 第41轮关Mosaic，scale0.1/translate0.025 | 第41轮关尺度扰动 |
| 增强 | 保留基础IR噪声/深度有效区扰动，关闭额外Gamma/局部对比度/RGB曝光 | 同左，HSV关闭 |

D-FINE运行时按variant选择官方X配置：B5骨干、编码器hidden_dim384/feedforward2048、decoder输入384及reg_scale8；其余解码器参数继承官方配置，不能把整个解码器误写成384维。新权重格式aic_dfine_5ch_v2明确保存variant，预测按元数据选L/X，旧v1权重仍按L兼容。YOLO早期融合已允许L/X规模，保持真实RGB3+IR1+Depth1。

### 官方数据1900/100已落位

工具`tools/split_official_dataset.py`先生成整数规划方案，再核对全部2000份官方标签与6000张三模态摘要；移动200组（800文件）从val到train，未增加/删除样本，标签修改0。datasets/test未动。审计为`runs/dataset_cleaning/official_labels_split_1900_100_v27/manifest.json`，内含旧/新划分、来源摘要和所选场景组；旧清单、YAML和标签缓存保存在同目录，不重复备份图片。

分组采用文件名前缀（合并suppl）及已有人工审阅组；只从原300张val中选择完整候选组，不按模型得分筛选。未知场景仍未完成全量视觉去重，不能宣称绝对无场景泄漏。约束保留各类框和含类图覆盖，稀有类至少一半可用框；最终验证76 PNG+24 JPG，训练1775 PNG+125 JPG。

| 类别 | 验证含类图 | 验证框 |
|---|---:|---:|
| person | 54 | 754 |
| boat | 2 | 7 |
| animal | 12 | 52 |
| seat | 20 | 71 |
| sign | 29 | 59 |
| bicycle | 23 | 90 |
| car | 20 | 84 |
| ball | 16 | 17 |
| light | 23 | 58 |
| garbage can | 27 | 29 |
| uav | 24 | 34 |
| tricycle | 8 | 12 |

同图可含多类，表中含类图不能相加当图片总数。稀有类覆盖优先导致100张并非按原300张比例随机抽取，AP数值不能直接与旧验证集比较。新方案同时改变容量、划分、输入和日程，是实际候选对比，不是单变量消融；不保证超过v26或60分。

### 权重、预测和运行

YOLO26x权重已在orgin_models。D-FINE-X从[官方模型库](https://github.com/Peterande/D-FINE#model-zoo)给出的[Objects365权重](https://github.com/Peterande/storage/releases/download/dfinev1.0/dfine_x_obj365.pth)下载至orgin_models/dfine_x_obj365.pth，SHA256为a3e3177e67fecf958d77599482730aaeb2700794b79a9cdd269470bdd8cb14eb。未改上游D-FINE源码与来源摘要。

predict1/predict2默认分别指向两套v27 best权重及独立历史产出目录，FP32、batch1；YOLO1536，D-FINE自动读检查点尺寸和variant。按用户选择，两入口提交max_det=12；12是本次策略而非官方上限，可能损失密集目标召回。轮末验证仍max_det=100，保留标准AP定义；因此验证与提交候选上限有明确差异。保持conf0.001，输出图保持原图尺寸，TXT还原原坐标。复赛材料仍由tools统一生成。

依次运行`uv run python train1.py`、`uv run python train2.py`，不要同时争用GPU。各自结束后运行对应predict入口；如运行名因重名追加后缀，按实际权重路径修改。X模型1536/FP32峰值显存尚未实测，batch1不等于保证16GB可容纳；未自动试跑、测试、模型前向、训练、推理或复评。本轮实际运行仅数据准备、文件哈希与下载，不把历史L模型测试当成X验证。正式启动前按仓库规则提交源码文档基点；本轮尚未提交或推送。


2026-09-25正式产物追加：用户随后完成v26完整运行104轮，best54 AP95=41.2176%，末轮38.901%，复赛48.2750；现场batch2/FP32、官方1700/300。此次正式运行成功与下文有界测试分别记录，不把104轮说成助手测试成果，也不保证其他条件永不OOM。后期过拟合，不继续last；同口径数据审阅和最佳权重短收尾为候选方案，尚未修改入口。详情见[最新v26复盘](历次训练与赛事分数对比.md#2026-09-25-复赛成绩与v26完成复盘最新)。

## 2026-09-25 train1有界运行检查

用户本轮明确授权测试并修复，检查前没有活动训练进程。现场入口已由用户设为batch2，保留1280、FP32、nbs16、workers2、patience50及官方1700/300数据；不改学习率、增强、模型或标签。以下结论只覆盖实际检查范围，不覆盖后续完整训练。

- `uv run python -m tools.check_train1`：执行真实train1入口，每轮只消费4个训练批次、2个验证批次，运行两轮；保留原始加载器长度和200轮学习率日程，仅把测试预算设为2并隔离输出目录。共8个训练批次、6次优化更新、4个验证批次，验证→保存→第二轮训练→正常退出均通过。首次启动检查包装函数与入口model关键字重名，修正工具形参后正式短流程通过；这不是训练器故障。
- `uv run python -m pytest tests/test_training_consistency.py tests/test_float_training_boundaries.py -q`：23项通过（2.60秒）。修复历史测试仍导入`aic`及简化夹具未初始化新架构标志的问题；没有通过修改生产逻辑迎合过时测试。
- 检查点CPU重载通过：FP32五通道、Adam状态存在、epoch=1（第二轮）。IR与Depth各576个首层参数非零，确认短训练对辅助模态首层产生更新；不等于已经证明三模态提分。
- 验证与预测连续浮点预处理在横图、竖图、奇数补边样例上逐像素一致；检查深度空洞、非整数深度保留、辅助补0、CPU快照不修改活动参数、历史早停与裁框等边界。

产物：`runs/train1_checks/entry_20260925_020231/check_report.json`、同目录`train.log`和`weights`；控制台日志为`runs/train1_check_console.log`，回归结果为`runs/train1_regression_console.log`。PyTorch峰值已分配显存10.333 GiB，训练进度显示保留显存约12.8 GiB；两者都不是系统全部GPU占用。短流程AP仅来自2张验证图，不参与版本比较，不使用这些权重提交赛事。测试不占用新训练版本号。

本次没有复现此前主机内存/第二轮CUDA分配失败，不能据此声称完整850批×200轮稳定或历史故障根因已确证。已有内存修复保持；本次训练入口只修正batch2、稳态累积8批及浮点补边的过时注释，未再改训练算法。测试结束无遗留Python训练进程，未启动正式训练、未提交Git。

## 2026-09-24 DataLoader与轮末内存修复

用户提供的batch1堆栈在`transform_float_image`的NumPy数组分配失败，属于主机内存分配错误。已有两份batch2日志（`runs/launch_v26_batch2_20260924_094340`及`runs/launch_v26_batch2_cachefix_20260924_1042`）均完成首轮验证，崩溃点实际为第二轮首次训练前向；分别申请100/50 MiB失败，报错仍显示约7.76/7.87 GiB显存空闲。因此不能把问题直接归为验证batch过大，也不能仅看显存空闲断言资源足够。没有故障当时的Windows提交内存/驱动记录，主机提交上限与驱动分配失败的因果仍未证实。

本机Ultralytics 8.4.152的362个包文件与安装`RECORD`的SHA256全部一致；没有发现安装后文件被修改的证据。此核验不是独立下载上游源码比对，也未升级或重装底层库。模型损失统计使用detach，当前检查未发现该统计保留跨批计算图。

已修复的资源峰值来源：

- 五通道FP32几何增强逐通道写入一个预分配数组，并原地clip；不再同时持有四张临时平面、dstack结果及clip结果。1280×1280×5单份数组31.25 MiB，四图Mosaic画布125 MiB；精度和插值规则保留。
- 浮点数据集只缓存Mosaic的有限候选索引，保留FIFO候选规则；每次从原图重新读取，不常驻保存多份FP32图像。增加解码开销，减少多进程内存占用。
- `train1.py`设workers=2；连续浮点验证使用workers=0，避免训练/验证合计8个Windows子进程常驻。使用有限轮次加载器，训练轮结束后不在验证/保存期间预取下一轮；成功/异常退出均关闭加载器。
- FP32检查点直接构建CPU EMA及CPU优化器快照，不先在GPU上deepcopy再搬CPU。使用临时文件流式写盘并替换last，最佳与定期权重从已写好的文件复制；省去完整BytesIO副本，保留FP32及检查点字段。

入口仍为batch1/nbs16、1280、FP32、patience50；未自动改为batch2或降低精度。workers及加载器生命周期变化会改变随机增强流，不能承诺与旧轨迹逐位一致。以上修复降低已发现的内存峰值，尚未用模型运行证明batch2长期稳定。

所提供失败日志使用旧1709/291；当前数据已恢复官方标签1700/300。旧清洗数据断点不可接到新划分上，本次也未开训。遵守用户默认禁测约束，仅核对源码、已有日志、安装文件指纹与Git差异，未运行pytest、导入/编译检查、模型前向、训练、预测或复评。

## 2026-09-24：v24最终复盘与续训决策

误删后重新取得的v24目录包含完整72轮结果、逐类指标、配方和权重。v24使用1709/291固定划分、五通道早期融合、1280、BF16、batch4/nbs16、主学习率5e-5、骨干1e-5、200轮余弦日程、Mosaic0.5及patience30。最佳为第42轮：AP50-95=0.4008757256、AP50=0.6279537053；末轮第72轮AP50-95=0.38451。训练因最佳后连续30轮未创新高而停止，不是跑满200轮。

与同一291张验证集、同一评估协议相比，v24比v14/v21的0.39215提高0.8726个百分点，比v22的0.37212提高2.8756个百分点，比v23的0.33780提高6.3076个百分点，比v25的0.38515提高1.5729个百分点。v4本地峰值0.40370来自旧256张验证集，不能按0.2812个百分点的表面差值判定v24退步或超越；初赛55.6000与本地AP也不能换算。

v24最佳至末轮的训练box从1.13919降至1.02722，验证box从1.25299基本持平到1.25002，但验证cls从1.04633升至1.11010，AP95下降1.6366个百分点。第67轮曾回升至0.39425，说明后段存在指标波动和局部恢复，但30轮始终没有超过第42轮。结论是存在中等程度的后期过拟合，继续训练`last.pt`的预期收益较低。若以后验证v24潜力，应从`best.pt`另开10–20轮低学习率、弱增强收尾实验，保留原v24为基线并分配新版本号；不能把这种微调称作v24断点续训或保证提分。

逐类相对v14的AP95变化显示收益并不均匀：boat +15.27、garbage can +5.70个百分点最明显；person、animal、sign、ball、uav小幅提高；seat、bicycle、car、light、tricycle回退，其中tricycle、car、bicycle分别约-5.12、-4.98、-4.74个百分点。下一步若做v24 best收尾，应以这些回退类是否恢复和总体AP95是否同时提高作为验收条件，不能只追boat等小样本类的波动。

现场核对时，活动命令为`uv run train1.py`，实际入口和输出目录是v26_no_tone；v25已经完成64轮。报告只读取现有产物，没有启动、停止或修改该训练进程。

## 2026-09-24：v25完成与v26对照启动计划（最新）

v25实际FP32/batch1完成64轮，约6.562小时；最佳34轮AP95=0.3851467445、AP50=0.6029528109，末轮AP95=0.37133。最佳至末轮训练框损失1.05123→0.92008，验证框损失1.24090→1.29958；连续30轮未创新高停止，不直接续训last。相对v14最佳0.39215下降约0.700个百分点；seat回退约10.58个百分点，ball提高约3.90个百分点。不能据多变量变化归因某一种增强。

用户批准v26_no_tone：仅YOLO train1切换新轮次，从官方基底重训、resume=False，关闭IR Gamma/局部对比度/RGB曝光三项新增增强，保留原IR传感器噪声、深度增强、连续FP32、1280、batch1/nbs16、1709/291、主5e-5/骨干1e-5、200轮日程与第101轮关Mosaic。patience按用户要求改50，指标仍为AP95且min_delta=0；总上限仍200，不保证一定进入收尾。增强组合与停止预算均改变，不称严格单变量完整训练对照；可比较共同轮次曲线。

执行顺序：核对进程避免重复训练→修改入口/文档→审阅暂存并提交Git→后台启动一次uv run python train1.py→只查看启动输出，不追加测试或独立复评。训练完成前不预填结果。D-FINE train2保留v25未启动配置，预测用户修改不覆盖、不夹带。此前“v24产物未找到”是误删后的临时状态，现已由上节完整72轮产物与指标替代。

2026-09-24版本更正：用户正在训练v24，下一轮只有v25。浮点基础与昼夜增强均未单独开训，合并为v25_illumination；版本不随代码改动递增。本次仅改编号与文档，不改变配方、数据、预测默认权重或已有运行目录。

## v25昼夜强度增强追加（代码已修改，未运行）

用户授权吸收老师建议并修改代码。本轮仅增强训练输入，不替换已定义的五通道共享骨干、不冒称三分支光照门控。依次修改共享SensorAugment和浮点增强函数、两个训练入口、本文档；只阅读源码和Git差异，不运行测试、训练或推理，不自动提交。

红外增加Gamma（概率0.25，指数0.8–1.2）和浮点局部对比度增强（概率0.25，最大细节增益0.3、单像素改变量上限0.05）；局部标准差抑制平坦区放大，不是热伪影识别。RGB增加概率0.25的Gamma/曝光扰动（指数0.7–1.8、曝光0.6–1.05、标准差0.003噪声），模拟亮度变化而非生成真实夜间。保留原深度增强与有效性插值，训练/验证基础归一化不变，预测不随机增强，旧权重输入协议不变。

各新增概率默认0，v25两个入口显式开启，避免旧配置静默启用。新运行名v25_illumination；此前两份未开训草案合并为同一次待训练版本。官方图片保持原位宽：既有审计支持本项目16位PNG深度的毫米解释，不能推广成所有16位图都为毫米；JPG只有灰度代理，转存16位不会恢复距离。运算使用FP32，不批量重写图像。不加入未经证实的昼夜动态阈值或空洞删框规则，不保证优于Gamma单独使用或保证60分。

用户已确认：新训练采用连续FP32深度、红外/深度传感器增强；旧权重仅保留推理，不恢复旧训练。完成后提交GitHub，不自动运行测试、训练或推理。

## 实施清单（代码已落地，运行未验证）

- [x] 共享模块src/modalities.py：保留uint16原始精度后转FP32，内部浮点0–255、网络统一除255；不取整、不回写数据。深度插值使用支持加权线性值和最近邻有效性，JPG仅灰度代理。
- [x] YOLO：浮点Mosaic/仿射/LetterBox替换原生uint8画布；标签几何沿用原生。新权重记录协议，训练与检查点FP32、关闭TF32；不恢复旧断点。
- [x] D-FINE：从Git c1a9923中的历史适配恢复真实五通道训练，迁移本地Objects365，移除旧训练兼容；接入共享浮点处理与增强，FP32训练、轮末验证、EMA及检查点。
- [x] 推理：CLI最终权重解析后读取D-FINE尺寸，缺失明确报错；按权重协议选择旧uint8/新FP32路径；补材料包含共享模块与实际源码。
- Git提交推送以实际提交记录为准；无自动测试/语法执行检查或模型运行，不宣称运行通过或提分。

## 增强与兼容约束

红外：概率0.3，增益0.95–1.05、偏置±0.01；概率0.1叠加标准差0.003的噪声（0–1量纲）。深度：概率0.2，有效毫米值尺度±1%、噪声标准差0.0005+0.0015*d²（归一化量纲）；概率0.1随机缺失0.5%有效点。8位深度代理只模拟缺失，不施加毫米距离假设。都是候选温和增强，不保证胜过Gamma。

新增增强只作用训练图像内存，验证/预测不使用。辅助通道补0、RGB补114；无效深度保持0，三模态几何严格同步。新YOLO运行命名v25，D-FINE命名v25_illumination；已有v24不续训新协议。FP32显存未测，默认物理batch2、有效batch16，用户可调整。

## 参数、精度与启动边界

YOLO：1280/200轮/AdamW主5e-5骨干1e-5/前100轮Mosaic0.5/patience30；D-FINE：1280/60轮/AdamW主1e-4骨干1e-5/3轮预热/第41轮关闭尺度扰动/patience15。均从本地官方基底重新迁移，不加载历史训练权重。两个入口分别运行，不自动同时占用GPU：`uv run python train1.py`或`uv run python train2.py`。

有效毫米深度定义为0<d≤20000，其他位置保持0；不对8位代理假造16位信息。内部先乘255/20000是连续线性缩放，不是8位量化；最后除255得到d/20000。随机增强仅训练启用；关闭噪声仍保留新插值/补边协议，所以不是旧权重兼容训练。

YOLO默认保存会把EMA和优化器转FP16；新协议绕过这一行为，保存FP32副本。官方预训练文件本来已经丢失的精度无法凭转FP32恢复。TF32/AMP关闭增加时间和显存，未测batch2能否放入本机显存；若OOM由用户调batch=1、保持有效16，不自动降精度。

新方案同时改变深度插值/补边、增强、训练精度与物理批次，不得描述为严格单变量消融或保证60分。后续只依据同划分结果和实际提交判断。旧模型可继续预测，但不能把本次未执行的兼容路径写成“已验证全部旧模型通过”。

源码身份：src/D-FINE/source_manifest.json为当前本地分发副本的LF规范化摘要，并保留历史上游提交标识；这证明后续是否变化，不等于本轮独立核验了每行与上游相同。运行时缺失/变化会明确报错，不再因缺少.git跳过校验。
