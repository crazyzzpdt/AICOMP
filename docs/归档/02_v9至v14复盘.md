# 02_v9至v14复盘

整理日期：2026-09-23。以下按原文件完整合订，保留当时的数据、结论与实施状态。历史“当前”“未训练”“未提交”和测试命令均只代表记录当时，**不覆盖现行入口和默认禁测约束**。现行信息见[知识索引](../项目知识索引.md)。原文件可从Git历史恢复；正文仅迁移引用路径，不把旧建议当作已证实收益。

## 原文件目录

- [docs/v9融合训练实施.md](#record-1)
- [docs/v9复盘与v10训练方案.md](#record-2)
- [docs/v10复盘与v11筛选方案.md](#record-3)
- [docs/v11复盘与v12训练方案.md](#record-4)
- [docs/v12复盘与v13训练方案.md](#record-5)
- [docs/v13复盘与v14阶段筛选.md](#record-6)
- [docs/v14一致性修复与执行记录.md](#record-7)


---

<a id="record-1"></a>

## 原文件：docs/v9融合训练实施.md

# v9：YOLO26l 门控融合与 1920×1080 矩形训练

2026-09-20 完成状态：v9已训练102轮，第63轮最佳mAP50-95=0.34952，用户线上53.4750；本页保留历史实施记录，当前入口已改为v10早期融合。逐类指标、预测哈希核对及后续配方见[v9复盘与v10训练方案](02_v9至v14复盘.md#record-2)。此前单批冒烟通过不证明新配方收敛或提分；本轮没有再次执行测试或模型任务。

2026-09-19 完整配置与授权短流程：保留所有显式配置，补充 model、mode、dis，并修正 dfl 的 L1 权重注释；核心训练数值未变。与本机 default.yaml 文本比对无遗漏配置键、无重复关键字。用户本轮明确授权测试，复用 runs/smoke_main/entry_check_20260919.py 经 main.py 完成一次训练批次、一次优化器调用、一次 FP32 验证批次；报告位于 runs/smoke_main/entry_smoke-2/smoke_report.json，status=passed，输入 [2,5,1088,1920]，训练损失 9.887825，显存 allocated/reserved 峰值约 7.60/8.30 GiB。首次尝试因测试包装函数的 model 形参与新增 model 关键字冲突而失败，已仅修改包装函数后复跑通过。

本次保持 seed=0、deterministic=True，但没有多次运行逐位一致性对比，不能宣称严格确定性已验证。测试临时关闭保存/绘图并限制加载器只产出一批，未改变正式入口 save=True/plots=True；未写出权重。第一步处于预热起点，不验证稳态梯度累积、长期收敛或检查点恢复。单图验证 AP 不用于模型比较；显存结果不代表全程峰值。框架重建了 train/val 标签缓存，未修改标签和图像。完整配置不意味着启用所有互斥增强，也不构成最优参数或提分证据。

2026-09-19 接口修正：用户授权的一次main.py冒烟测试在模型构建时失败，实际训练/验证均为0批，未保存权重；用户随后正式启动复现同一TypeError。已将aic/model.py的_predict_once签名同步为(x, profile=False, embed=None)，去掉旧visualize形参及父类调用中的多余实参，父类调用改用关键字，避免embed错位。未改变模型结构、融合逻辑或训练配方；本次修正后尚未重新运行测试。先前“未运行测试”仅适用于当时阶段，不代表这次已授权测试不存在。

2026-09-19，用户批准执行融合方案，并要求恢复原生 YOLO 训练代码风格、训练和预测都使用 1920×1080。代码已写入。此前只做过一次AST静态语法解析和git diff格式检查，没有运行pytest、模型前向、训练、推理、独立评估或显存探测；静态解析不等于代码已经过正式运行验证，也不能据此声称必达60分。后续默认不再自动进行AST/compile/import检查，除非用户明确要求。

原 v8 D-FINE（线上 53.5740）不改名、不覆盖；v4 的 55.6000 仍是最高已知线上基线。新运行名为 `AIC_RGBIRDepth_yolo26l_1920x1080_v9_fusion`。

文件整理（2026-09-19）：根目录仅保留`main.py`和`predict.py`。预测专用实现已全部合并到predict.py；训练器与数据集归入aic/training.py，模型结构/共用预处理归入aic/model.py，共享读图/常量归入aic/data.py，清洗工具移到tools/prepare_dataset.py。删除无当前用途的旧训练器与D-FINE训练循环；旧D-FINE预测仍保留。详情和恢复边界见[代码结构与运行](../代码结构与运行.md)。

本轮不改训练/预测参数、数据或权重，仅阅读引用和文件差异，未运行测试、语法解析、导入检查或模型任务。

## 1. 如何启动

报错修正（2026-09-19）：用户实际日志确认imgsz=1080与image_width=1920冲突。现已将imgsz改为IMAGE_HW[0]=1920，并同步“宽、高”注释；内容仍1920×1080，张量补齐1920×1088。另据本机框架get_save_dir源码，将PROJECT_PATH改为从入口位置构造的绝对runs/detect，避免相对project被拼到全局runs_dir/task下。历史错误目录不移动、不删除，重复运行仍另建目录，预测时核对实际输出路径。没有测试或启动训练，修正仅经文本核对，不代表已确认全部训练流程运行正常。

在项目根目录执行一次正式训练：

```powershell
uv run python main.py
```

训练结束后，确认 `predict.py` 的 MODEL_PATH 指向实际新运行的 `weights/best.pt`，再执行：

```powershell
uv run python predict.py
```

默认预测结果为 `predict_v9/images`、`predict_v9/labels`、`predict_v9/比赛提交内容/submission.zip`。已有目录不覆盖，再次提交使用 `--output` 指定新目录。重名训练由 YOLO 递增目录名，预测不会擅自选择最新目录。

不能把 D-FINE v8 权重当作新融合权重使用：本轮需要重新训练。旧 YOLO 和 D-FINE 后端继续保留，使用旧模型时显式指定其权重及原训练尺寸。

## 2. 真正落实 1920×1080，而不是只把 imgsz 改成 1920

- 外部五通道顺序仍为 RGB3、IR1、Depth1，模型内部拆成三个分支。
- 内容窗口是宽 1920、高 1080。1080 不是网络最大步长 32 的整数倍，因此画布仅补边到宽 1920、高 1088，标准 1080p 图像上下各补 4 像素，不先缩成 1280，也不拉伸到 1088。
- 原生 YOLO 训练参数 `imgsz` 使用标量长边1920，由 `RectangularDataset` 输出固定矩形；`rect=False` 不代表本实现输入为正方形。最初`IMAGE_HW=(1080,1920)`按高、宽书写；当前工作区改成宽、高后，须同时调整所有索引，见上方开训阻塞说明。
- 不同原尺寸图像等比放入内容窗口，不强行改变目标长宽比。低清 JPG 放大不会产生新的真实细节，较大输入也不保证提高泛化成绩。
- 前 80 轮允许温和同步几何增强和概率 0.25 的矩形四图拼接；不是每次都保持原像素一比一。第 81 轮起关闭拼图、尺度及位移，回到完整画面收尾。
- 验证与预测使用同一 `letterbox_fused`，不做随机增强。三模态共用缩放和填充；HSV 仅用于训练 RGB。OpenCV 缩放/仿射逐通道执行，避开此前五通道插值报错。

## 3. 结构与权重迁移

`aic/model.py` 的 `FusionDetectionModel` 保留官方 YOLO26l 三通道 RGB 主干，红外、深度分别经轻量卷积和 GroupNorm 提取三尺度特征，在 P3/P4/P5 经投影和可学习空间门控加入 RGB 特征，送入同一颈部和检测器。

辅助投影以小幅非零参数初始化，门控偏置为 -2；这是保护初始 RGB 表示的设计，不是把模态固定清零。真实贡献由训练学习，不保证能修正严重配准偏差。正式训练记录分支梯度和门控概况，用于发现失活，不等于证明提分。

初始化为本地官方 `orgin_models/yolo26l.pt`，不是旧 v4/v5 或 D-FINE 项目权重。分类层按匹配语义迁移，并显式检查两个训练分支的 `sports ball→ball=7` 权重与偏置；不把一个 COCO 动物类冒充全部 animal。

训练保留原生一对多和一对一分配，损失全程固定为 0.8/0.2。正式验证、提交只使用一对多输出，不融合两个头。这样不再借 5000 个总 epochs 间接控制损失比例；这仍是候选设计，尚无本轮效果证据。

## 4. 已写入的参数

| 项目 | 本轮设置 |
|---|---|
| 内容 / 网络张量 | 1920×1080 / 5×1088×1920 |
| 训练 / 验证 / 预测 batch | 2 / 1 / 4；峰值显存未实测，由用户调整 |
| 有效批次 | nbs=16，稳态 batch=2 时累积 8 批，预热沿用原生策略 |
| 训练精度 | BF16；验证及预测 FP32 |
| 加载 | workers=4、预取 1 批、pin_memory=False、cache=False |
| 周期 | 最多 120 轮，预热 5 轮，余弦衰减到各组初始学习率的 1% |
| AdamW 学习率 | RGB 主干 2e-5、颈部/检测头 1e-4、IR/Depth/门控 2e-4 |
| 衰减 | 5e-4，偏置和归一化参数不衰减，实际参数组写入配方 JSON |
| 几何增强 | mosaic=0.25、scale=0.2、translate=0.05、fliplr=0.5 |
| RGB 增强 | HSV=0.01/0.15/0.15 |
| 收尾 | 第 81 轮起关闭 Mosaic/scale/translate；保留翻转与 RGB 增强 |
| 早停 | 收尾阶段重新计数，至少允许 20 轮收尾；patience=20、min_delta=0.0002 |
| 类别策略 | cls_pw=0、不重采样、不裁弱类、不随机丢模态 |
| 检测候选 | 单标签 NMS，conf=0.001、iou=0.7、max_det=100 |
| 权重留存 | best.pt 按 AP95；best_map50.pt 单独留存；last.pt；原生每 20 轮周期文件 |

默认训练 batch=2 是未经试跑的起点，不代表恰好用满 15GB。若用户改为 4，稳态累积自动改为 4 批。保持 nbs=16；不要为了凑满显存而盲目增加有效批次。原生训练期间若遇 OOM 可能按框架逻辑减小 batch，这不是本轮执行了自动探测。

## 5. 验证、提交与记录

- 两端共享五通道读图、等比矩形填充、除以 255、单标签 NMS和内容区裁框；补边中的退化框不会只在提交时消失。验证在画布坐标匹配，提交逆变换回原图，两者保持同一几何口径。
- FP32 指前向和后处理精度；YOLO 原生 `.pt` 序列化 EMA 为半精度、加载时转回 FP32，不能宣称与保存前的 FP32 EMA 逐位相同。
- 一份检查点、一次常规前向、一个尺度；没有多权重 WBF、投票、TTA 或弱类重检测。`visual_conf=0.25` 仅决定画框展示。
- 每轮正常验证记录 `results.csv`、12 类 `per_class_metrics.csv`、复用同批预测的 `domain_metrics.csv`。分域 AP 不额外运行模型。
- `fusion_diagnostics.csv` 记录两头平均损失、六个门控均值及辅助编码器末次优化步梯度范数。它们不是完整分布统计，也不构成模态收益消融实验。
- `optimization_recipe.json` 留存矩形尺寸、双头比例、参数组、源码/框架/清洗审计指纹；同时保存入口及组件源码快照、数据审计快照。
- 原生曲线和混淆矩阵在收尾或末轮生成；取消训练结束后再次加载最佳模型的独立复评。图上的末轮指标不应冒充 `best.pt` 所在轮指标。
- 预测元数据记录结构版本、内容尺寸、张量尺寸、检查点轮数及权重指纹。融合模型不走历史 `yolo_profile=v4/current` 路径。

## 6. 数据与断点边界

v9不是只有训练参数，完整流程为：官方新版标签 → 已留痕清洗及固定划分 → 开训清单/标签指纹核验 → 真实三分支融合与同步增强 → 正式轮末验证/择优 → 同口径预测和提交打包。代码归并不重新执行清洗，也不撤回已经落位的清洗结果。

现有`official_refresh_20260918_214843/summary.json`记录：标签来源为`数据集/训练集/new_labels_2000`，状态为applied，57个标签文件有修订，动作共64处越界裁剪、1行重复去除、1处类别纠正；官方刷新时三模态图像和清洗标签内容差异均为0。这是读取已有审计所得，不代表本轮重新核验了全部源图或完成新一轮人工审阅。

清洗实现仍在`tools/prepare_dataset.py`，共用读图/类别/指纹在`aic/data.py`。训练入口继续指向`datasets/data.yaml`和该manifest，开训核对三模态文件名集合、标签配对及标签内容哈希；它不自动判断漏标、错标或像素级配准是否正确，也不随训练重写数据。

继续读取当前清洗版 `datasets`：1709 train / 291 val，57 个标签文件修订不回滚，不新建大体积 NPY，不下载或添加其他数据。来源仍为 `official_refresh_20260918_214843/manifest.json`。用户启动训练时核对三模态文件清单与标签指纹；这不是新的逐框人工清洗。

老师建议的约 200 张人工复核仍是待办，不写成已完成。此前空间清理已删除两个 `before` 中的 RGB images 和废弃 `code_cleanup` 备份；现有审计与标签继续保留，旧图像需按清单从官方源重建，不再声称 `before` 是完整三模态备份。

`RESUME_PATH=None` 表示新训练。仅把同配方中断的 `last.pt` 填入恢复；恢复另建目录。组件源码、框架、数据审计及学习配方变动会拒绝恢复。只改入口中的恢复路径和允许的资源设置不算修改组件源码。恢复沿用原生 EMA/优化器恢复机制，不保证与不中断运行逐步完全一致。

## 7. 下一次判断依据

先由用户完成这一轮正式训练，不提前写新成绩。比较与 v8 相同验证划分下的总体 AP95、12 类 AP95、PNG/JPG域和收尾趋势；历史旧划分 AP 不直接横排。优先提交 `best.pt`，记录真实线上反馈，再决定后续动作。更多像素、门控融合及新参数同时变化后，即使涨分也不能单独归因某一项。

设计来源及老师、群聊建议取舍见 [v8改进方案与下一轮融合设计](01_早期训练与清洗.md#record-5)，历史成绩见 [历次训练与赛事分数对比](../历次训练与赛事分数对比.md)。


---

<a id="record-2"></a>

## 原文件：docs/v9复盘与v10训练方案.md

# v9复盘与v10训练方案

更新：2026-09-20。读取已有日志、CSV、曲线、配方、Git历史与本地可信检查点；没有执行测试、模型前向、复评、训练或推理。检查点仅在CPU解包查看结构与参数。v10已写入入口，尚未训练，不承诺60分。

同日用户追加要求：不止恢复v4，还要吸收历次经验，并按多数原图1920×1080统一训练/预测。本页v10已更新为全尺寸增强版（`yolo26l_early_rect_v10_2`），替代尚未训练的1280草案。这里按用户给出的具体宽高理解“1k”为1080p内容，不是长边1024；若原图更小，放大不会凭空增加细节。

## 结论与证据边界

v9用户反馈赛事53.4750，比v4的55.6000低2.1250分，确实未完成提分目标。v9后期存在过拟合；没有找到“预测未使用辅助模态”或“保存图片压缩了模型输入”的代码证据。没有在同一验证集重评两模型，因此不能排除其他运行差异，也不能把差距唯一归因于某一个参数。

此前一次叠加门控结构、更大输入、分组学习率、定制拼图与收尾策略，缺乏逐项收益证据。本轮不再以结构复杂度代替有效性，恢复有线上成绩支持的v4早期融合路线，保留后续已经查明的球类语义映射与审计能力。

## 训练与线上结果

| 项目 | v4 clean_lr1e4 | v9 fusion |
|---|---:|---:|
| 用户报告赛事分数 | 55.6000 | 53.4750 |
| 训练/验证图片 | 1744/256 | 1709/291 |
| 完成轮数 | 200 | 102 |
| AP95最佳轮 | 100 | 63 |
| 最佳轮mAP50 | 0.58625 | 0.56575 |
| 最佳轮mAP50-95 | 0.40370 | 0.34952 |
| 独立mAP50峰值 | 0.60632（42轮） | 0.59553（34轮） |
| 最后一轮mAP50-95 | 0.36372 | 0.33293 |
| 日志训练耗时 | 6.154小时 | 5.211小时 |

本地AP不是赛事分数；两版本划分不同，0.40370与0.34952不能作为严格对照实验。旧验证集UAV仅3框、三轮车仅2框；当前为37/12框，ball由12增至17框。v4两个极少样本类别接近满分，抬高了宏平均，但不能因此认定线上优势是假的。

### 同一最佳权重的逐类指标（百分数）

v4取train.log末尾重载best的约三位有效数字，v9取per_class_metrics.csv第63轮；不是将各类不同轮次峰值拼接。框架验证精度也有差异。

| 类别 | v4 AP50 / AP95 | v9 AP50 / AP95 |
|---|---:|---:|
| person | 68.30 / 38.60 | 72.68 / 42.08 |
| boat | 93.30 / 78.50 | 82.53 / 73.96 |
| animal | 78.30 / 50.90 | 80.50 / 52.39 |
| seat | 33.10 / 20.30 | 45.60 / 27.57 |
| sign | 38.80 / 28.10 | 40.78 / 27.62 |
| bicycle | 33.30 / 16.20 | 41.63 / 24.19 |
| car | 62.00 / 35.80 | 67.34 / 40.29 |
| ball | 0.00 / 0.00 | 30.68 / 19.67 |
| light | 44.80 / 18.10 | 40.95 / 14.37 |
| garbage can | 52.70 / 40.00 | 55.92 / 35.50 |
| uav | 99.50 / 76.40 | 68.75 / 33.72 |
| tricycle | 99.50 / 79.50 | 51.55 / 28.06 |

v9不是每一类都差：ball已经能学习，人、动物、座椅、自行车、汽车的本地AP95也更高。船、灯、垃圾桶定位值得关注；UAV/三轮车的落差同时受到验证样本增加影响。不能凭低AP自动删掉困难标签。

### 过拟合发生在哪里

| 轮次 | train box | train cls | val box | val cls | mAP50-95 |
|---|---:|---:|---:|---:|---:|
| 60 | 1.03047 | 0.49330 | 1.41318 | 1.30799 | 0.34648 |
| 80 | 0.96214 | 0.44167 | 1.42271 | 1.33145 | 0.34496 |
| 81 | 0.86842 | 0.39503 | 1.41968 | 1.34138 | 0.34494 |
| 102 | 0.77087 | 0.35079 | 1.45950 | 1.38869 | 0.33293 |

后期训练损失下降、验证损失上升且AP退化，共同支持过拟合判断。第81轮关闭所有几何扰动后没有超过第63轮；旧早停强制进入收尾后才计数，使后续训练继续，但best.pt仍正确保留第63轮，因此多跑的轮次不是best提交下降的直接原因。根目录PR曲线等最终图对应末轮，不应误当第63轮best的曲线。

## Git与权重核验

- `86411da:main.py`对应v4的运行名和lr0=1e-4；`86411da:三模态训练.py`是当时的五通道数据/训练实现。`a9ed7be`更早，main仍为v3较高学习率，不应冒充v4完整配方。
- v4 best.pt模型为原生DetectionModel，首层权重`[64,5,3,3]`；不是v9的独立门控融合器。实际一对多输出，12类顺序一致。
- v4首层IR/Depth切片L2范数约0.06538/0.04254，非零；RGB切片约9.94/8.01/8.06。证明不是永久冻结的零辅助权重，但仅凭范数不能量化模态贡献，也不能证明深度一定有益。
- v9首层RGB卷积`[64,3,3,3]`是设计如此，另有IR/Depth编码器。第63轮辅助梯度记录约3.81，门控均值约0.10–0.19；不支持“辅助通道根本没参与”，也不能证明增加了准确率。
- v4无明确的`sports ball → ball`同义类迁移；v10保留当前两头逐层迁移校验。这是合理补强，不是证明ball历史AP=0只有一个原因。
- v4旧训练集包含现验证集新增的样本，不能用v4训练好的best作为新初始化后，再把当前验证当成从未见过的数据。v10从官方COCO权重重新迁移。

## 推理检查

读取`predict_v9/prediction.json`：1000张、250批×4、EMA、epoch63、backend=fusion、FP32、1920×1080内容/1920×1088张量、conf=0.001、iou=0.7、单标签、max_det=100、无TTA。SHA256为`4f21b6011721613c51a1c043ce0337e3853e0c663b2c85f2be6bc69ee74bce86`，与当前v9 best.pt文件匹配。这确认了本地预测记录对应权重；线上53.4750仍以用户反馈为来源。

训练/推理都调用`aic.data.fuse_modalities`，使用RGB3+IR1+Depth1和/255；v9共用letterbox、内容裁框与单标签NMS，推理按保存的几何参数还原原图坐标。PNG压缩用于推理后的带框图片，不影响输入或TXT。没有找到足以解释全部分差的明显代码错误，但未重新执行模型验证，不能写成“已证明推理完全正确”。不建议凭感觉提高conf或加入多模型WBF。

## v10实施配方

1. 官方YOLO26l→五通道首层早期融合；移除本轮独立随机辅助分支，整个预训练主干共同接收模态信号。新增两个输入卷积切片仅初始化为0，之后正常训练；三模态图像始终真实输入，不是模态置零伪装。
2. 训练、验证、预测统一1920×1080内容，等比缩放后只补8像素成为1920×1088；不做1920方形训练，不把长边缩成1080。前40轮Mosaic=0.25（75%样本保持完整画面）、scale=0.3、translate=0.1、fliplr=0.5；复用已有矩形拼图及逐通道仿射组件，不宣称与v4原生随机Mosaic完全相同。第41轮关拼图，改为scale=0.1/translate=0.025的完整画面轻增强，避免v9后期几何正则化全部取消。仅RGB施加HSV=0.01/0.15/0.15，IR/Depth不做颜色增强；这不是已证明的单项提分幅度。
3. AdamW所有参数lr0=1e-4、wd=5e-4、batch=2/nbs=16、warmup=5、120轮余弦/lrf=0.01、cls_pw=0.25；保留v4主干更新强度，不再用2e-5，并缩短衰减周期。类别权重会同时影响该类正负项，收益仍待验证，不叠加稀有类重复采样。全尺寸显存未实测，用户可自行将batch改4，禁止自动探测或偷偷降分辨率。
4. 双头训练固定0.8/0.2，单头输出；v4的5000轮上限使前200轮双头权重仍以一对多为主，不能直接将epochs改200却沿用随epochs变化的原生双头衰减。v10日志损失取一对多头，与v4原生打印的头不同，不跨版本直接比较损失绝对值。
5. 从第一轮计早停，patience=30、min_delta=0。保留best(AP95)、best_map50候选、last与周期检查点。不为等待收尾强行继续训练，不在末尾额外重载复评。
6. FP32轮末验证与正式预测、单标签NMS、100框上限。预测默认新v10 best、1920/height1080，自动识别早期融合检查点并走共用固定矩形批量后端（batch=4），不再使用逐图v4原生补边。检查点存储结构版本和内容宽高，不匹配直接报错；prediction.json记录实际结构版本/尺寸/批次。历史v4仍可显式1280、yolo_profile=v4预测。用户的OUTPUT_PATH=`predict`保持不变，已有目录不覆盖。
7. 清洗数据1709/291和57文件修订原样保留，训练前核验既有审计，不重洗、不改变验证划分、不生成大NPY缓存。每轮仍留逐类/分域指标；辅助切片权重范数仅为参与学习的诊断，不当作贡献评分。

完整YOLO参数仍显式列于main.py，非检测任务参数不会因为写出来就参与训练。参数定义参照[Ultralytics官方训练说明](https://docs.ultralytics.com/modes/train/)，具体执行以本机8.4.152源码为准，没有升级依赖。

## 如何执行与判定

```powershell
uv run python main.py
# 训练完成后核对实际目录，重名运行会带后缀。
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1920x1080_v10_early_fusion/weights/best.pt --imgsz 1920 --height 1080 --output predict_v10
```

这是待验证的基线修复候选，不是已证实“最优模型”。先与v9在同一1709/291划分的总体AP95、逐类及PNG/JPG分域表现对照，再看用户正式提交；不能要求v10必须复现旧验证集0.40370才算有效，也不能只按AP50峰值选权重。

目标60意味着相对v4还需4.4分，不能根据群聊推算“某技巧必加2分”。本轮按用户要求组合全尺寸、RGB增强和分阶段轻增强，不是单变量实验，最终涨跌不能直接归因到一个开关。如果仍无法突破，下一步优先结合正式训练保存的验证可视化，对船/灯/垃圾桶及少样本类进行约200张人工标注与三模态对齐复核，记录后再改标签；这项人工复核本轮未完成。进一步比较应固定划分并逐项改动。老师的难例挖掘须额外运行模型，本轮没有擅自启动；仅官方训练数据可用，拒绝多权重投票与测试集伪标注。

保留所有历史权重、日志及推理后端。历史v9已完成，不再续训；如需恢复旧代码生成的中断运行，应使用对应代码快照，不绕过源码/配方签名。


---

<a id="record-3"></a>

## 原文件：docs/v10复盘与v11筛选方案.md

# v10复盘与v11筛选方案

更新：2026-09-20。结论来自已有`results.csv`、逐类指标、训练日志、预测元数据和用户线上分数；没有重新训练、推理、复评或执行测试。

## 结论

v10不是因为只训练49轮而欠拟合。其最佳mAP50-95出现在第19轮（0.33592），此后训练损失继续下降，验证损失总体恶化，连续30轮没有刷新AP95并按配置早停。继续沿同一轨迹训练更可能加深过拟合，不能以延长轮数作为主方案。线上50.9240也低于v4的55.6000。

“首轮mAP50-95必须达到0.4”不适合作为停止条件：v4首轮为0.29976，最终验证峰值0.40370且线上55.6000；若使用0.4首轮门槛，当前最强历史模型也会在第一轮被误杀。首轮可用于发现明显的通道、标签或迁移错误，但不能预测最终上限。

| 运行 | 第1轮AP95 | 第5轮AP95 | 第10轮AP95 | 第20轮AP95附近 | 最佳AP95 | 线上分数 |
|---|---:|---:|---:|---:|---:|---:|
| v4 clean | 0.29976 | 0.33212 | 0.33903 | 0.36784 | 0.40370 | 55.6000 |
| v9 gated | 0.21490 | 0.30695 | 0.33511 | 0.32881 | 0.34952 | 53.4750 |
| v10 early fusion | 0.25831 | 0.30289 | 0.29404 | 0.31602 | 0.33592（第19轮） | 50.9240 |

## v10暴露的问题

v10第19轮相对v9最佳的AP95变化并非整体小幅波动：seat约下降8.05个百分点、ball下降16.12个百分点，boat、animal、car和tricycle也回退；person、bicycle、light和uav有所上升。尤其ball首轮AP95为0.35996，v10最佳总体轮仅0.03541，说明类别头和语义迁移能够识别ball，但后续优化将其破坏；这不支持“融合代码从未学到ball”的解释。

v10相对v9同时改变了架构、学习率、增强和`cls_pw`，无法从一次运行证明唯一因果。已有历史提供一个值得优先隔离的方向：v4/v10启用`cls_pw=0.25`时ball表现差，而v5/v9关闭该项时ball明显更可用。类别频率权重同时影响对应类别的正负分类项，并不是只增强正样本。因此v11只关闭该项，其余关键条件保持不变，避免继续一次改多个变量。

## v11配方与阶段筛选

v11继续使用官方YOLO26l、五通道首层融合、清洗版1709/291、1920×1080内容/1920×1088张量、AdamW全模型1e-4、batch4/nbs16、120轮余弦，以及v10相同的前40轮增强和后续完整画面阶段。唯一训练变量是`cls_pw: 0.25 → 0.0`。

为节省明显弱配方的时间，训练器在指定轮次检查“从第1轮至当前的历史最佳mAP50-95”，不是检查单轮值：

- 第10轮历史最佳低于0.31：停止；该值低于v4第5轮，也容许短期波动。
- 第20轮历史最佳低于0.34：停止；v4在此之前已明显越过，v10则无法达到。
- 通过两关后继续由`patience=30`和AP95择优控制；门槛通过不代表一定超过v4或线上60分。

停止时仍保留当轮结果与检查点，用于复盘；未通过的候选不建议提交比赛。若v11通过第20轮但ball再次从早期高点大幅塌陷，下一步应检查逐类分类梯度或采用受限的train-only类别采样，而不是恢复`cls_pw`、盲目加轮数或改预测阈值掩盖训练问题。

## 运行

```powershell
uv run python main.py
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1920x1080_v11_no_class_weight/weights/best.pt --imgsz 1920 --height 1080 --output predict_v11
```

预测必须使用实际生成目录。赛事提交仍为单模型、单轨迹best权重，不使用投票、WBF或测试集调参。


---

<a id="record-4"></a>

## 原文件：docs/v11复盘与v12训练方案.md

# v11复盘与v12训练方案

更新：2026-09-20。只读取已有训练产物和历史代码，没有运行额外评估、测试、训练或推理。

## v11结论

v11正常通过第10轮门槛，并在第20轮因历史最佳AP95=0.32668低于0.34停止。最佳出现在第5轮：mAP50=0.52537、mAP50-95=0.32668。停止是预设筛选生效，不是异常崩溃；用户后续提交得到线上44.4710。

关闭`cls_pw`解决了一部分ball问题：第5轮ball AP50=0.52781、AP95=0.41520，明显高于v10最佳总体轮的ball AP95=0.03541。但v11总体没有提高，且后续ball仍回落；这说明类别频率权重会影响弱类稳定性，却不是当前总体分数的主要瓶颈。

v4仍是唯一线上达到55.6000的方案，使用原生1280方形几何。v9至v11改为1920×1088固定矩形后，线上和本地总体指标均没有超过v4。更大输入保留了像素，但同时改变了填充、Mosaic、透视增强、批次内几何和目标尺度分布；“像素更多”不等于有效监督更多。

## v12方案

v12恢复v4的原生1280数据链，而不是从v4权重续训：

- 官方YOLO26l重新迁移，五通道首层早期融合；
- 当前清洗版1709 train / 291 val与审计保持不变；
- 原生1280×1280 LetterBox、Mosaic和检测增强链；
- batch4/nbs16、AdamW 1e-4、warmup 5、200轮余弦；
- 前100轮Mosaic0.5、scale0.3、translate0.1，后100轮关闭Mosaic；
- HSV全部为0、`cls_pw=0`，保留ball语义迁移；
- 固定双头0.8/0.2、FP32单标签验证、best按AP95保存；
- 第10轮历史最佳AP95须达到0.34，第20轮须达到0.36，否则停止。

本轮不同时加入ball/tricycle采样。v11已经证明ball能够学到，当前首先隔离固定矩形几何是否是总体回退来源；若v12通过门槛但最终只在极少类上回退，再单独加入限额采样才有可解释性。

v4的验证集为旧256张，v12为当前291张，不能要求本地AP逐点复现；真正是否超过v4仍以赛事提交为准，也不承诺达到60分。

## 运行

```powershell
uv run python main.py
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v12_native/weights/best.pt --imgsz 1280 --height 1280 --output predict_v12
```


---

<a id="record-5"></a>

## 原文件：docs/v12复盘与v13训练方案.md

# v12复盘与v13训练方案

更新：2026-09-20。只读取v12正式训练产物、历史v4结果和源码，没有运行测试、额外评估、训练或推理。

后续更正：v12线上47.3270；v13完成151轮，最佳51轮0.35681，后期退化。下述门槛仅解释v12的停止原因，不能称作模型低分的根因；移除门槛也未带来突破。预测补边不一致和下一轮安排见[v13复盘](02_v9至v14复盘.md#record-6)。本文保留当时方案供追溯。

## 提前停止原因（不是性能回退的已证实根因）

v12第10轮被阶段筛选停止，但不是因为曲线已经恶化。其AP95从第1轮0.26555逐步达到第7轮0.33056，并在第10轮刷新到0.33257；同期mAP50达到0.55075。ball第10轮AP50=0.58091、AP95=0.45121，也没有出现v10式塌陷。

停止门槛0.34来自v4旧256张验证集，而v12使用当前291张验证集。当前验证集包含从旧训练划分移入的场景，v4权重见过其中一部分；两条曲线不能用于绝对阈值标定。v12距离门槛仅0.00743，且当轮仍创新高；当时只能确认存在过早截断上升轨迹的风险，不能预先断言这是最终优质配方的假阴性。

用户补充的v11线上成绩为44.4710，已纳入历史记录。该结果进一步说明不能用本地单轮指标直接换算赛事分数，但不改变v12门槛缺乏同口径依据的事实。

## v13唯一改动

v13保持v12的模型、官方初始化、清洗数据、1280原生方形几何、batch4/nbs16、AdamW 1e-4、200轮余弦、前100轮Mosaic0.5、后100轮关闭Mosaic、HSV全0、`cls_pw=0`、固定双头和预测路径不变。

唯一改动是删除第10/20轮硬门槛，让AP95最佳权重和`patience=100`决定停止。这样才能检验v12上升轨迹能否在关闭Mosaic前后继续改善。此次不叠加弱类采样、RGB单模态或新结构。

## 运行

```powershell
uv run python main.py
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v13_native_full/weights/best.pt --imgsz 1280 --height 1280 --output predict_v13
```

v13是否超过v4仍须由完整训练和正式赛事提交确认，不预先承诺60分。


---

<a id="record-6"></a>

## 原文件：docs/v13复盘与v14阶段筛选.md

# v13复盘、预测补边修正与v14阶段筛选

2026-09-21 完成追加：v14实际55轮结束，最佳第15轮AP95=0.39215，第55轮patience40正常早停，10/20/50轮阶段门槛通过。best.pt按固定291张、FP32/batch2独立复评AP95=0.39179，高于v13同口径0.35655；后期仍过拟合，没有达到0.60。下文“准备/完成后”等是启动前计划；当前无需重复开训。详见[v14执行记录](02_v9至v14复盘.md#record-7)。

2026-09-21 执行追加：用户已授权修复、测试与训练，当前已完成10项回归和短训练/预测闭环。新协议统一内容裁框，并用已有v13重新建立291张val基线；原生polish_*从无效的0改为None，明确保留原scale/translate，轻增强须另行显式启用。最佳轮图单独保存，不再混用末轮图。正式状态见[v14一致性修复与执行记录](02_v9至v14复盘.md#record-7)；下文“未测试”等是2026-09-20状态。

更新：2026-09-20。读取v13的args.yaml、optimization_recipe.json、results.csv、results.png、per_class_metrics.csv、domain_metrics.csv、fusion_diagnostics.csv、train.log，以及两份Ultralytics参数参考和本机框架源码。只整理已有产物与修改源码，没有运行测试、AST/compile/import检查、模型前向、独立评估、训练或预测。

## 一、结论与分数口径

- 用户确认v12线上为 **47.3270**；具体提交权重和prediction.json未在本轮核验，不能把预测差异定量归因为全部掉分来源。v13未提供数值赛事分数，不代填。
- v13完成151轮、约5.013小时；最佳AP95为第51轮 **0.35681**，该轮AP50=0.56103；AP50峰值为第14轮0.57054。末轮AP95=0.31912。
- v13相对v12第10轮的0.33257提升0.02424，但后100轮没有刷新AP95。训练损失下降、验证损失恶化，支持后期过拟合，不支持继续增加轮数。
- 以前把“v12被阶段门槛停止”写成性能问题的“根因”过于武断。它解释了为什么停止，不能证明继续训练能超过v4；v13已经说明延长训练不足以解决问题。
- 本地mAP50-95达到0.60与线上60分是两个目标，不能换算。v4本地最佳0.40370对应线上55.6000；不同验证划分不能以绝对AP差判定架构因果。首轮分数不是最终上限。

## 二、训练轨迹

| 轮次 | 当轮AP95 | 截至该轮最佳AP95 | 当轮AP50 | 说明 |
|---:|---:|---:|---:|---|
| 10 | 0.33257 | 0.33257 | 0.55075 | 与v12末轮相同 |
| 20 | 0.33017 | 0.34157 | 0.53071 | 暂时最佳第14轮 |
| 50 | 0.34236 | 0.34598 | 0.54790 | 改进已经较慢 |
| 51 | 0.35681 | 0.35681 | 0.56103 | 全程最佳，使用best.pt |
| 75 | 0.31910 | 0.35681 | 0.51403 | 整体退化 |
| 100 | 0.32112 | 0.35681 | 0.50541 | 尚未关闭Mosaic |
| 125 | 0.31713 | 0.35681 | 0.51171 | 关闭Mosaic后未创新高 |
| 151 | 0.31912 | 0.35681 | 0.49889 | 100轮无新高，停止 |

第51→151轮，训练box/cls损失从1.13605/0.59312降到0.82298/0.37184；验证box/cls从1.36858/1.23545升到1.41372/1.47702。第101轮关闭Mosaic后的训练损失下降也包含增强难度变化，不能单独当成泛化改善。

## 三、各类学习情况

下表为同一轮权重的AP95百分数，不拼接各类独立峰值。v4使用旧256张验证集，仅供背景参考；v12/v13使用当前291张。

| 类别 | v4 best历史日志 | v13第10轮 | v13最佳第51轮 | v13末轮151 |
|---|---:|---:|---:|---:|
| person | 38.60 | 42.56 | 38.51 | 35.20 |
| boat | 78.50 | 45.57 | 75.85 | 76.82 |
| animal | 50.90 | 50.17 | 51.99 | 47.90 |
| seat | 20.30 | 20.39 | 16.65 | 17.44 |
| sign | 28.10 | 20.89 | 26.90 | 28.25 |
| bicycle | 16.20 | 22.64 | 23.70 | 15.77 |
| car | 35.80 | 40.34 | 34.24 | 32.58 |
| ball | 0.00 | 45.12 | 38.50 | 20.04 |
| light | 18.10 | 13.29 | 15.56 | 16.21 |
| garbage can | 40.00 | 31.30 | 32.39 | 31.18 |
| uav | 76.40 | 39.86 | 42.50 | 41.11 |
| tricycle | 79.50 | 26.95 | 31.39 | 20.44 |

ball不是没学会，而是后期退化；seat/light一直偏弱，bicycle/person/car也需保护泛化，不能只盯ball。当前val的boat/ball/uav/tricycle分别7/17/37/12框，旧val为7/12/3/2框，稀有类别AP波动尤其大。不能把同一图重复采样几次等同新增独立样本。

第51→151轮，PNG域AP95=0.37867→0.35260，JPG域0.40446→0.36867，两个域均退化；JPG只有24张，不能据此认定其普遍更容易。分域AP不是总体AP的图像数加权平均。

fusion_diagnostics.csv中IR/Depth首层权重范数从首轮0.00439/0.00516到末轮0.06448/0.04409，说明切片确实更新，并非永久冻结为零；范数大小不能证明模态贡献或对齐质量。

## 四、确认的代码问题：原生验证与预测补边不一致

v12/v13训练器选择`native_square`：

1. 原图按长边1280缩放，短边向上取整，包含放大小尺寸JPG。
2. 原生`LetterBox(scaleup=False)`对已缩放图填满1280方形；五个通道补边值全部为114。
3. 五通道保持RGB3+IR1+Depth1顺序，归一化除255，FP32轮末验证。

旧predict.py只按模型类选fusion后端，忽略权重配方的`geometry`，调用旧固定矩形`letterbox_fused`：RGB补114，IR/Depth补0，短边用round。1920×1080输入对应1280×720内容，上下各280像素补边，占画布43.75%。这是真实的输入分布差异，不是输出图片压缩问题。

本次修改：预测读取检查点`fusion_training.signature.recipe.geometry`；`native_square`改用共享长边ceil缩放与原生五通道114补边；历史`fixed_rect`保留辅助通道0补边，旧v4和D-FINE不改。prediction.json记录`validation_geometry`、`padding_values`、准确的预处理版本，固定画布不再误记为`rect=True`。

这修正了源码上的已知差异；未运行预测验证收益。它不能解释v13本地AP95停滞，因为本地验证一直采用114补边。v10/v11固定矩形路线也不属于此bug范围。不能承诺修正后线上上60。

优先复用v13最佳权重，无需重训：

```powershell
uv run python predict.py
```

默认输出新目录`predict_v13_preprocess_fix`，提交其中`比赛提交内容/submission.zip`。已经存在该目录时改用新的`--output`，不会覆盖。必须看到启动日志显示`native_square`和五个114；本次助手未执行此命令。

## 五、v14：一个优化假设加预算约束

运行名`AIC_RGBIRDepth_yolo26l_1280_v14_backbone_guard`。继续从官方COCO初始化，不续训v13末轮，也不拿见过当前部分val图的v4作基底。

训练优化假设：全模型1e-4可能使预训练特征过度适配小训练集。因此第1–10层骨干改为2e-5，第0层五通道首层、颈部和双检测头保留1e-4。首层IR/Depth仍正常学习，不做伪多模态。沿用原生AdamW的bias/归一化不衰减分组；框架预热逐组使用initial_lr，保持比例。

这是待正式训练检验的假设，不是已确认根因或最优值。v5/D-FINE也曾采用分层学习率，但其结构、增强与划分不同，不能据历史直接证明本候选有效。

保留v13的模型、1709/291审计清洗、1280方形、batch4/nbs16、BF16训练/FP32验证、200轮余弦、warmup5、wd0.0005、cls_pw0、固定双头0.8/0.2、前100轮Mosaic0.5/scale0.3/translate0.1/HSV0。没有重洗、改划分、加外部数据、弱类复制或新模型。

预算改变：`patience=40`从首轮计数；另外执行下表阶段门槛。任一条件触发即停止，200轮只是上限，不强求走完Mosaic收尾。源码变更后不恢复旧中断配方，应从官方权重新训。

| 轮次 | 历史最佳mAP50-95至少达到 | 依据 |
|---:|---:|---|
| 10 | 0.32 | 留出适配空间；v13为0.33257 |
| 20 | 0.34 | v13历史最佳0.34157附近 |
| 50 | 0.36 | 要求超过v13全程最佳0.35681 |
| 75 | 0.38 | 用户预算目标，并非预测值 |
| 100 | 0.40 | 用户预算目标，并非预测值 |
| 125 | 0.45 | 用户预算目标，并非预测值 |
| 150 | 0.50 | 用户预算目标，并非预测值 |
| 175 | 0.55 | 用户预算目标，并非预测值 |
| 200 | 0.60 | 用户本地目标，不等同线上60分 |

按历史最佳而非当轮波动判定；等于门槛通过。`screening_history.csv`记录当前值、历史最佳、门槛和继续/停止原因；正常阶段停止沿框架轮末流程保存results.csv与last.pt，best.pt保留真正历史最佳，不用末轮覆盖。200轮门槛仅记录最终是否达标，无法节约此前计算。若套用v13已有轨迹，第50轮会被0.36门槛筛掉；这也会错过第51轮0.35681，但该峰值仍未达到0.36。这是主动接受的预算取舍，并非证明其永无提升可能。

用户需要新训时运行：

```powershell
uv run python main.py
```

v14完成后显式指定新权重，预测默认仍留给已有v13修正提交：

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v14_backbone_guard/weights/best.pt --imgsz 1280 --height 1280 --output predict_v14
```

## 六、参数参考带来的约束与后续决策

- `patience`与阶段门槛只节省时间，不直接提升AP。分辨率变大不保证分数变高；此前同时改过增强、批次和几何，不能只归因分辨率。
- `dropout/erasing/auto_augment`等分类配置不是当前detect的通用抗过拟合开关；本机RandomHSV会跳过非三通道，五通道原生链仅调HSV数值也不会实现RGB专用增强。
- 不将`conf`从0.001盲目改到0.25，也不靠`visual_conf`或PNG压缩调AP；NMS阈值属于真实候选筛选，变化有利有弊。本次固定conf/iou/max_det，先消除已确认的输入不一致。
- 若v14筛选失败，保留记录，不自动放宽门槛重跑。下一步优先人工复核训练集seat/light/garbage can等漏标、框边界及三模态空间对齐，而不是按模型预测自动删难例。此前200张人工复核仍未完成；本轮没有新做清洗。
- 再换架构或RGB+IR主导融合须有同划分依据、仍真实使用所有要求模态；不能用测试集训练、清洗或多权重投票来追分。

参数语义参考用户保存的两份文档、本机8.4.152源码及[官方训练说明](https://docs.ultralytics.com/modes/train/)、[官方预测说明](https://docs.ultralytics.com/modes/predict/)。官网可能更新，当前接口以本机源码为准；官网不是本项目达到60分的证据。


---

<a id="record-7"></a>

## 原文件：docs/v14一致性修复与执行记录.md

# v14 一致性修复与执行记录

日期：2026-09-21。用户已批准上一轮审查方案，并明确授权相关代码修改、测试、训练、推理；未授权提交或推送。本次在当前工作区继续，不覆盖历史产物，不上传赛事数据。

后续追加：[v15短周期收尾](03_v15至v19实验.md#record-3)已于2026-09-21 11:15完成9轮训练及一次独立复评，最佳第1轮，AP95=0.3849390289，比v14下降0.68486个百分点，保留v14。第5轮筛选通过，第9轮耐心早停，不是第10轮门槛误停。下面“下一候选尚未实施/启动”是v14交付当时的状态；v14权重与复评基线未改。下一步训练样本审阅需另行开展，不自动再训练。

## 设计与验收

保留 v4 的真实融合机制：官方 RGB 预训练、五通道首层、新增 IR/Depth 卷积切片零初始化且可训练。不是门控融合，不永久清零模态。固定现有清洗版 1709/291，禁止用曾见过新增验证图的 v4 权重续训后声称公平胜出。

统一当前训练验证与预测的 native_square 缩放、五通道 114 补边、单标签 NMS、真实内容区裁框。评估协议版本单独记录；先用已有 v13 best 在新协议下重新建立本地基线，不能直接把协议差异算作新训练收益。历史 v4 的旧验证数据和矩形协议保留为历史事实。

训练变量仍只有骨干第1–10层学习率降至 2e-5，首层/颈部/头保持 1e-4。保留完整入口参数、200轮上限及阶段预算门槛。原生轻增强收尾改为显式可选并验证有效，本次不启用；避免和学习率实验混合。早停可能先于第101轮关闭 Mosaic，不承诺完整收尾或 AP95 达到0.60。

## 执行清单

- [x] `tests/test_training_consistency.py`：10项回归覆盖原生预处理、边缘裁框、缓存关闭/失效、内容审计、可选收尾及早停禁用。
- [x] `aic/model.py`、`aic/training.py`、`predict.py`：共用输出几何处理并记录评估协议；保留历史推理兼容。
- [x] `aic/training.py`：缓存包含读图实现，标签内容加入缓存键；开训核对来源图像哈希；最佳轮图与指标单独保存，不追加模型前向。
- [x] 原生收尾参数不再静默无效；`patience=0`遵循禁用早停语义。
- [x] 两批 GPU 训练/验证、检查点重载及预测打包检查，以及已有 v13 的完整291张本地基线；不推理官方测试集。
- [x] 修正当前文档、参数注释及授权记录，保留真实测试结果和限制。
- [x] 正式 v14 已启动并完成55轮，最佳第15轮；独立重载复评确认相对v13有提升，未达到0.60。

## 资源与范围

开始时 RTX5080 显存占用约1.2GiB，无 Python 训练进程，可用内存约18GiB，D盘剩余约32GiB。保留 batch4、workers4、低预取、不写大体积NPY；不杀其他程序，不自动扩大批次。测试使用独立临时目录，报告放 runs 中。仅本地执行，不采用云训练技能的上传流程。

## 实测结果

1. `.venv/Scripts/python.exe -m pytest tests/test_training_consistency.py -q --tb=short`：最终10 passed，约2.5秒。裁框、缓存、原生收尾、早停及新快照接口均先观察到相应失败再修正。快照测试发现NumPy整数不能直接写JSON，已显式转换。
2. `runs/checks_20260921/main_check.py`：第一次已完成两批训练/验证，但检查脚本漏填PredictionConfig必填项，预测环节未完成。补齐检查脚本后复跑至`main_check-2`成功：2训练批、2验证批、2优化步，生成并重载best.pt，2张val图（PNG/JPG）预测和ZIP打包通过。峰值reserved显存11.88086GiB。测试产物不是正式训练成果。
3. RGB首层迁移逐值一致，额外IR/Depth切片初始为零、requires_grad=True，实际反向得到非零梯度。PNG/JPG验证输入与预测输入逐像素相等。没有测试全部历史D-FINE/门控模型或长期断点恢复。
4. `.venv/Scripts/python.exe -m tools.validate_local --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v13_native_full/weights/best.pt --output runs/local_validation/v13_content_clip_v2`：291张val，AP95=0.3565488837，AP50=0.5577577474。修复标签缓存键后重新运行到`v13_content_clip_v2_fresh_cache`，标签重扫且两项结果完全相同。
5. 新协议v13基线不是新模型成绩；其与旧CSV AP95=0.35681接近，边缘裁框不是本地性能瓶颈。补边修复影响旧预测路径，不能由本次验证推导线上涨分。后续v14须与新协议0.35655比较，仍不等于当前验证划分上超过v4——v4旧AP来自另一划分，而且见过后来移入val的35张图。

评估完整元数据、逐类指标、PR曲线和混淆矩阵保存在`runs/local_validation/v13_content_clip_v2_fresh_cache/`。正式训练每次AP95新高保存`best_validation/metrics.json`与同轮曲线；根目录图只代表最近被绘制的验证轮次。

## 正式训练完成与独立复评（2026-09-21 追加）

2026-09-21 01:53:16后台启动，启动器PID为36464，约03:44结束，现在已无该训练进程。运行目录为`runs/detect/AIC_RGBIRDepth_yolo26l_1280_v14_backbone_guard/`，启动输出在`runs/checks_20260921/launch_v14/`。完成55轮，框架报告1.853小时；第15轮最佳后40轮没有AP95新高而正常早停，不是异常退出，也不是阶段门槛失败。第10/20/50轮筛选全部通过。

| 口径 | v13 best 第51轮 | v14 best 第15轮 | v14变化 |
|---|---:|---:|---:|
| 训练当时记录 AP50 | 0.56103 | 0.62932 | 不作为统一协议复评差值 |
| 训练当时记录 AP95 | 0.35681 | 0.39215 | 不作为统一协议复评差值 |
| 同协议重载权重 AP50，batch2 | 0.5577577474 | 0.6293876168 | +7.163个百分点 |
| 同协议重载权重 AP95，batch2 | 0.3565488837 | 0.3917876103 | +3.524个百分点 |

v14复评目录为`runs/local_validation/v14_best_content_clip_v2_batch2/`；固定291张、FP32、1280、单标签NMS、conf0.001/iou0.7/max_det100，与v13复评相同。v14权重SHA256为`b9dcf291dbe55857620b84b5f2f08b196c71bec6e129bfcd9bfb29a7ddcb557c`。评估工具补记batch/workers，不改变评估运算。

先按训练的batch1重载复评得到AP50=0.6289019752、AP95=0.3915779482，保存在`v14_best_content_clip_v2/`；随后按v13基线batch2对齐，以上表后两行为当前比较依据。框架保存EMA时转为FP16，重载FP32不能还原舍入前参数；本次也观察到batch1/2的微小数值差异。因此训练中最佳图/JSON与重载复评结果分别留存，不声称逐位一致，也不据此认定存在重大推理故障。

## 结论与下一步边界

- 本次真实进步来自一次受控训练对照，支持降低预训练骨干更新幅度的方向；仍只有单种子，不能宣称所有场景必然改善。来源图像与标签没有改动，不能把本次提高算成重新清洗的收益。
- 第15→55轮train/box_loss从1.26023降至1.07916，但val/box_loss从1.27950升至1.36625，val/cls_loss从1.12098升至1.19295，AP95降至0.35687。仍有后期过拟合，增加轮数不是已证明的解决办法。
- 第一轮AP95仅0.24671，后面仍达到0.39215，再次说明首轮低于0.4不能判定配方无效。
- 第101轮才关闭Mosaic，实际第55轮已停，所以本轮从未进入无Mosaic收尾。这是现有日程与早停的交互，不应靠放宽门槛掩盖。下一候选是保留v14作为基线，从其best做独立的短周期低学习率、关闭Mosaic收尾；须另建运行并记录新初始化/优化器，不能把已完成的last当中断resume。该候选尚未编写或启动，不自动扩展为多模型搜索。
- 逐类AP95有10类提高，boat与ball回退。boat只有2张/7框，不能仅凭该类波动自动改标签或加强重采样。下一轮数据复核优先查训练集boat/ball，以及light的框边界和三模态对齐；不改验证标签以追分，不根据预测自动删难例。此前约200张人工复核仍未完成。
- 不把v4旧划分AP95=0.40370和当前结果直接排胜负；v4见过当前val中的35张图。没有赛事新分数，本地0.60目标尚未达到。当前可用候选是v14第15轮best.pt，不是第55轮last.pt。
- 本轮未提交或推送Git，未运行官方测试集推理、未删除历史权重，也没有启动第二次正式训练。main.py保留可复现的v14配方，predict.py原有v13默认选择不擅自覆盖；使用v14时显式指定权重。
