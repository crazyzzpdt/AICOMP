# 05_旧运行与环境记录

整理日期：2026-09-23。以下按原文件完整合订，保留当时的数据、结论与实施状态。历史“当前”“未训练”“未提交”和测试命令均只代表记录当时，**不覆盖现行入口和默认禁测约束**。现行信息见[知识索引](../项目知识索引.md)。原文件可从Git历史恢复；正文仅迁移引用路径，不把旧建议当作已证实收益。

## 原文件目录

- [docs/电脑训练环境.md](#record-1)
- [docs/训练前释放资源.md](#record-2)
- [docs/比赛任务要求.md](#record-3)
- [docs/代码结构与运行.md](#record-4)
- [docs/预测与赛事提交.md](#record-5)
- [docs/项目知识索引.md](#record-6)
- [docs/superpowers/plans/2026-09-16-predict.md](#record-7)
- [README.md](#record-8)


---

<a id="record-1"></a>

## 原文件：docs/电脑训练环境.md

# 电脑训练环境

2026-09-18 项目状态：当前v8为D-FINE-L五通道、1280、batch=4/有效16、workers=4，BF16训练前向与FP32损失/验证；预测使用FP32。旧YOLO的显存实测不能推出D-FINE峰值，GPU占用率也不能证明检测质量。资源由用户管理，本轮未采集硬件实时数据或运行模型测试。依赖以pyproject.toml/uv.lock为准，方案见 [知识索引](../项目知识索引.md)。

2026-09-16 配方同步：当前 v3 保留用户已完成长训的 YOLO26l/五通道/1280/batch=5/workers=4/BF16/disk 配置，v2 日志显存约 15 GiB，179 轮耗时 5.518 小时。该数值是已完成运行的日志记录，不是 v3 的全程峰值保证。训练日程与精度比较见 [训练配置与数据集复核](04_数据与调参经验.md#record-2)。资源释放由用户管理。

以下驱动、磁盘剩余空间与可用内存为历史采集快照，不能当作当前实时资源量；本次文档更新未调整系统设置。

## 1. 训练环境总览

| 项目                 | 配置                                           |
| -------------------- | ---------------------------------------------- |
| CPU                  | AMD Ryzen 9 9950X                              |
| CPU 核心/线程        | 16 核 / 32 线程                                |
| GPU                  | NVIDIA GeForce RTX 5080                        |
| GPU 显存             | 16303 MiB，约 16 GB                            |
| 系统内存             | 31.2 GB，约 32 GB                              |
| SSD                  | WD_BLACK SN850X 2000GB                         |
| SSD 类型             | NVMe SSD                                       |
| SSD 健康状态         | Healthy / OK                                   |
| PyTorch              | `2.14.0+cu132`                                 |
| PyTorch CUDA Runtime | CUDA 13.2                                      |
| CUDA 可用            | 是                                             |
| Ultralytics          | 项目要求 `>=8.4.152`；锁文件快照中为 `8.4.152` |
| Python               | 项目声明 `>=3.14.5`                            |
| 包管理器             | `uv`                                           |
| 主要训练设备         | `cuda:0` / `device=0`                          |

已实际执行并验证：

```text
PyTorch: 2.14.0+cu132
CUDA: 13.2
GPU: NVIDIA GeForce RTX 5080
```

## 2. GPU / CUDA 信息

NVIDIA 驱动检测结果：

```text
GPU: NVIDIA GeForce RTX 5080
VRAM: 16303 MiB
Driver: 616.92
Driver CUDA UMD capability: 13.4
```

当前 Python/PyTorch 环境：

```text
torch: 2.14.0+cu132
torch.version.cuda: 13.2
torch.cuda.is_available(): True
torch.cuda.get_device_name(0): NVIDIA GeForce RTX 5080
```

## 3. CPU 信息

```text
AMD Ryzen 9 9950X
16 cores
32 logical processors
```

## 4. 内存信息

物理内存：

```text
31.2 GB
```

## 5. 硬盘配置

物理硬盘：

```text
WD_BLACK SN850X 2000GB
MediaType: SSD
BusType: NVMe
HealthStatus: Healthy
OperationalStatus: OK
```

该 SSD 为 WD_BLACK SN850X 2000GB，NVMe SSD，健康状态 Healthy / OK。

### 当前分区

| 盘符 |     大小 | 剩余空间 |
| ---- | -------: | -------: |
| A:   |  1024 GB | 185.9 GB |
| B:   |   450 GB |  54.9 GB |
| C:   | 288.2 GB |  41.5 GB |
| D:   |   100 GB |  53.8 GB |

A/B/C/D 主要位于同一块 WD_BLACK SN850X 2TB 物理 NVMe SSD 上。

## 6. Python 项目与依赖管理

| 项目             | 值                                                                 |
| ---------------- | ------------------------------------------------------------------ |
| 包管理器         | `uv`                                                               |
| Python 要求      | `>=3.14.5`                                                         |
| 项目名称/版本    | `yolo 0.1.0`                                                       |
| 项目依赖声明     | `torch==2.14.0`、`torchvision>=0.29.0`、`ultralytics>=8.4.152`     |
| 锁文件实际解析   | `torch 2.14.0+cu132`、`torchvision 0.29.0+cu132`、`ultralytics 8.4.152` |
| 其他主要环境包   | `numpy 2.5.2`、`opencv-python 5.0.0.93`、`matplotlib 3.11.2`、`polars 1.44.2`、`psutil 7.2.2`、`pyyaml 6.0.3`、`requests 2.28.1`、`nvidia-ml-py 13.610.43` |


---

<a id="record-2"></a>

## 原文件：docs/训练前释放资源.md

# 训练前释放资源

2026-09-18 适用范围：本页服务、进程、磁盘和显存数据均为历史操作记录，不是自动执行清单。本轮没有停止进程、调整系统或做CUDA测试。当前D-FINE不全量缓存训练图像至RAM，旧YOLO的cache=disk不是D-FINE入口参数；调度由用户负责。v4恢复推理为模型逐张前向，current才是YOLO批量路径，D-FINE仍批量推理。见 [知识索引](../项目知识索引.md)。

2026-09-16 状态：资源管理由用户自行负责；本页为历史操作记录，不是每次训练的自动前置步骤。当前配方保留 batch=5、workers=4、BF16、disk；已完成 v2 日志显存约 15 GiB。此前发生过 NumPy CPU 内存分配失败，单次空闲内存快照不能证明长训没有内存瓶颈。最新配置见 [训练配置与数据集复核](04_数据与调参经验.md#record-2)。

记录日期：2026-09-15，实测机器见 `docs/电脑训练环境.md`（Ryzen 9 9950X / RTX 5080 16 GB / 31.2 GB 内存 / Windows 11 Pro）。

## 0. 给 AI 的使用说明

把本文件交给 AI 并说明"训练前释放资源"，按下面顺序执行：

1. 读第 1、2 节，确认收益预期与**保留清单**（杀错进程会导致桌面或本次会话中断）；
2. 跑第 4 节的安全检查（提权状态、系统代理、运行中的虚拟机）；
3. 执行第 3 节的 A（普通权限进程）与 B（提权服务）；D/E/F 段属于**持久性配置改动**（暂停更新、禁用自启、禁用计划任务），必须先取得用户同意再执行；
4. 用第 5 节验证，并报告清理前后的可用内存与 GPU 占用；
5. 若扫描出的进程/服务与本文件清单不同（换过软件），先列出来给用户确认再动手。

## 1. 结论与预期收益

- 历史快照：31.2 GB 内存，清理前 18.6 GB 可用，清理后 19.2 GB，净增约 0.6 GB；GPU 桌面占用从 981 MiB 降到 794 MiB。该快照不能排除后续 CPU 提交内存不足。
- 显存与系统内存都需按实际训练观察。旧 batch=4 五通道 1280 短训峰值已分配 10.89 GiB、保留 11.87 GiB，不代表当前 batch=5 或长训的峰值。
- GPU 占用**不会降到 0**：`dwm`、`NVDisplay.Container` 等桌面合成常驻 600~900 MiB，属正常现象。
- 清理效果有限但无成本，且全部可逆（见第 6 节）。

## 2. 保留清单（禁止结束）

| 对象 | 原因 |
|---|---|
| `explorer.exe` | Windows 桌面外壳，杀掉会丢任务栏与桌面图标 |
| `dwm.exe`、NVDisplay.Container、nvcontainer（显示容器） | 桌面合成与显示驱动容器，杀掉可能黑屏 |
| `System`、`Registry`、`smss`、`csrss`、`wininit`、`winlogon`、`services`、`lsass`、`svchost`、`WUDFHost`、`dasHost`、`taskhostw`、`sihost`、`LsaIso`、`NgcIso` 等 | 系统关键进程 |
| `HipsDaemon`、`HRWSCCtrl`、`HipsTray`、`PopBlock`、`wsctrlsvc.exe` | 火绒（用户明确要求保留）；进程名与显示名不同，`HRWSCCtrl` 对应的进程是 `wsctrlsvc.exe` |
| `claude.exe`、`pwsh.exe`、`OpenConsole.exe`、`cmd.exe`、`WindowsTerminal.exe` | AI 自己的会话**以及承载它的终端**，杀掉即中断本次操作。先查父进程链确认宿主（`claude.exe` 的父进程常是 `cmd.exe` 或 `WindowsTerminal.exe`），不要凭名字猜 |
| `pycharm64.exe`、`full-line-inference.exe`、`cef_server.exe`、`fsnotifier.exe`、`jetbrainsd.exe` | 用户要求保留 PyCharm（若要极致释放，见第 10 节，改用 Power Save Mode） |
| `python.exe` / 训练进程 | 本文件的目标是**开训前**清理，若已有训练在跑，先问用户 |

## 3. 清理清单

### A. 普通权限：用户级进程

```powershell
$targets = 'cc-switch','msedgewebview2','SearchHost','Widgets','WidgetService',
  'kernelUpdate','YunDetectService','OfficeClickToRun','AsusUpdateCheck',
  'WinRAR','Rar','UnRAR',
  'CrossDeviceResume','AppActions','DataExchangeHost','UserOOBEBroker',
  'LockApp','PrintDialog','SystemSettings','ApplicationFrameHost',
  'StartMenuExperienceHost','TextInputHost','ShellExperienceHost','ShellHost',
  'TabTip','RuntimeBroker','dllhost','smartscreen'
$freed = 0
foreach ($name in $targets) {
    foreach ($p in @(Get-Process -Name $name -ErrorAction SilentlyContinue)) {
        $mb = [math]::Round($p.WorkingSet64 / 1MB)
        try { Stop-Process -Id $p.Id -Force -ErrorAction Stop; $freed += $mb } catch { }
    }
}
"已释放约 $freed MB"
```

各目标说明：

| 进程 | 说明 |
|---|---|
| `cc-switch` + `msedgewebview2` | 配置切换工具的界面与其 WebView 树（约 550 MB） |
| `kernelUpdate`、`YunDetectService` | 百度网盘的版本检查/检测残留（在请求 pan.baidu.com 版本接口）；再次运行百度网盘会复活 |
| `OfficeClickToRun`、`AsusUpdateCheck` | Office 与华硕更新器，**提权进程，普通权限杀不掉**，交给 B 段 |
| `WinRAR`、`Rar`、`UnRAR` | 解压工具（本机 7.11，装在 `A:\压缩和解压软件`）。本体不常驻，**扫到即说明正在解压**，直接结束即可；右键菜单扩展 `rarext.dll` 挂在 `explorer.exe` 进程内，不是独立进程，杀不掉也不用杀 |
| `SearchHost`、`Widgets`、`WidgetService`、`TextInputHost`、`StartMenuExperienceHost`、`ShellHost`、`TabTip` | Win11 外壳与小组件（注意：会自启，见第 9 节） |
| `CrossDeviceResume`、`AppActions`、`DataExchangeHost`、`UserOOBEBroker` | 跨设备/AI 后台（约 148 MB） |
| `LockApp`、`PrintDialog`、`SystemSettings`、`ApplicationFrameHost` | 锁屏、打印、设置等 UI 残留 |
| `RuntimeBroker`、`dllhost`、`smartscreen` | 按需重启的宿主进程 |

### B. 需要提权：后台服务

服务是 SYSTEM 权限，必须先提权。若 AI 本身就是以管理员启动的（先跑第 4 节第 1 项确认 `IsAdmin: True`），**下面这套直接 `Stop-Service` 即可，输出就在当前进程里**；只有非提权时才需要把脚本写入临时文件再用 `RunAs` 启动——提权进程的标准输出不会回到调用方，所以让脚本自己写日志再回读：

```powershell
$script = @'
$log = "$env:TEMP\stop_bg_services.log"
$services = @(
    'Apple Mobile Device Service',   # Apple 移动设备支持
    'MySQL94',                       # MySQL 数据库
    'SQLWriter',                     # SQL Server VSS 写入器
    'VMAuthdService',                # VMware Workstation 授权
    'VMUSBArbService',               # VMware USB 仲裁
    'VMnetDHCP',                     # VMware NAT 网络的 DHCP
    'VMware NAT Service',            # VMware NAT
    'WSLService',                    # WSL
    'vmms',                          # Hyper-V 虚拟机管理
    'vmcompute',                     # Hyper-V 主机计算服务
    'GameViewerService',             # 网易 UU 远程
    'clash_verge_service',           # Clash Verge 代理助手
    'ClickToRunSvc',                 # Office 更新服务
    'NvContainerLocalSystem',        # NVIDIA App 后台（不影响显示与 CUDA）
    'WSearch',                       # Windows 搜索索引（减少训练期磁盘 IO）
    'DiagTrack'                      # Windows 遥测
)
$lines = @()
foreach ($s in $services) {
    try { Stop-Service -Name $s -Force -ErrorAction Stop; $lines += "已停止服务: $s" }
    catch { $lines += "停止失败 $s -> $($_.Exception.Message)" }
}
foreach ($p in @('OfficeClickToRun', 'AsusUpdateCheck', 'kernelUpdate', 'YunDetectService')) {
    try { Stop-Process -Name $p -Force -ErrorAction Stop; $lines += "已结束进程: $p" }
    catch { $lines += "进程已不存在或无法结束: $p" }
}
$lines += (Get-Service -Name $services -ErrorAction SilentlyContinue |
    Select-Object Name, Status, StartType | Sort-Object Name | Format-Table -AutoSize | Out-String)
$lines | Out-File -FilePath $log -Encoding utf8
'@
$path = "$env:TEMP\stop_bg_services.ps1"
$script | Out-File -FilePath $path -Encoding utf8
Remove-Item "$env:TEMP\stop_bg_services.log" -ErrorAction SilentlyContinue
Start-Process pwsh -Verb RunAs -ArgumentList '-NoProfile','-ExecutionPolicy','Bypass','-File',$path   # 弹 UAC，需用户点“是”
$deadline = (Get-Date).AddSeconds(100)
while (-not (Test-Path "$env:TEMP\stop_bg_services.log") -and (Get-Date) -lt $deadline) { Start-Sleep -Milliseconds 2000 }
if (Test-Path "$env:TEMP\stop_bg_services.log") { Get-Content "$env:TEMP\stop_bg_services.log" -Encoding utf8 } else { "等待超时：UAC 未确认" }
```

### C. 易漏项

- **VMware 的 `VMnetDHCP`、`VMware NAT Service` 注册在 `C:\WINDOWS\SysWOW64\` 下**，用"路径不含 C:\Windows"筛服务会漏掉它们，必须按名字显式列出（已并入上面 B 段）。
- **`HRWSCCtrl` 是火绒的服务名，进程名是 `wsctrlsvc.exe`**，用服务名去 `Get-Process` 会查不到，别误以为它没运行。
- **按名字筛压缩软件会漏**：`Get-Process -Name 'rar*'` 匹配不到 `WinRAR.exe`（名字以 `Win` 开头），要写 `'WinRAR'` 或 `'*rar*'`；本机压缩软件还装在 `A:\压缩和解压软件` 而不是 `C:\`，按盘符筛同样会漏（同 VMware 那个坑）。
- 已经 `Stopped` 但 `StartType=Auto` 的第三方服务（`AweSunService`、`ToDesk_Service`、`BaiduNetdiskUtility`、`EpicGamesUpdater`、`GoogleUpdater*`、`edgeupdate*`、`Steam Client Service`、`Pluto Service`、`com.docker.service`、`Bonjour Service` 等）本身不占资源，**不要去改启动类型**；它们可能在训练中被定时任务唤醒（对应处理见 D/E/F 段，属持久性改动，需用户同意）。

### D. 提权：暂停 Windows 更新（防训练中途重启）

本机活动时间为 8:00–2:00，即**凌晨 2:00–8:00 在活动时间之外，Windows 可自动安装更新并重启**；跨夜的长训练必然覆盖该时段。开训前暂停更新 5 周（提权执行）：

```powershell
$start = (Get-Date).ToUniversalTime().ToString('yyyy-MM-ddTHH:mm:ssZ')
$end = (Get-Date).ToUniversalTime().AddDays(35).ToString('yyyy-MM-ddTHH:mm:ssZ')
$ux = 'HKLM:\SOFTWARE\Microsoft\WindowsUpdate\UX\Settings'
foreach ($n in 'PauseUpdatesStartTime','PauseFeatureUpdatesStartTime','PauseQualityUpdatesStartTime') { Set-ItemProperty -Path $ux -Name $n -Value $start -Type String -Force }
foreach ($n in 'PauseUpdatesExpiryTime','PauseFeatureUpdatesEndTime','PauseQualityUpdatesEndTime') { Set-ItemProperty -Path $ux -Name $n -Value $end -Type String -Force }
```

复核：`(Get-ItemProperty 'HKLM:\SOFTWARE\Microsoft\WindowsUpdate\UX\Settings').PauseUpdatesExpiryTime` 应为 5 周后的 UTC 时间，设置界面会显示"更新已暂停至 …"。暂停不等于关闭更新，到期自动恢复。

### E. 提权：禁用重型自启项

这些条目现在未必在运行，但**重启后会全部回来**，是长训练最大的隐性开销：

| 自启位置 | 条目 | 开机后会发生什么 |
|---|---|---|
| HKCU `...\CurrentVersion\Run` | `WallpaperEngine` | 动态壁纸，持续占 GPU 渲染 |
| 同上 | `GameViewer`（网易UU远程，`--auto-run`） | 远程控制服务，截屏编码，GPU/CPU 双占 |
| 同上 | `Docker Desktop` | 虚拟化 + 常驻后台 |
| 同上 | `Steam`（`-silent`） | 常驻 |
| 启动文件夹 | `Ollama.lnk` | 本地大模型服务，**会占显存** |

做法：**先整键导出备份**再删除；启动文件夹里的快捷方式只移动、不删除。

```powershell
reg export "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" "$env:USERPROFILE\autostart-backup\HKCU-Run-backup.reg" /y
foreach ($n in 'WallpaperEngine','Steam','GameViewer','Docker Desktop') { Remove-ItemProperty -Path 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Run' -Name $n -Force -ErrorAction SilentlyContinue }
Move-Item "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup\Ollama.lnk" "$env:USERPROFILE\autostart-backup\Startup\" -Force -ErrorAction SilentlyContinue
```

- **`GameViewer` 会自愈自启项，只删 Run 键治标不治本**：2026-09-16 重启后复查，被移除的 `GameViewer` 又出现在 Run 键里（值 `"A:\远程控制软件\网易UU远程\GameViewer\GameViewer.exe" --auto-run`）。原因是 `GameViewerService` 的 `StartType=Automatic`，开机自启后由它把该键写回。要根治得**把 `GameViewerService` 启动类型改成 `Disabled`**（持久性改动，需用户同意），或在 UU 远程软件内关闭"开机自启"。其余三条（`WallpaperEngine`、`Steam`、`Docker Desktop`）重启后没有再回来。

未处理的轻量项：`AMDNoiseSuppression`、`electron.app.WeChatDataAnalysis`、`iTunesHelper`、`Everything`、`vmware-tray`（注册表里有重复两条）、启动文件夹的 `Clash Verge.lnk`——不影响训练，默认保留。

### F. 提权：禁用第三方计划任务

Google / Firefox / Git / NVIDIA / Edge 的更新器，以及 `SoftLanding`、`WiseCleaner` 这类推广任务，会在训练中途唤醒，造成 CPU 与磁盘抖动：

```powershell
$records = @()
foreach ($t in (Get-ScheduledTask | Where-Object { $_.State -ne 'Disabled' -and $_.TaskPath -notmatch '^\\Microsoft\\' -and $_.TaskName -notmatch 'Huorong' })) {
    Disable-ScheduledTask -TaskPath $t.TaskPath -TaskName $t.TaskName | Out-Null
    $records += "$($t.TaskPath)`t$($t.TaskName)"
}
$records | Out-File "$env:USERPROFILE\autostart-backup\disabled_tasks.txt" -Encoding utf8
```

- **必须排除 `\Huorong PopBlock`**：它是火绒的弹窗拦截任务，禁掉就违反了"火绒不动"的要求（2026-09-15 误禁过一次，已恢复）。
- 记录文件按「任务路径 + Tab + 任务名」两列写入，恢复脚本据此逐条启用。

D/E/F 三段做完后，跑第 5 节验证，并把备份目录 `%USERPROFILE%\autostart-backup\` 的路径告诉用户。

## 4. 执行前的安全检查

```powershell
# 1) 是否已提权（False 表示需要走 B 段的 RunAs）
([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)

# 2) 系统代理：停 clash_verge_service 前确认没有在用代理，否则可能自断网络（AI 自己的连接也会断）
Get-ItemProperty 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Internet Settings' | Select-Object ProxyEnable, ProxyServer
Get-NetAdapter | Where-Object { $_.InterfaceDescription -match 'TAP|TUN|WireGuard|Wintun|VPN' }   # 有 TUN 网卡说明流量走代理，先停手
@(Get-Process | Where-Object { $_.ProcessName -match 'clash|mihomo|xray|v2ray|sing-box' }).Count      # 只有 *-service 助手、没有代理核心 = 安全

# 3) 是否有虚拟机在跑：有则不要停 vmms / vmcompute / VMware 服务
@(Get-Process | Where-Object { $_.ProcessName -match 'vmmem|vmware-vmx|vmwp' }).Count

# 4) 训练进程是否已在运行
@(Get-Process | Where-Object { $_.ProcessName -match 'python|yolo|tensorboard' }).Count
```

2026-09-15 实测：`ProxyEnable=0`、无 TUN 网卡、无代理核心、无运行中的虚拟机、python 进程 0 个，四项均通过。

## 5. 验证

```powershell
$os = Get-CimInstance Win32_OperatingSystem
"内存: 可用 $([math]::Round($os.FreePhysicalMemory / 1MB, 2)) GB / 总 $([math]::Round($os.TotalVisibleMemorySize / 1MB, 2)) GB"
nvidia-smi --query-gpu=memory.used,memory.total,utilization.gpu --format=csv,noheader
Get-Service HipsDaemon, HRWSCCtrl | Select-Object Name, Status                  # 必须都是 Running
Get-Service -Name 'Apple Mobile Device Service','MySQL94','SQLWriter','VMAuthdService','VMUSBArbService','VMnetDHCP','VMware NAT Service','WSLService','vmms','vmcompute','GameViewerService','clash_verge_service','ClickToRunSvc','NvContainerLocalSystem','WSearch','DiagTrack' | Where-Object Status -eq 'Running'   # 期望为空
@(Get-Process | Where-Object { $_.ProcessName -match 'python|yolo|tensorboard' }).Count   # 期望 0

# 最后一道：确认停掉 NVIDIA 服务后 GPU 仍可计算（在项目根目录执行）
uv run python -c "import torch; x = torch.zeros(1024, device='cuda'); print(torch.cuda.get_device_name(0), x.sum().item())"   # 期望输出显卡名与 0.0
```

期望结果（2026-09-15 实测）：内存可用 **19.2 GB / 31.15 GB**；GPU **794 MiB / 16303 MiB，0~5% 利用率**；16 个服务全部 Stopped；火绒两个服务 Running；python 进程 0；GPU 张量运算输出 `NVIDIA GeForce RTX 5080 0.0`。

## 6. 恢复

- **首选：重启电脑**。所有服务的 `StartType`（Automatic / Manual）一个字没改，重启后原样恢复。
- 不想重启：管理员 PowerShell 里 `Start-Service <服务名>` 单独拉起，例如要用 VPN 时 `Start-Service clash_verge_service`。
- 被结束的进程（cc-switch、百度网盘助手、Office/华硕更新器）随对应软件下次启动自动回来。
- **自启项、计划任务、更新暂停**：以管理员运行 `%USERPROFILE%\autostart-backup\restore-autostart-and-update.ps1`，它会导入 `HKCU-Run-backup.reg`、把 `Startup\*.lnk` 移回启动文件夹、按 `disabled_tasks.txt` 逐条启用任务、清除 6 个更新时间值。

## 7. 副作用

| 影响 | 说明 |
|---|---|
| 开始菜单 / 文件搜索 | `WSearch` 停止后结果变慢、可能不全，重启恢复 |
| NVIDIA App / 游戏内覆盖 | `NvContainerLocalSystem` 停止后不可用；**`nvidia-smi`、CUDA 计算不受影响**（已实测 `torch.zeros(1024, device='cuda')` 成功执行） |
| VPN | Clash Verge 需要时手动启动服务或重启 |
| 打印 / 锁屏 / 小组件 | 相关 UI 进程被结束后首次调用会稍慢，系统按需重启它们 |
| Office / 华硕更新 | 训练期间不再自动更新 |

## 8. 实测记录（2026-09-15）

| 项目 | 清理前 | 清理后 |
|---|---|---|
| 可用内存 | 18.6 GB / 31.2 GB | 19.2 GB / 31.2 GB |
| GPU 桌面占用 | 981 MiB | 794 MiB |
| 桌面 GPU 利用率 | 1% | 0~5% |
| 被结束进程数 | — | 37 个（用户级 36 + 提权补杀 1） |
| 被停止服务数 | — | 16 个 |
| python / yolo 残留 | 0 | 0 |

第一轮（普通权限）结束时统计"已结束 36 个进程，释放约 2012 MB"；但 `StartMenuExperienceHost`、`SearchHost`、`TextInputHost` 及其 WebView 子进程在几秒内自动重启（约 550 MB），所以净增只有 0.6 GB。

同日追加第二轮（用户确认后执行第 3 节 D/E/F）：暂停 Windows 更新至 2026-10-20；移除 4 条 Run 自启（Wallpaper Engine / Steam / GameViewer / Docker Desktop）、移出 `Ollama.lnk`；禁用 13 个第三方计划任务（Google×4、Mozilla×2、Edge×2、NVIDIA、Git、SoftLanding×2、WiseCleaner）。**这三项不改变当前可用内存**——那些软件本来就没在运行；价值在于重启之后和训练中途不再被抢占。备份与一键恢复脚本在 `%USERPROFILE%\autostart-backup\`。误禁的火绒 `\Huorong PopBlock` 已当场恢复。

随后用户自己新开了一个 Claude 会话（371 MB）并打开任务管理器（167 MB），可用内存回落到 18.74 GB——这两项是用户正在使用，不属于清理对象。复查确认 16 个服务无复活、所有自启的重型软件（Wallpaper Engine、Docker、Ollama、GameViewer、Steam）当前均未运行。

用户随后提出"WinRAR 进程没删掉"，当场定性复查：176 个进程全列无 `WinRAR`/`Rar`/`UnRAR`，服务、计划任务、三处 Run 键、启动文件夹也都没有它，**确认当时并未运行，无可删对象**。本机装的是 rarlab 官方国际版 7.11（`A:\压缩和解压软件`，目录内只有 `WinRAR.exe`/`Rar.exe`/`UnRAR.exe` 等标准文件，无广告或更新模块），本体不常驻；`C:\WINDOWS\Prefetch` 已被清空（0 条记录），无法据此回溯它最后一次运行时间。已据此把 WinRAR 补进 A 段清单与 C 段易漏项。

### 2026-09-16 重启后二次清理

重启会把 A/B 两段的成果全部还原（服务 `StartType` 一个字没改，自启项本就在 E 段处理），但 **D/E/F 三段是持久性配置，重启后依然生效**，复查确认：更新仍暂停至 `2026-10-20T11:45:32Z`；`WallpaperEngine`/`Steam`/`Docker Desktop` 三条自启仍被移除；13 个计划任务**全部仍为 `Disabled`**；`\Huorong PopBlock` 状态 Running、未被动过。

| 项目 | 清理前 | 清理后 |
|---|---|---|
| 可用内存 | 24.42 GB / 31.15 GB | 24.83 GB / 31.15 GB |
| GPU 桌面占用 | 642 MiB / 16303 MiB | 640 MiB / 16303 MiB |
| 进程总数 | 186 | 151 |
| 被结束进程数 | — | 28 个（约 1886 MB） |
| 被停止服务数 | — | 16 个（全部 Stopped，无残留） |

这一轮是**刚开机**做的，桌面 GPU 占用本来就只有 642 MiB，所以 GPU 几乎没变化（不像 2026-09-15 那次能从 981 降到 794）；可用内存也只净增约 0.4 GB，因为被结束的外壳进程（`TextInputHost`、`StartMenuExperienceHost`、`SearchHost`、`msedgewebview2` 等，本次共约 927 MB）在数秒内就自动重建了，与第 9 节结论一致。本次 PyCharm 未启动（上次它一家占 3.45 GB），python/训练进程 0 个。停掉 `NvContainerLocalSystem` 后按第 5 节跑 CUDA 冒烟测试，输出 `CUDA OK: NVIDIA GeForce RTX 5080 0.0`；`nvcontainer` 进程随之退出，`NVDisplay.Container`（显示驱动容器）保留。AI 会话本次跑在 `cmd.exe` 里（父链 `claude.exe → cmd.exe → explorer.exe`），用户自己开的 `WindowsTerminal`（108 MB）已按保留清单跳过。

## 9. 杀了也白杀 / 不值得杀

- **Windows 外壳进程会自启**：`StartMenuExperienceHost`、`SearchHost`、`TextInputHost`、`ShellExperienceHost`、`ShellHost` 和它们的 `msedgewebview2` 子进程，被杀后几秒内自动重建。杀掉只能短暂释放，用户一碰开始菜单或一打字就回来，**不必反复追杀**。
- **火绒**（`HipsDaemon` 等）保留，且 194 MB 不构成负担；要减少它对训练的干扰应该加**目录白名单**而不是关防护（见第 10 节）。
- **`SystemSettings`、`ApplicationFrameHost` 等 UWP 宿主**杀掉后会随用户操作重建，收益是临时的。

## 10. 真正影响训练速度的三件事

清理进程/服务只能拿回约 0.6 GB，下面三条的收益更大：

1. **PyCharm 开 Power Save Mode**（File 菜单）：它占 3.45 GB，索引器和 AI 补全（`full-line-inference`）会在训练时与 DataLoader 抢 CPU 和磁盘；或训练期间直接关掉 IDE。
2. **给火绒加目录白名单**：把 `datasets/`、`数据集/`、`runs/` 加入信任区。首个 epoch 要生成几千个 npy 缓存文件，实时扫描是纯开销。
3. **训练期间关掉 AI 会话**（`claude.exe` 约 420 MB）和不再需要的浏览器/工具窗口。

此外提醒：可用内存虽然涨到 19.2 GB，但五通道全量 RAM 缓存需要约 18 GiB，**`main.py` 的 `cache` 仍应保持 `disk`**，不要因为"内存变多了"改成 `ram`（会紧贴上限触发换页，反而更慢）。


---

<a id="record-3"></a>

## 原文件：docs/比赛任务要求.md

# 比赛任务要求

项目导航更新（2026-09-18，非官方正文）：当前是D-FINE v8五通道候选，datasets为1709/291、全部2000组保留，已有清洗修订沿用；YOLO v4推理路径已恢复，D-FINE使用独立后端。评分按官方mAP50-95记录，未确认任何经验加权公式。新状态见 [知识索引](../项目知识索引.md)。下文官方摘录保留原文，旧项目导航仅作历史。

项目导航（2026-09-16，非官方正文）：实际下载数据中 PNG 深度为 16-bit、JPG 深度为 8-bit RGB；当前新版标注、已知瑕疵与 1744/256 划分见 [数据核验记录](04_数据与调参经验.md#record-1)，v3 配方见 [训练配置复核](04_数据与调参经验.md#record-2)。下文保留官方 PDF 摘录口径，不用项目实测结论改写官方原文。

> 以下内容仅整理自你提供的官方赛题 PDF。已删除此前由我自行添加的解释、建议、执行顺序、基线方案、消融实验建议等内容，仅保留与比赛任务、数据集、模型训练、评估和训练约束直接相关的官方信息。

## 三、赛题任务

本赛题旨在解决复杂城市场景下单一模态感知能力受限的问题，要求参赛者设计并实现一种基于可见光、热红外与深度三模态数据融合的目标检测算法。

### （一）任务描述

参赛队伍需利用赛题提供的空间对齐三模态图像数据，通过构建深度学习模型，有效提取并融合多源异构特征，实现对路口、公园、室内及空中等多场景下关键目标的精准定位与分类。如图1所示。

图1 部分样例展示（第一行为可见光图像、第二行为红外图像、第三行为深度图像）

数据集中总共包含从“0”开始编号的12个类别：行人、船、动物、座椅、标识（包括路牌、标语、标志）、双轮车（包含自行车、双轮电动车）、四轮汽车、球、灯（包含路灯和室内的照明灯）、垃圾箱、无人机、三轮车），分别编号为：0: person, 1: boat, 2: animal, 3: seat, 4: sign, 5: bicycle, 6: car, 7: ball, 8: light, 9: garbage can, 10: uav, 11: tricycle。
### （二）任务输入输出说明

数据集标签说明：目标在图像中的标签为对应目标的矩形边界框，格式为：

```text
[class_id, norm_center_x, norm_center_y, norm_w, norm_h]
```

其中：
- `class_id` 是类别编号，表示该目标属于哪个类别；
- `norm_center_x` 表示目标框中心点在图像宽度方向的位置，经过归一化处理后的值，取值范围为 0~1；
- `norm_center_y` 表示目标框中心点在图像高度方向的位置，经过归一化处理后的值，取值范围为 0~1；
- `norm_w` 是目标框的宽度，占整张图像宽度的比例，归一化到 0~1；
- `norm_h` 是目标框的高度，占整张图像高度的比例，归一化到 0~1。

输入数据：处理赛题提供的空间对齐的三模态图像数据，包括可见光图像（RGB）、红外图像（Infrared）和深度图（Depth）。

核心目标：对图像中的关键物体进行精准定位与分类。

输出要求：算法需输出目标的边界框坐标、置信度及对应的类别标签，格式为：

```text
[class_id, norm_center_x, norm_center_y, norm_w, norm_h, confidence]
```

其中 `confidence` 范围为 `[0,1]`。

## 四、数据集及数据说明

### （一）数据来源

本赛题提供的数据集为自行采集的数据，其中可见光 RGB 图像序列和 Depth 深度图像序列是使用 ZED 双目相机采集；采用 InfiRay LG6122 热成像相机获取红外图像。

### （二）数据规模

本赛题提供的数据集包含训练集、初赛测试集、复赛测试集和半决赛测试集，每张图像都有包含多个目标的空间对齐的三模态图像（RGB、Depth 和 Infrared）。

- 训练集：2,000 组带目标标注框的多模态图像。
- 初赛测试集：1,000 组。
- 复赛测试集：1,000 组。
- 半决赛测试集：1,000 组。

### （三）数据格式

RGB、Infrared 和 Depth 图像为 PNG 格式与 JPG 格式的混合，一个样本的三模态图像格式保持一致。

**RGB：**
- 三通道 8 位无符号整型数据；
- 像素取值范围 `[0,255]`。

**Infrared：**
- 存储为三通道 8 位无符号整型数据；
- 各通道视觉特征高度一致，无实际彩色语义；
- 本质为 3 个单通道热辐射灰度图像堆叠；
- 像素取值范围 `[0,255]`；
- 像素数值越高表示目标温度越高，画面视觉效果越亮。

**Depth：**
- 单通道 16 位无符号整型数据；
- 理论取值范围 `[0,65535]`；
- 单位为毫米；
- 深度相机的可感知距离范围约为 30 厘米到 20 米，即取值范围约 `[0,19999]`；
- 像素值为 0 或者过小代表无效深度区域；
- 数值越大表示目标距离相机越远，数值越小表示目标距离相机越近。

所有图像均未经过归一化处理。

每组图像中的所有物体标签保存在对应 TXT 文件中，每一行记录目标的类别、位置和尺寸。

## 五、算法设计要求

### （一）算法类型

本赛题对算法类型不做严格限制，鼓励参赛者采用深度学习的视觉目标检测识别算法，如基于卷积神经网络或 Transformer 的视觉目标检测网络，也可以结合其他机器学习算法。

### （二）创新性

本赛题鼓励参赛队伍提出创新算法框架或者在现有算法基础上做出创新改进，以提高复杂场景下基于多源异构数据的视觉目标检测识别算法性能。

例如，设计新的多源异构数据信息融合方法和视觉目标检测识别网络架构提升性能。

## 六、性能指标要求

本赛题使用经典目标检测性能指标 `mAP@50-95` 评估提交方法的有效性。

`mAP@50-95` 衡量模型在 IoU 阈值从 0.50 到 0.95、步长为 0.05 的 10 个不同阈值下计算的 Average Precision（AP）的平均值，并对所有类别取平均。

IoU thresholds：

```text
T = {0.50, 0.55, 0.60, 0.65, 0.70, 0.75, 0.80, 0.85, 0.90, 0.95}
```

对于每个类别和每个 IoU 阈值：
1. 将该类别下所有测试图像的预测框按置信度从高到低排序。
2. 若预测框与某个尚未匹配的真实框满足 `IoU >= t`，判定为 TP，并将该真实框标记为已匹配。
3. 若 `IoU < t`，或对应真实框已被更高置信度预测框匹配，则判定为 FP。
4. 遍历结束后，未被任何预测框匹配的真实框记为 FN。

本赛事采用 101 点插值法计算 AP：在召回率区间均匀选取 101 个采样点 `(0.00, 0.01, ..., 1)`，最终 AP 为 101 个插值点的算术平均值。

## 七、功能要求

### （一）准确性

算法模型实现面向多源异构数据的视觉目标检测识别时，需要具备较高的准确性，确保在较为复杂的场景下也能准确识别目标。

### （二）鲁棒性

面对不同质量的 RGB 可见光图像、Depth 深度图像和 Infrared 红外图像数据，算法应能稳定运行，输出可靠的检测识别结果。

在一个或者多个模态数据成像质量较差时，算法也不应出现大幅性能波动，保持对目标检测的准确性和稳定性。

## 八、开发环境

### （一）软件环境

**编程语言：**

本赛题不强制编程语言的使用，但推荐使用 Python 3.8 及以上版本。

**深度学习框架：**

推荐使用 PyTorch。

**计算资源：**

本赛题不对参赛队伍使用的计算资源做限制，亦不提供算力支持。参赛者可使用本地工作站或云端计算平台进行开发和训练。

### （二）硬件环境

本赛题对 CPU 型号、内存大小、GPU 型号等硬件资源不做强制性要求，参赛队伍可根据自身需求自由配置。

## 九、成绩评价

### （一）输入数据格式要求

参赛者算法应能正确读取赛题数据，包括 RGB 图像、Infrared 图像与 Depth 图像，以及目标矩形边界框：

```text
[class_id, norm_center_x, norm_center_y, norm_w, norm_h]
```

的 TXT 文件。

### （二）输出数据格式要求

算法对每个图像数据进行目标检测识别，要求每张测试图必须提交一个同名的预测 TXT 文件，文件中每一行表示一个目标在图像中的位置、置信度和类别信息：

```text
[class_id, norm_center_x, norm_center_y, norm_w, norm_h, confidence]
```

要求：
- 若某张图未检测到目标，也须提交一个空 TXT 预测文件，不允许缺失；
- 每张图最大预测框数量为 100，超过数量会按置信度截断；
- 出现非法类别、非法坐标或置信度缺失，将导致该预测无效，不参与结果运算。

### （三）成绩计算

成绩根据算法输出结果与真实标注数据对比，依据性能评估指标 `mAP@50-95` 进行打分。

若参赛者提交结果低于赛事方公布的基线成绩，赛事方有权将其认定为无效成绩。

## 十、解题思路

### （一）数据预处理

在数据训练过程中可以进行一些数据增强的预处理操作，例如对 RGB 图像、Infrared 图像和 Depth 图像进行归一化操作、进行图像旋转、缩放、翻转、裁剪等操作扩充训练数据集，增强模型的泛化能力。

### （二）特征提取

参赛队伍应选取合适的深度学习网络结构提取 RGB 图像、Infrared 图像和 Depth 图像的特征，通过训练并结合机器学习方法如特征选择等实现高判别性和高鲁棒性的数据特征表示。

### （三）模型训练

参赛队伍可以选择合适的深度学习框架（如 PyTorch）搭建模型，设置合理的训练参数，如学习率、迭代次数、批量大小等。

在训练过程中，可自行划分验证集对模型进行评估和调优，防止模型过拟合。

## 十一、赛题约束条件

### （一）算法约束

- 禁止调用任何在线服务或 API，所有训练与推理须可在本地离线完成。
- 禁止采用手工标注测试集目标位置的方式生成结果，所有预测结果必须由算法模型自主推理输出，严禁任何形式的人工干预测试结果生成过程。
- 禁止将多个不同结构或训练阶段的模型进行简单集成，直接采用投票法、平均法等方式决策。

### （二）数据使用约束

- 参赛者仅可使用赛事官方提供的训练数据集，严禁私自扩充外部数据。
- 允许使用 ImageNet、COCO、Objects365 等公开预训练权重。
- 参赛者仅可使用赛事官方统一提供的初赛、复赛、半决赛测试集数据进行算法预测，严禁私自对测试集进行标注、篡改，严禁将测试集数据用于模型训练环节。
- 赛事提供的所有测试数据集均为赛事专用，禁止将数据集以任何形式泄露、传播或用于非本次赛事相关用途。


---

<a id="record-4"></a>

## 原文件：docs/代码结构与运行.md

# 代码结构与运行

2026-09-22 v19实施：根目录仍只有main.py训练和predict.py预测入口。aic/data.py新增从原始深度计算支持及支持加权插值；aic/model.py新增QualityLetterBox、P3质量融合模型；aic/training.py沿用原生增强布局/框变换并接入六通道派生输入。predict.py按新检查点签名选择同一处理，旧模型预处理不变。无新增执行脚本、无数据修改，无测试或模型任务；用户运行`uv run python main.py`启动v19，实际可运行性及收益未验证。详见[v19接口与命令](03_v15至v19实验.md#record-8)。下方“main仍v18/代码未改”是上一阶段。

2026-09-22 当前：main.py保留已完成v18，55轮best15轮AP95=0.37521，未改善，不建议再次启动；predict.py用户默认未改。下一版质量感知融合尚为设计，本轮没有修改任何训练/预测源码、入口或数据。模态读数代码与摘要仅留本地runs/modality_audit/v18_20260922，不新增根目录py或自动检查。执行的是CPU文件读数，不是测试或模型评估；见[v18审计](03_v15至v19实验.md#record-2)。

2026-09-21 更新：根目录仍仅main.py/predict.py。按本轮授权新增tools/validate_local.py（显式评估当前291张val）与tests/test_training_consistency.py（10项回归），无后台自动测试。内容裁框共用，最佳轮图在best_validation；原生polish_*为None不改增强、数值才实际启用收尾。详见[v14执行记录](02_v9至v14复盘.md#record-7)。

2026-09-20 最新v14：入口仍仅main.py/predict.py，无新增测试脚本。aic/model.py增加共用原生ceil缩放与五通道114补边；predict.py按权重几何选择，不混用固定矩形辅助通道0补边。aic/training.py支持早期融合骨干降学习率与screening_history.csv；预测默认仍是已有v13，训练默认v14。未执行测试、静态执行检查或模型任务，详见[v13复盘与v14阶段筛选](02_v9至v14复盘.md#record-6)。

2026-09-20 当前入口为v11：根目录仍仅使用`main.py`训练和`predict.py`预测；`aic/training.py`在既有轮末验证中检查第10/20轮历史最佳AP95，不增加独立评估或额外前向。预测默认v11权重和`predict_v11`，历史v4/v9/v10仍可显式指定。未执行测试、训练或推理，详见[v10复盘与v11筛选方案](02_v9至v14复盘.md#record-3)。

2026-09-20 同日后续：v10改为全尺寸早期融合，训练/验证/预测共用1920×1080内容及1920×1088张量；根目录仍仅main.py/predict.py。删除本轮1280草案新增但现已不用的原生增强适配类，复用RectangularDataset并增加独立的收尾尺度/位移配置。预测FusionPredictor同时识别v9门控和v10首层模型，实际结构版本写入元数据。无测试、模型执行或历史产物删除。

2026-09-20：入口布局不变，main.py当前选择v10五通道早期融合（1280），aic中保留历史v9模型类以便预测；predict.py默认v10 best、v4原生YOLO路径，输出predict且不覆盖已有目录。本轮未执行测试或模型任务，不能用此前v9冒烟结果证明v10通过。详见[v9复盘与v10训练方案](02_v9至v14复盘.md#record-2)。

2026-09-19：按用户要求减少根目录Python文件，移除中转层；本轮不更换模型，不调整参数，不改数据或权重。

## 日常只用两个入口

```text
main.py                 训练参数与启动
predict.py              预测参数、推理、结果保存、提交ZIP打包
aic/
  __init__.py           包声明，不启动任务
  data.py               三模态读图、类别定义、文件指纹
  model.py              模型结构、共用几何变换与NMS
  training.py           当前训练器、数据集、正式轮末验证
tools/
  __init__.py           工具包声明
  prepare_dataset.py    有记录的数据审计与清洗
```

训练运行 `uv run python main.py`；预测及打包运行 `uv run python predict.py`。
2026-09-19已修正main.py的imgsz宽高索引（改为第一项1920），输出目录改为相对入口解析的绝对runs/detect以避免全局设置拼接。完整依据见[v9融合训练实施](02_v9至v14复盘.md#record-1)。未进行运行验证，既有输出目录保留。
预测参数仍在文件底部的 `predict(PredictionConfig(...))` 分节配置，权重/输入/输出常量在文件顶部；命令行覆盖方式不变。
数据准备仅在明确需要时运行 `uv run python -m tools.prepare_dataset`，它不会随训练或预测自动执行。

## 合并与删除

- 三模态预测.py、FusionPredictor及DFinePredictor合并至predict.py。D-FINE的模型构建、旧版预处理和查询排序一并迁入，保留旧权重预测，不保留旧训练循环。
- 三模态融合.py中的可序列化模型及共用几何/NMS归入aic/model.py。
- 三模态训练.py中的读图归入aic/data.py；数据集及仍需要的加载器/学习率逻辑归入aic/training.py。
- 融合训练.py归入aic/training.py；FusionDetectionTrainer直接继承框架DetectionTrainer，不再经过旧MultimodalDetectionTrainer中转。旧ScheduledMultimodalDetectionTrainer等无当前用途的训练实现移除。
- 准备三模态数据集.py移至tools/prepare_dataset.py，类别和文件指纹抽出为共享数据组件，预测不再依赖清洗脚本。
- 旧v5训练优化.py已删除。以上旧文件均可从Git提交08a4180恢复，没有删除历史数据或权重。

保留的模型、数据集与训练器子类是框架扩展点，不是额外运行入口。预测不导入训练器或清洗工具；训练和预测共用同一份读图及几何函数，避免两份实现逐渐不一致。

## 兼容与提交

数据清洗没有被删除或自动重做：仍使用已审计的1709/291和57个标签文件修订。额外约200张人工复核仍待执行，不能把文件名/哈希校验当作漏标、错标和模态配准审阅。

aic.model加载时为旧模块名三模态融合注册别名，供已有v9检查点反序列化；新检查点使用aic.model。旧YOLO与D-FINE后端继续通过predict.py选择，尺寸与类别校验不变。D-FINE仍需本地vendor/D-FINE固定版本源码，不会联网下载。

源码路径与指纹已变，新训练按新结构留代码快照。重组前的中断训练仍须使用原运行code快照或对应Git版本恢复，禁止去掉签名校验强行续训。旧已完成权重用于预测不校验训练源码签名；此次未实际加载权重验证兼容。

预测结束仍输出images、labels和比赛提交内容/submission.zip；已有输出不覆盖。初赛预测提交物仍为该ZIP；如果赛事另要求提交可运行源码，必须同时提供predict.py、aic所需共享模块、依赖以及相应模型资源，不能把predict.py当作无依赖单文件程序。不得把赛事数据公开上传GitHub。

本轮仅阅读代码、引用与Git差异；未运行pytest、AST/compile/import、训练、推理或独立评估，不宣称已经验证运行正确。


---

<a id="record-5"></a>

## 原文件：docs/预测与赛事提交.md

# 三模态预测与初赛提交

2026-09-22 初赛反馈补充：截图显示v5标为multi-label=True的提交50.8550，False为51.0830；差0.2280，但当时权重和其他配置没有逐项复核，不能当作严格单变量实验，也没有依据默认开启多标签。v6两份候选55.1490/55.1360、v7两份均52.4010。完整16条记录见[初赛台账](../初赛提交成绩台账.md)。不根据作品名称更改D-FINE后处理实现；本轮预测参数不变，未重新推理。

2026-09-22 v19兼容：predict.py用户默认权重/source/output不改。v19权重按结构及预处理版本读取RGB3+IR1+Depth1+派生支持比例，深度支持加权缩放、辅助填0，与训练验证共用实现；缺失版本元数据则拒绝预测。旧v4–v18保持历史五通道补边，不能因新方案更换旧权重输入。仍为FP32、单模型、单标签NMS与每图100框，支持掩码不写入提交TXT；结果不覆盖。本轮未执行预测。v19显式复赛命令见[实施记录](03_v15至v19实验.md#record-8)，只有用户训练后才会存在对应权重。

2026-09-21 更新：复赛暂不可评分，本轮仅对两张验证图完成预测及ZIP闭环，未重新推理官方1000图测试集。训练验证与fusion预测统一内容裁框，元数据记录fp32_single_label_content_clip_v2。predict.py默认v13保留；使用复赛数据前须按实际路径与数量配置，不能把初赛1000组当复赛事实。详见[v14执行记录](02_v9至v14复盘.md#record-7)。

2026-09-20 最新：默认v13 best、1280×1280、输出predict_v13_preprocess_fix。修正v12/v13原生验证与预测的IR/Depth补边114/0差异，按检查点geometry区分native_square和fixed_rect，前者共享ceil缩放/原生114补边；旧v4和D-FINE不改。先运行`uv run python predict.py`可生成新的v13包，无需重新训练，不能预言涨分。v14训练完成后需显式传其--weights及--output，默认不会自动切换。未运行测试或预测，详见[v13复盘与v14阶段筛选](02_v9至v14复盘.md#record-6)。下方默认值均属历史。

2026-09-20 当前默认改为v11 `best.pt`、1920×1080内容和`predict_v11`输出；推理几何、FP32单标签NMS及100框上限与v10相同，本轮不通过阈值修改掩盖训练回退。v11尚未训练，运行预测前须确认实际权重目录。未执行测试或推理。

2026-09-20 同日后续：当前v10改为1920×1080内容、1920×1088张量；默认imgsz=1920/height=1080，FP32批量4，自动检测EarlyFusionDetectionModel并走训练共用几何与NMS。检查点early_rect_v10_2与内容宽高必须匹配，prediction.json记录实际结构和版本；不受仅供历史YOLO的yolo_profile影响。下方1280/v4路径描述为已替代草案；旧v4仍可显式1280预测。无测试或额外推理，输出predict不变。

2026-09-20 当前默认：v10早期融合best.pt、1280、FP32、yolo_profile=v4原生逐图矩形预测。用户OUTPUT_PATH=predict保留，已有输出不覆盖；建议新训练后传`--output predict_v10`并确认权重实际目录。height仅供历史v9固定矩形后端使用，v10忽略。v9本地predict_v9/prediction.json的EMA第63轮和SHA256与best匹配，用户线上53.4750；未发现漏模态或输出PNG压缩改变输入的证据，但没有复评排除全部差异。没有新增测试/推理。详见[v9复盘与v10训练方案](02_v9至v14复盘.md#record-2)。

代码收敛（2026-09-19）：所有预测专用实现（含FusionPredictor、DFinePredictor）、参数配置、结果保存和ZIP打包均在predict.py，不再有三模态预测.py。旧D-FINE只迁移模型构建、预处理、原生排序和加载逻辑，未改变候选筛选。共享读图及模型/几何函数复用aic.data与aic.model；预测不导入aic.training或清洗工具。旧v9模块路径有反序列化兼容别名。当前代码提交复现需包含这些共享组件，不能单独拷贝predict.py到空目录运行。下方模块名是历史记录，详见[代码结构与运行](../代码结构与运行.md)。没有执行测试或推理。

最新v9实现（2026-09-18）：用户批准新融合训练及同步矩形预测。predict.py默认改为`AIC_RGBIRDepth_yolo26l_1920x1080_v9_fusion/weights/best.pt`、imgsz=1920、height=1080、FP32、batch=4、预取2批，结果输出到独占`predict_v9`。训练尚未由本轮执行，须先完成训练并核对实际目录。下文v8/v7默认值均为历史。完整参数见 [v9融合训练实施](02_v9至v14复盘.md#record-1)。

新融合模型自动识别为fusion后端：与训练验证共用等比矩形补边到1920×1088、五通道归一化、单标签NMS和内容区裁框，元数据记录内容/张量尺寸与权重轮数。`yolo_profile`只控制历史YOLO，不控制v9融合，也不控制D-FINE。旧模型仍需原训练尺寸；不能用D-FINE权重套用新尺寸和新分支。

晚间入口更新：用户已将默认改为v8 best_map50.pth、1280，当前predict/prediction.json记录第27轮EMA、1000图像、FP32。v8本次提交得53.5740；v7 best_map50.pth已确认52.4010。下方默认v7或待评分文字为早前阶段。本轮只更新过时注释，保留用户权重和资源选择；详见 [最新复盘](01_早期训练与清洗.md#record-6)。

入口快照更新（2026-09-18）：用户当前默认权重为v7的weights/best_map50.pth，imgsz=1536、batch=18、读取/保存各8线程、预取10批；这只是用户选定的候选，不代表已经获得更高分数。下方显式v7 best.pth命令保留为历史提交模型用法，v8需要显式1280和实际新权重。知识入口见 [项目知识索引](../项目知识索引.md)。

## 恢复 v4 YOLO 推理路径（2026-09-18）

已从 Git 历史提交 `86411da` 找回 v4 阶段沿用的五通道预测实现：原尺寸融合数组、逐张预测、rect=True、框架原生单标签后处理、nms=True、FP32、conf=0.001、iou=0.7、max_det=100、无增强。历史提交中的默认权重名称仍为v3，恢复依据是推理实现与参数，不声称该提交就是55.6000提交包的不可变源码快照。

当前 `predict.py` 保留 `V4MultimodalDetectionPredictor`，由 `--yolo-profile v4` 选用，不再走现有批量自定义NMS。配对、五通道预处理、原图坐标还原和六列标签写出仍共用相同逻辑。读取和保存继续并行，模型逐张前向；prediction.json记录配方、来源提交及实际模型批次。

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v4_clean_lr1e4/weights/best.pt --imgsz 1280 --yolo-profile v4 --output predict_v4_restored
```

YOLO默认采用v4路径。若需现有批量自定义后处理，显式传 `--yolo-profile current`；可选多标签也只在current路径使用。已有结果目录不覆盖。

**v7是D-FINE，不能直接加载到YOLO预测器或使用YOLO一对多检测头。** D-FINE仍使用其训练一致的五通道、1536输入和原生查询排序，忽略yolo_profile，不把NMS阈值当成有效调参项。预测v7仍用用户入口或显式指定：

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_dfine_l_1536_v7/weights/best.pth --imgsz 1536 --output predict_v7_native
```

代码读取没有发现当前D-FINE遗漏模态、图片保存压缩改变模型输入或训练/推理尺寸不一致。未运行推理或模型测试，故恢复路径不是已确认的提分修复，也不能据此确认v7比v4线上更强。v8训练配置不受本次恢复影响。

## v8 使用提示（2026-09-18 最新）

训练入口已为v8，`predict.py` 保留用户v7默认权重、1536及资源参数，不自动改成尚未产生的v8权重。v8完成后执行：

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_dfine_l_1280_v8/weights/best.pth --imgsz 1280 --batch 18 --output predict_v8
```

使用实际运行目录；已有预测目录不覆盖。保留五通道、FP32、conf=0.001、max_det=100的训练同口径处理，不引入多模型集成。v8数据与配置见 [v8训练方案与数据清洗](01_早期训练与清洗.md#record-7)。以下默认参数描述为此前阶段记录，当前入口以predict.py为准。本轮未执行推理或测试。

## 批量预测与并行读写（2026-09-18）

当前 `predict.py` 已整理为与 `main.py` 相同的 `predict(PredictionConfig(...))` 分节入口；当时拆分在 `三模态预测.py` 与 `D细化训练.py`，现已统一合并回 `predict.py`。用户选择的 v6 `best.pth` 路径保持不变，不修改训练参数或已有权重。

源码确认原流程为 batch=1，逐张读取三模态、推理、画框、编码、写盘。新流程采用读取线程提前准备输入、主线程整批 GPU 推理、保存线程后台画框写盘；尚未实测各阶段耗时，不能将源码判断当作性能测量结果。

| 入口参数 | 默认值 | 作用 |
|---|---|---|
| batch | 8 | 每批最多 8 组五通道图像，尾批不丢弃 |
| workers / save_workers | 4 / 4 | 读取融合缩放与绘图保存分别并行 |
| prefetch_batches | 3 | 预读窗口最多 24 组，保存队列也最多 24 组；不累计整套解码图 |
| pin_memory | True | D-FINE 当前批次锁页传输，CPU/YOLO 路径不使用此开关 |
| imgsz / 精度 | 1280 / FP32 | 不缩小分辨率，不新增半精度、TF32 加速设置或 INT8 量化 |
| conf / max_det | 0.001 / 100 | 保留原有提交候选口径，不人为抬高弱类分数 |
| png_compression | 1 | PNG 无损低级别压缩，降低编码开销，允许更大图片文件；JPG 不变 |

D-FINE 在 CPU 读取线程中沿用原有等比填充，然后以 uint8 NCHW 批量传输，在 GPU 转为 FP32 并除以 255；单张输入传输量是原先 CPU 浮点输入的四分之一。坐标恢复、EMA、查询类别排序不变。YOLO 按相同原图尺寸分组，保留 `rect=True` 的填充方式；尺寸混杂时实际批次可能小于 8。YOLO 离线 NMS 不设置实时超时截断，避免批次后部图片被跳过。

保存线程只接收 CPU 检测结果，不保留 GPU 张量。全部图片和标签保存成功后才生成正式 ZIP；任何线程异常都会阻止打包，已有目录仍不覆盖。运行结束记录包含读取、推理、绘图、保存的累计秒数与组/秒，以及实际批次大小分布；该时间不包含模型加载与 ZIP 打包。

```powershell
uv run python predict.py --output predict_v6_fast
```

资源由用户根据正式运行情况调整，例如 `--batch 12 --output predict_v6_b12`。没有自动显存探测或隐式减批；显存不足时保留已写结果并提示降低 batch、换新输出目录。默认 8 是未经本机峰值显存实测的配置，不承诺显存占满或固定加速倍数。批量计算可能有浮点微小差异，不保证预测文件逐字节相同，也不承诺提高比赛分数。

本次实施步骤为入口配置整理、两个后端批量化、并行读写与文档同步。仅核对代码引用、实际差异和静态语法；没有新增测试脚本，也没有执行测试、测速、模型推理或训练。

## D-FINE v6（2026-09-18）

predict.py 保留用户当前选择的 v6 `best.pth` 默认权重路径。`.pt` 自动选择 YOLO，`.pth` 自动选择本项目 D-FINE；也可用 `--backend` 明确指定。官方 RGB 预训练权重不能直接用于比赛预测。

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_dfine_l_1280_v6/weights/best.pth --output predict_v6
```

D-FINE 使用 FP32、与训练相同的正方形等比填充、0–1 五通道输入和单模型 EMA。必须使用权重记录的 imgsz；自动减去填充并恢复原图坐标。原生查询×类别候选排序后保留最多 100 框，不调用 YOLO NMS；`--iou` 不起作用，`--multi-label` 是 YOLO 专用开关，对 D-FINE 显式报错。预测 JSON 记录实际后端、轮次、源码版本、NMS 关闭等信息，不沿用 YOLO 的元数据描述。

输出仍为 images、labels、比赛提交内容三个子目录，提交 ZIP 内只有六列 TXT。已有目录不覆盖，不自动上传。此处不推断 v6 线上成绩，本次仅优化预测代码。

v5 best.pt 的已确认线上成绩为 51.0830，实际 conf=0.001、1280、FP32、单标签 NMS；不能将 0.25 图片显示阈值误认为提交阈值。以下保留历史 YOLO 使用说明。

## v5_full 提交开关（2026-09-17）

当前训练输出为 `runs/detect/AIC_RGBIRDepth_yolo26l_1280_v5_full`。预测默认权重路径与 conf/iou 继续由用户维护，本次不替换用户设置。推荐显式指定实际训练输出的单个权重：

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v5_full/weights/best.pt --output predict_v5
```

新增 `--multi-label` / `--no-multi-label`，默认关闭：在同一个模型的一对多输出上，允许同一位置保留多个超过阈值的类别，再做按类别 NMS 和每图 100 框限制。不是多模型投票、分数平均或人工抬高 ball 分数。该开关由自定义预测器接收，不作为不存在的 Ultralytics train/predict 配置传入。

可选候选包：

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v5_full/weights/best.pt --multi-label --output predict_v5_multilabel
```

它可能保留被最高类别覆盖的正确候选，也可能增加误检；不能承诺提分。训练内验证仍是单标签口径，多标签提交不应冒称与该本地指标完全一致。`prediction.json` 记录开关。更大输入尺寸仍可用既有 `--imgsz` 指定，不默认改变，不与多标签同时改动来归因效果。上述命令本轮均未执行，未额外读取测试图或运行推理。

以下按时间保留旧运行说明，当前权重路径以代码或显式 --weights 为准。

最新线上反馈：用户报告 v4=55.6000、epoch50.pt=53.2540，因此下方 epoch50 命令仅保留为历史候选，不再推荐替换 v4。predict.py 的 MODEL_PATH 由用户手动维护，建议通过 --weights 显式指定，本文历史默认路径不覆盖实际代码。v5 已在训练入口实现单标签验证与最小填充，对齐当前预测；未改变用户的置信度或默认权重。v5 训练完成后使用实际目录的 best.pt，见 [v5方案](01_早期训练与清洗.md#record-2)。

## 运行

在项目根目录执行：

```powershell
uv run python predict.py
```

默认读取官方 `数据集/测试集/AIC2026_PHASE_1_1000/` 中的 `visible`、`infrared`、`depth`，检查同名配对及 1000 组总数。推理只使用本地权重，不下载、不上传、不训练，不修改官方图像或标注。

历史 v4 默认权重为 `runs/detect/AIC_RGBIRDepth_yolo26l_1280_v4_clean_lr1e4/weights/best.pt`，当前默认以文首及入口为准。v4 的 CSV 最佳 mAP50-95=0.40370（第 100 轮），同轮 mAP50=0.58625；它不是第 42 轮 mAP50=0.60632 的峰值权重，第 42 轮没有单独留存。

按用户关注的 mAP50，现有 `epoch50.pt` 是候选：检查点元数据确认其实际完成第 51 轮，mAP50=0.59183、mAP50-95=0.37457。该信息来自已有记录，未重新推理，不保证线上更好。保留 best.pt，不做检查点集成；不得使用未微调的三通道 COCO 权重提交。

显式更换权重和输出目录：

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v4_clean_lr1e4/weights/epoch50.pt --output predict_v4_epoch51
uv run python predict.py --help
```

相对路径以 `predict.py` 所在目录为基准。更换比赛阶段时，同时指定 `--source` 和该阶段正确的 `--expected-count`。默认设备为 `0`，无 CUDA 的功能检查可用 `--device cpu`。

## 结果和提交

```text
predict/
├── images/                  # 原图尺寸的带框 RGB 可视化，同名、同扩展名
├── labels/                  # 每张测试图对应一个同名六列 TXT
├── 比赛提交内容/
│   └── submission.zip       # 初赛只提交这个文件
└── prediction.json          # 本地追溯信息，不放进 ZIP
```

根目录只有三个子文件夹；`prediction.json` 是文件，记录模型 SHA256、运行名、框架版本、样本数和推理参数。

TXT 每行严格为：

```text
class_id norm_center_x norm_center_y norm_w norm_h confidence
```

- 类别沿用赛事顺序 0～11，不重新映射；ball=7。
- 坐标去除缩放和填充后，按原图宽高归一化；不是按 1280 或填充后尺寸归一化。
- 按置信度降序，每图最多 100 个框；无检测生成同名空 TXT。
- ZIP 根目录直接放 TXT，不包含 labels 父目录、图片、权重或 JSON。
- 初赛官方规则要求提交预测结果压缩包；复赛等阶段额外材料需另按当阶段要求准备。

## 历史 YOLO 单张预测配置及依据（当前批量配置见文首）

| 配置 | 当前设置 | 依据 |
|---|---|---|
| 五通道融合 | RGB + IR + Depth | 直接复用 `三模态训练.py`，uint16 深度仍按 20000 mm 截断映射到 0～255 |
| imgsz | 1280 | 与训练验证分辨率一致；没有验证证据时不盲目放大 |
| rect | True | 单张最小矩形填充，保持纵横比，减少空白像素 |
| conf | 0.001 | 与验证候选阈值一致，为 AP 评测保留低分候选 |
| iou | 0.7 | NMS 去重阈值，与比赛的 AP IoU 阈值不同 |
| nms | True | 当前框架显式选择一对多头，与训练验证 nms=None 一致 |
| max_det | 100 | 官方单图预测框上限 |
| quantize | 32 | FP32 推理基线；不沿用训练专用 amp 参数 |
| batch | 1 | 逐张融合与推理，不在内存累计 1000 张五通道图 |
| augment | False | 不引入未经验证的 TTA，不做多模型集成 |
| visual-conf | 0.25 | 仅减少可视化遮挡，不删掉 TXT 中的低分候选 |

因此，图片上的框可能比同名 TXT 行数少，这是两个置信度阈值分工，不是标签错配。

参数基于本项目训练和官方格式设置，并非已验证的全局最优组合。参数和模型比较应使用训练集划分出的验证集，不能人工修改测试集预测来提分。预测脚本的正确性不等于能够保证比赛名次。

## 防覆盖与失败处理

### 后续候选：对齐多标签后处理

安装版本训练验证使用 multi_label=True，当前预测继承默认单标签后处理。可考虑自定义预测器保留同一候选框的多个类别，再执行分类别 NMS；不能向 model.predict 直接增加未支持的 multi_label 配置。此项尚未实施，可能同时增加正确候选和误检，并改变每图前 100 框分配。置信度由用户自行调整，默认仍为 0.001；可视化 F1 最优阈值不能直接当作提交 AP 最优阈值。

已有输出根目录即报错，空目录也不复用；不删除、不追加、不自动混合两次预测。需要重新预测时使用新的 `--output`，例如 `predict_v3`。默认 `predict/`、`predict_*/` 产物被 Git 忽略；使用其他自定义输出名时应补充忽略规则，避免提交运行信息或赛事数据。

任何模态缺失、尺寸不一致、图像损坏、类别不符或非法标签都会中止；不会跳过图片生成表面完整的提交包。中断时已经生成的结果保留，但只有全量预测与逐文件格式检查完成后，才会出现正式 `submission.zip`。打包中断可能留下 `.zip.partial`，它不能提交。

不要在预测进行中修改输入或输出文件。若目录被外部改动导致核验失败，应保留现场，使用新的输出目录重跑，而不是手工补测试集标签。

## 验证

```powershell
uv run pytest tests/
```

`tests/test_predict.py` 覆盖配对、同名冲突、防覆盖、原图坐标、五通道顺序、空标签、低分保留、非法标签拒绝、ZIP 根目录结构，以及小型五通道网络 CPU 全链路。

另已用正式旧运行 best.pt 在 CPU、1280 下分别对一组 1920×1080 PNG 和一组 640×360 JPG 三模态样本完成预测、图片保存和打包检查。烟雾结果仅在系统临时目录，只有两张，不能当作正式初赛提交。未自动运行完整 1000 张测试集，也未向赛事平台上传文件。


---

<a id="record-6"></a>

## 原文件：docs/项目知识索引.md

# 项目知识索引

2026-09-22 当前入口：[v19复盘与v20模态诊断](../v19复盘与v20模态诊断.md)。v19完成75轮、best62 AP95=0.3672142710，未超过v14；main.py已配置v20 RGB诊断，保留v14条件与200轮日程、单独20轮预算后停止。只用于查明模态贡献，非赛事提交模型；数据和predict.py不改，未测试或开训，未提交推送。下方v19未运行为历史状态。

2026-09-22 成绩证据：[初赛提交成绩台账](../初赛提交成绩台账.md)收录用户截图16条DONE记录及打分时间；[历次训练与赛事分数对比](../历次训练与赛事分数对比.md)同步补充v5多标签50.8550。最高v4=55.6000，53.1270/54.0500版本待确认，复赛成绩仍未知。本次归档v19代码及成绩文档，不调整配方、不执行测试或模型任务。

2026-09-22 当前：[v19质量感知局部融合实施](03_v15至v19实验.md#record-8)。已获用户批准并写入main.py/aic/predict.py，尚未运行。RGB预训练主干、IR复制浅层与P3局部对应、Depth支持加权几何及门控；1280/batch2/nbs16、200轮、原清洗1709/291不变。训练和预测共用新版本六通道张量契约，旧模型五通道路径保留；用户预测默认不改。本轮未测试、语法执行检查或模型任务，不承诺0.60。以下main仍v18、设计待审核均为历史。

2026-09-22 最新：[v18完成复盘与三模态数值审计](03_v15至v19实验.md#record-2)。v18共55轮、best15轮AP95=0.3752101829，未改善，保留v14近期同协议候选。已读取训练1709组IR/Depth，PNG/JPG深度采样零值27.65%/80.92%，5张JPG完整像素全零；20米量程有官方依据，采样没有超上限值。新增4组RGB/IR目视只提供局部残差证据，不代表全量配准审核。代码/数据不改，无测试或模型任务。[下一融合设计草案](03_v15至v19实验.md#record-9)待审核，不是已实现v19；main.py仍为已完成v18，不要重复开训。以下尚未训练等均是历史状态。

2026-09-21 最新：[v18分模态首层学习率](03_v15至v19实验.md#record-6)。v17已完成55轮、best15轮AP95=0.3851431884，未超过v14；关闭受限重复。v18维持原数据、1280与v14训练日程，独立提高IR/Depth首层学习率至5e-4，RGB首层1e-4、骨干2e-5。预测识别新结构，旧模型兼容路径保留，用户默认未改。仅读源码和差异，未测试/训练/推理，由用户开训；以下“v17未运行”属于历史。

2026-09-21 最新入口：[v17受限长尾采样与v4参数取舍](03_v15至v19实验.md#record-5)。全12类参与含图频率计算，常见类不主动加权；受限额外采样、固定轮长、逐轮主图/增强后框曝光记录已实现，恢复v14的200轮日程。保留原清洗及预测实现，由用户开训，本轮未测试或运行模型。v16已完成60轮，最佳36轮AP95=0.36766，不推广；以下“v16尚未运行”等为历史交付时状态。

2026-09-21 当前训练入口：[v16短日程计划](03_v15至v19实验.md#record-4)已按用户确认写入main.py，尚未运行。保留清洗1709/291、v14五通道及分层学习率，从官方基底重迁移；60轮衰减，第21轮关Mosaic，其余几何增强不变。用户已用v4提交复赛，未知分数不填；预测v16显式指定复赛source，predict.py默认不动。本轮未新增测试、模型执行或数据修订，未提交推送。

2026-09-21 第二批只读审阅：已完成全部15张含三轮车训练RGB图的初筛，场景重复明显；发现`000021_015_00000001`的3个石球标为ball，而5张相似广场图没有ball标签。6份标签均与官方new_labels_2000逐文件SHA256相同，非本项目清洗引入。待确认官方是否将球形路障纳入ball，不能自动删/补框。全部78个训练球框中11个缩到1280后短边小于16像素，不能把弱类问题全归因于分辨率。详情见[样本审阅第二批](03_v15至v19实验.md#record-1)，未改标签或训练。

2026-09-21 训练数据只读审阅：[v15后训练样本只读审阅](03_v15至v19实验.md#record-1)。1709份训练标签格式/边界及同名配套文件未发现异常；三轮车仅15框、球78框。目视2组官方图发现IR叠加纹理与轮廓残差，尚未证明全局对齐错误或分数因果；未改标签、未训练、未推理。后续先扩大有记录的样本审阅，再决定训练方案，不能将本次写成完成全量清洗。

2026-09-21 最新验收：[v15最佳权重短周期收尾](03_v15至v19实验.md#record-3)已完成9轮及一次复评，最佳第1轮，9轮正常耐心早停。相同291张验证集重载AP95=0.38494，低于v14的0.39179；保留v14，不自动重训。ball改善而tricycle/uav/boat等退步，[历次训练对比](../历次训练与赛事分数对比.md)已记录12类AP50/AP95。18回归和两批GPU检查通过不代表提分。main.py保留已完成配方，predict默认不改，未提交推送；下一步训练样本审阅另行开展。

2026-09-21 完成更新：v14已完成55轮、最佳第15轮；同口径重载AP95从v13的0.35655提高到0.39179，AP50从0.55776提高到0.62939。训练后期仍过拟合，0.60未达成；逐类对比见[历次训练与赛事分数对比](../历次训练与赛事分数对比.md)，测试、权重哈希与下一步边界见[v14执行记录](02_v9至v14复盘.md#record-7)。没有第二次正式训练在跑。

2026-09-21 当前入口：[v14一致性修复与执行记录](02_v9至v14复盘.md#record-7)。用户授权后已执行回归、短训练/预测闭环和本地基线评估；固定1709/291，不拿协议变化冒充提分。v4真实融合保留，v14仅检验骨干分层学习率，正式运行状态以该记录为准。

2026-09-20 最新入口见[v13复盘与v14阶段筛选](02_v9至v14复盘.md#record-6)：v12线上47.3270；v13最佳51轮AP95=0.35681，训练151轮后过拟合。先修正native_square预测的辅助通道补边0→114及ceil缩放，默认已有v13最佳/新输出目录；v14准备骨干2e-5、首层/颈部/头1e-4，并按九阶段历史最佳AP95控制预算。阶段目标不是预测收益。未运行测试或模型任务，未改清洗数据。下方历史“当前”和硬门槛根因表述不覆盖本段。

2026-09-20 当前入口为[v12复盘与v13训练方案](02_v9至v14复盘.md#record-5)：v11线上44.4710；v12第10轮仍创新高至AP95=0.33257、ball AP95=0.45121，却被从不同验证划分推导的0.34门槛误停。v13仅删除阶段硬门槛，其余v12变量不动，由best AP95和patience100完成同口径验证。未运行测试、训练或推理，不承诺60分。

2026-09-20 当前入口为[v10复盘与v11筛选方案](02_v9至v14复盘.md#record-3)：v10最佳第19轮AP95=0.33592、线上50.9240，验证退化而非轮数不足；v11保持结构、数据和训练日程，只关闭类别频率权重，并在第10/20轮按历史最佳AP95 0.31/0.34筛选。首轮0.4不作为门槛，v4首轮仅0.29976。未运行测试、训练或推理，不承诺60分。

2026-09-20 同日最新：v10已按用户要求替换1280草案为1920×1080内容/1920×1088张量，保持早期融合，新增RGB专用HSV、前40轮矩形拼图/较强几何、第41轮起完整画面轻几何；120轮余弦、batch2/nbs16、patience30。预测同步尺寸并自动使用共用矩形批量后端，检查权重版本与宽高。球类迁移、类别加权、清洗审计和权重留存不变。下面同日1280描述为被替代草案；无测试或模型任务，详见[v9复盘与v10训练方案](02_v9至v14复盘.md#record-2)。

2026-09-20 当前入口以[v9复盘与v10训练方案](02_v9至v14复盘.md#record-2)为准：v9已完成102轮、最佳63轮AP95=0.34952、线上53.4750；v4仍为55.6000基线。v10已实施五通道首层融合、1280、全模型1e-4、200轮、原生v4增强、cls_pw0.25和从首轮计数的早停；保留球类迁移与审计。预测默认v10 best/1280/v4路径，输出predict，不覆盖。没有测试或模型执行，不承诺60分。下面2026-09-19的“当前”“尚无v9结果”均为历史；既有清洗与划分不变。

2026-09-19 状态澄清：v9保留清洗与开训审计，额外约200张人工复核尚未完成。根据实际报错已修正main.py的imgsz索引为宽1920，并消除输出project对全局runs_dir的相对拼接；其余配方不变，未运行测试或训练。详见[v9融合训练实施](02_v9至v14复盘.md#record-1)。

代码布局更新（2026-09-19）：根目录仅保留main.py与predict.py；旧中文模块已归并或迁移，不再按历史文件路径执行。以[代码结构与运行](../代码结构与运行.md)为准，原文件可从Git提交08a4180恢复。

更新日期：2026-09-19。此页汇总项目已知事实、候选方案与历史资料的适用范围。官方原文、用户线上反馈、已有训练产物和未验证建议分别记录，避免把历史的“当前配置”当成今天的入口。

最新执行：用户批准YOLO门控融合并指定1920×1080，当前训练/预测已切为v9候选。此前仅执行一次AST静态语法解析和git diff格式检查，没有运行pytest或任何模型任务；后续默认不再自动进行静态执行检查。历史线上v8=53.5740、v7=52.4010、v4最高55.6000保持不变。详见 [v9融合训练实施](02_v9至v14复盘.md#record-1)。

## 当前代码与数据

- 代码清理：旧v5专用`训练优化.py`已删除，原中文组件已归并到`aic/`、`predict.py`及`tools/prepare_dataset.py`，旧实现可从Git提交`08a4180`恢复；详见[代码结构与运行](../代码结构与运行.md)。
- 训练：`main.py` → `aic/training.py`/`aic/model.py`，当前为待用户训练的v19质量感知P3融合；1280、batch2/nbs16、200轮上限、patience40；RGB骨干/复制IR层2e-5、检测器/新增模块1e-4。代码已改但未运行，v14仍为近期同协议候选。
- 数据：官方2000组、1709 train/291 val及既有57个标签文件修订不变，审计仍为`official_refresh_20260918_214843`。此前清理已删除两次before的images与code_cleanup备份；历史标签及清单保留，图像恢复从官方源重建，不能再把before当作完整三模态备份。
- 预测：默认已有v13 best.pt、1280/height1280、FP32、batch4、预取2批，输出predict_v13_preprocess_fix。按检查点几何选择五通道补边，历史YOLO/D-FINE后端保留。v14完成后须显式指定其权重，不自动选最新目录。
- 输出：独占新输出目录，images为带框RGB图片，labels为六列TXT，比赛提交内容为只含TXT的ZIP。再次运行须选未存在目录，正式提交单个模型的包。

## 线上反馈与适用结论

| 模型 | 用户反馈分数 | 可以确认的结论 |
|---|---:|---|
| v4 best.pt | 55.6000 | 最高已知初赛基线；已提交复赛但复赛分数未知 |
| v4 epoch50.pt | 53.2540 | 更高本地AP50候选未必线上更好 |
| v5 best.pt | 51.0830 | ball改善未带来总体收益 |
| v6 best.pth | 55.1490 | 未超过v4 |
| v6 best_map50.pth | 55.1360 | AP50最佳选择未改善该次线上反馈 |
| v7 best.pth / best_map50.pth | 52.4010 / 52.4010 | 两个候选未突破基线 |
| v8 用户本次提交 | 53.5740 | 高于v7但仍低于v4；当前JSON为best_map50.pth |
| v9 best.pt（本地记录第63轮） | 53.4750 | 比v4低2.1250，后期过拟合；新旧验证划分不同 |
| v10 用户反馈 | 50.9240 | 本地最佳0.33592，后期过拟合 |
| v11 用户反馈（训练20轮） | 44.4710 | 未核验本次提交权重与最佳轮的对应关系 |
| v12 用户反馈（训练10轮） | 47.3270 | 原预测补边差异已写入修正，尚无修正后分数 |
| v13 | 未提供数值 | 本地最佳0.35681，不能据此编造线上分数 |

本轮没有运行模型前向、测试、独立评估、推理或训练。数据审计、原图审阅和文件指纹核对是数据准备，不是模型性能验证。正式训练内部验证用于选权重与早停，并复用同轮预测记录PNG/JPG分域AP。

## 沉淀的判断原则

1. **清洗记录比版本猜测可靠。** v6/v7已使用清洗标签，不能把v4优势归因于后续漏清洗。既有清洗与学习率同时改变，不能拆出每项独立收益；不能因弱类AP低便删框或改类。
2. **验证覆盖影响选权重。** v8移动35组审阅场景：24张湖边JPG、6张三轮车PNG、5张球类PNG。新增验证仅部分修补覆盖；boat仍7框，JPG仅一个新增场景。文件指纹无跨集完全重复不证明场景语义独立，新旧划分AP不可直接比较。
3. **弱类与整体是两件事。** v5支持组合配方改善部分弱类，不证明每项增强均有益。训练分类损失下降不是独立的过拟合判据；固定验证集后期退化才支持泛化变差。重新使用官方预训练也不保证消除过拟合。
4. **预测参数要按架构解释。** YOLO v4路径从历史提交86411da恢复，采用原尺寸五通道数组、逐张矩形填充、框架原生单标签NMS；历史默认权重名仍为v3，不能冒充55.6000提交的不可变快照。current路径为批量自定义NMS。D-FINE采用正方形等比填充和原生查询类别排序，不能套用YOLO NMS或多标签开关。
5. **展示与候选阈值分开。** visual_conf和图片保存压缩不改变模型输入或TXT检测结果。v7此前1000张均输出100框、全部候选置信度≥0.01，降低0.001阈值不会改变该包的前100候选。没有发现当前D-FINE只用RGB或坐标还原不一致，恢复YOLO路径未被证明是涨分修复。
6. **评分依据采用官方定义。** 官方资料使用mAP50-95、101点插值、每图最多100框；不保留未经确认的经验加权公式。仅官方训练数据用于训练，不使用测试集标注/训练、在线服务或多模型平均集成。
7. **同分辨率不等于同预处理。** v12/v13同为1280，但曾存在验证/预测的辅助通道补边114/0差异；还应核对取整、通道次序、归一化、填充与坐标恢复。此bug不解释本地过拟合，也不能用于否定全部旧架构。

## 文档导航与阅读范围

| 文档 | 阅读用途 |
|---|---|
| [v18复盘与三模态数值审计](03_v15至v19实验.md#record-2) | v18完成结果、深度/IR数值证据及本轮审计范围 |
| [下一融合设计草案](03_v15至v19实验.md#record-9) | 待审核的质量感知与局部容错方案，尚未实施 |
| [v13复盘与v14阶段筛选](02_v9至v14复盘.md#record-6) | 当前预测修正、逐类结果、预算门槛及下一轮假设 |
| [v9融合训练实施](02_v9至v14复盘.md#record-1) | 历史门控1920矩形实施与当时未实测边界 |
| [v8改进方案与下一轮融合设计](01_早期训练与清洗.md#record-5) | 原始候选依据；后续按用户要求改为1920并实施v9 |
| [历次训练与赛事分数对比](../历次训练与赛事分数对比.md) | 早期至v8整体峰值、实际赛事成绩与全部12类AP50/AP95 |
| [v8完成复盘与官方下载刷新](01_早期训练与清洗.md#record-6) | 最新结果、下载内容对比、截图经验与下一轮融合方向 |
| [v8训练方案与数据清洗](01_早期训练与清洗.md#record-7) | 历史D-FINE配方与划分来源；备份完整性以最新清理状态为准 |
| [预测与赛事提交](../预测与赛事提交.md) | 当前预测、恢复v4路径、单模型提交 |
| [数据集成分与划分记录](04_数据与调参经验.md#record-1) | 数据来源、旧审计与v8新划分限制 |
| [训练配置与数据集复核](04_数据与调参经验.md#record-2) | 历次配方、结果和历史检查，按阶段阅读 |
| [D-FINE三模态实施方案](01_早期训练与清洗.md#record-1) | v6架构引入、源码固定及迁移机制 |
| [v7训练方案与清洗核验](01_早期训练与清洗.md#record-4) | v7历史增强/重复策略和清洗证据 |
| [v5训练方案与球类诊断](01_早期训练与清洗.md#record-2) | 历史弱类组合方案，不再是入口 |
| [v5训练完成复盘](01_早期训练与清洗.md#record-3) | 弱类收益、后期退化与线上回退 |
| [Ultralytics训练参数参考](../参考/Ultralytics训练参数参考.md) | YOLO参数默认值，不替代D-FINE配置 |
| [Ultralytics预测参数参考](../参考/Ultralytics预测参数参考.md) | YOLO预测接口，按后端适配 |
| [YOLO训练通用经验](04_数据与调参经验.md#record-3) | 其他项目经验及本赛题适用边界 |
| [数据集目录规范](04_数据与调参经验.md#record-4) | 当前三模态目录、硬链接和缓存区别 |
| [丹方](04_数据与调参经验.md#record-5) | 数据工作笔记和外部网站收藏，非赛事增补来源 |
| [电脑训练环境](05_旧运行与环境记录.md#record-1) | 硬件规格、依赖与历史资源快照 |
| [训练前释放资源](05_旧运行与环境记录.md#record-2) | 历史系统操作记录，不自动执行 |
| [比赛任务要求](05_旧运行与环境记录.md#record-3) | 官方PDF摘录，项目导航另列 |
| [官方赛题规则](../比赛资料/官方赛题规则.md) | 保留抓取时官方正文，非本轮实时规则核验 |
| [参赛手册](../比赛资料/2026%20AIC%20视觉多模态目标检测参赛手册.md) | 官方链接和完整流程，项目阶段按日期区分 |
| [视频讲解](../比赛资料/视频讲解-完整内容.md) | 历史视频整理，非当前代码说明 |
| [早期预测计划](05_旧运行与环境记录.md#record-7) | 归档实现计划，测试条目不作为当前指令 |
| [早期清洗计划](01_早期训练与清洗.md#record-8) | 归档路线，独立重评未被本轮执行 |
| [v5完整方案记录](01_早期训练与清洗.md#record-9) | 归档实施过程，后续结果以复盘为准 |

历史资料中的测试、资源清理或旧恢复命令不授权再次执行。版本变更后先核对入口配置、数据审计和实际运行目录，完成训练/线上提交后再填写新结果，不提前把候选写成成功经验。


---

<a id="record-7"></a>

## 原文件：docs/superpowers/plans/2026-09-16-predict.md

# 三模态赛事预测实现计划

归档说明（2026-09-18）：这是早期实施计划，不是当前执行指令。预测入口已为predict.py，组件三模态预测.py，兼容YOLO和D-FINE；v4原生路径已恢复，未运行本轮测试或推理。下方测试、技能、禁止提交等描述仅是当时计划；后续用户已要求提交及推送。当前约束和知识见 [索引](../项目知识索引.md)。

> 执行方式：当前会话内按 executing-plans 顺序实施；用户已确认方案和文件名，不创建工作树，不提交 Git。

**Goal:** 根目录 `predict.py` 离线预测官方三模态测试集，生成图片、六列标签和可提交 ZIP。

**Architecture:** 复用 `三模态训练.fuse_modalities`；扩展 DetectionPredictor，将五通道输入保留到网络，仅在构建显示结果时转回 BGR。逐张推理，全部文件验证成功后才生成正式 ZIP。

**Tech Stack:** 本仓库 Ultralytics、PyTorch、OpenCV、NumPy、pytest、标准库 zipfile。

## 约束

- 用户指定 `predict.py`，输出 `predict/images`、`predict/labels`、`predict/比赛提交内容`。
- 不覆盖既有输出、不改变训练配方、不启动训练、不上传数据。
- 默认单模型、1280、conf=0.001、iou=0.7、NMS、每图最多 100 框；显示阈值与提交阈值分离。
- 空预测仍有同名空 TXT；ZIP 根目录只有 TXT。
- 默认选择现有验证指标较高的旧运行 best.pt；通过 --weights 显式更换，不自动追踪新权重。

## Task 1：数据、坐标与提交合同

文件：新增 `tests/test_predict.py`、`predict.py`。

接口：`collect_samples(source: Path) -> list[tuple[Path, Path, Path]]`；`prepare_output(output: Path) -> None`；`prediction_rows(result: Results) -> np.ndarray`；`save_prediction(result: Results, image_name: str, output: Path, visual_conf: float) -> None`；`build_submission(output: Path, image_names: list[str]) -> Path`。

- [x] 写失败测试：缺模态/同词干冲突拒绝；已有输出保留；手工框 `[20,10,60,30]` 在 100×50 图上输出 `[类别,0.4,0.4,0.4,0.4,置信度]`；空 TXT；非法数值拒绝；ZIP 文件名与测试图严格一致。
- [x] 运行 `uv run pytest tests/test_predict.py -q`，确认因新模块未实现而失败。
- [x] 实现配对、显式防覆盖写入、按置信度排序、格式核验与完整 ZIP。
- [x] 运行同一测试，确认通过。

## Task 2：五通道预测与命令行

文件：`predict.py`、`tests/test_predict.py`。

接口：`MultimodalDetectionPredictor`；`predict_image(model: YOLO, fused: np.ndarray, output: Path, device: str, imgsz: int, conf: float, iou: float) -> Results`；`main() -> None`。

- [x] 测试五通道像素次序、LetterBox 坐标还原和 RGB 显示；拒绝三通道/错误类别模型。
- [x] 实现逐张融合与预测，显式填写参数，记录运行参数与权重摘要到输出根目录的 JSON。
- [x] 小型本地五通道随机模型 CPU 集成测试，验证真实预测链路，不依赖训练或下载。
- [x] 正式权重小样本烟雾测试仅写临时目录，不生成冒充完整提交的产物。

## Task 3：文档与最终验证

文件：`README.md`、`docs/预测与赛事提交.md`、`.gitignore`。

- [x] 写明默认权重、替换命令、输出布局、参数依据及不覆盖规则；忽略 predict 产物。
- [x] 运行 `uv run pytest tests/`、`uv run python predict.py --help`、`git diff --check`。
- [x] 检查实际差异，交付运行命令和验证结果；不承诺榜单分数，不自动上传。

验证结果：新增 15 项预测测试，全套 33 项通过。正式权重在 CPU/1280 下完成两组不同原图尺寸的烟雾推理与打包。未修改训练代码，未运行全量测试集，未提交 Git 或上传赛事平台。


---

<a id="record-8"></a>

## 原文件：README.md

# AIC 城市场景三模态目标检测

初赛成绩已按用户提供的16条平台记录更新：[提交成绩台账](../初赛提交成绩台账.md)。最高仍为v4的55.6000；补录v5多标签50.8550，以及版本待确认的53.1270/54.0500。全部属于初赛，不挪作复赛分数；本次文档更新不改变训练参数。

## 当前：v19质量感知局部融合已写入代码，等待用户训练（2026-09-22）

`main.py`已切换v19：官方YOLO26l RGB主干 + 预训练IR浅层局部对应 + 带有效性支持的Depth残差，仅在P3进入颈部时融合。保持1280和清洗1709/291；batch2/nbs16，新增分支显存未实测；不改源图或标签。初始化、学习率、训练预测契约及命令见[v19实施记录](03_v15至v19实验.md#record-8)。

启动训练：`uv run python main.py`。不需要重新准备数据。预测默认仍保留用户旧选择，训练完成后按文档显式指定v19权重、复赛source与新输出目录。

本轮只修改源码并核对文本差异，未运行测试、语法执行检查或模型任务。v19尚无AP或赛事分数；保留v14近期同协议候选，不保证达到0.60。旧模型的预测预处理不变，不能给旧权重套用v19支持掩码处理。

## 历史：v18未改善及模态数值审计

v18完成55轮，最佳第15轮AP50=0.60406、AP95=0.37521；低于v14训练峰值0.39215及已有batch2重载结果0.39179，不推广。辅助首层有更新、有响应，不能把失败归因为“没有学习到模态”。[完整结果与审计](03_v15至v19实验.md#record-2)、[历次类别对比](../历次训练与赛事分数对比.md)。

训练1709组IR/Depth已做只读数值审计：PNG/JPG深度采样零值约27.65%/80.92%，5张JPG深度全像素为0；PNG采样没有超过20000的值，量程有官方依据。部分IR目视存在黑边/模糊/轮廓残差，尚无全量错位率。[质量感知、局部容错融合设计](03_v15至v19实验.md#record-9)已获用户批准，实施状态以上方v19为准。

v18完成审计时仅提交了文档；后续v19实施已改变源码，不能继续将其描述为“代码不变”。现有清洗、标签和官方图像仍不变，不上传原图/逐图输出。

## 历史：v17受限长尾采样交付时状态

已读取v4的args.yaml；v17以v14为基准，恢复200轮日程、第101轮关Mosaic，保持1280/batch4/nbs16、AdamW主1e-4/骨干2e-5和原清洗1709/291。新增全类别频率驱动的受限采样，不叠加类别损失加权。每图主索引最多2次、额外总量≤10%、每序列/已审阅场景额外≤2，球类提议倍率另限1.25；Mosaic附加图不包含在主索引上限中。运行时写三份采样审计，不复制图像、不修改标签或验证集。

用户运行 `uv run python main.py`，输出`runs/detect/AIC_RGBIRDepth_yolo26l_1280_v17_bounded_repeat`，重名另建。最多200轮不代表强制跑满，阶段筛选和patience40仍生效。v16已完成60轮，最佳第36轮AP95=0.36766，未超过v14；不再继续v16。v17实现未经过测试/模型执行，本轮未提交推送，不保证提分。

`predict.py`用户默认配置不改；选定v17后显式填写实际best权重及`--source "数据集/复赛测试集"`。完整参数、v4取舍、记录口径和预测命令见[v17计划](03_v15至v19实验.md#record-5)。

## 历史：v16配置交付时状态

使用当前清洗版1709/291，从官方YOLO26l重新迁移五通道。保留v14主学习率1e-4/骨干2e-5、1280/batch4/nbs16，改为60轮余弦，第21轮关闭Mosaic；不继续v15微调、不重洗数据。用户运行 `uv run python main.py` 开训，预计完整60轮约2小时，仅为历史速度估算。未进行测试、训练或预测，不承诺提分。

用户已自行用v4提交复赛，成绩未知。`predict.py`默认仍保留用户历史配置；v16完成后必须显式指定其实际best路径及 `--source "数据集/复赛测试集" --imgsz 1280 --height 1280 --output predict_v16_round2`，不要直接运行默认预测。参数、停止条件、完整命令见[v16计划](03_v15至v19实验.md#record-4)。未提交推送；下方v15等“当前”指对应历史阶段。

## 当前：v15收尾无增益，保留v14（2026-09-21）

从v14第15轮best继承五通道权重的低学习率、无Mosaic短收尾已完成：9轮正常早停，最佳第1轮。一次同口径独立复评AP95=0.38494，低于v14的0.39179；AP50虽由0.62939微升至0.63178，不能代替AP95提升。18项回归及两批GPU检查通过只证明限定功能可用，不证明提分。

保留v14 best为当前候选；main.py保留已完成v15配方供追溯，不要重复开训。predict.py原有默认未改，使用v14须显式指定权重。任务状态为completed/retain_v14，未自动启动下一轮，未提交推送。[v15参数与验收记录](03_v15至v19实验.md#record-3)、[完整逐类对比](../历次训练与赛事分数对比.md)。下一步需另行审阅训练标签与模态对齐，不自动改验证集。

## 当前：一致性修复与本地AP95对照（2026-09-21）

v14已训练完成55轮，最佳第15轮，随后正常早停。同291张验证集、FP32/batch2重载复评：v13 AP95 **0.35655** → v14 **0.39179**，提升3.524个百分点；AP50为0.55776 → 0.62939。训练当时最佳AP95为0.39215，不能与重载复评数值混用。后期仍有过拟合，尚未达到0.60，也不能据此认定超过旧划分v4。当前没有正式训练进程，不需要重复启动main.py。

复赛暂不能线上评分，固定清洗版1709/291。v4真实五通道首层融合已保留并实测迁移和梯度，不是特殊门控结构。10项回归、两批训练/验证、检查点重载及两张验证图预测打包通过。v14单独检验骨干2e-5/其余1e-4的假设，不承诺AP95=0.60。

最佳权重对应图在`best_validation/`，不要把根目录末轮图当最佳图。显式复评命令：`uv run python -m tools.validate_local --weights runs/detect/实际运行名/weights/best.pt`。只使用当前291张val。完整结果、修复范围及训练状态见[v14一致性修复与执行记录](02_v9至v14复盘.md#record-7)。以下日期段落保留历史语境。

## 当前：v13预测修正与v14预算筛选（2026-09-20）

v12线上47.3270。v13完成151轮，最佳第51轮mAP50-95=0.35681，最后0.31912，后期过拟合。v13具体线上分数未提供，v4的55.6000仍为最高已知基线。

已发现原生验证与预测的IR/Depth补边114/0不一致。先运行`uv run python predict.py`，复用v13 best生成`predict_v13_preprocess_fix`，无需重训；本轮未执行此命令，收益未知。历史固定矩形/v4/D-FINE保持各自预处理。

`main.py`已准备v14：五通道1280，骨干第1–10层lr2e-5，首层/颈部/头1e-4，保留数据与增强；patience40及九个阶段AP95门槛控制预算。未运行测试、静态执行检查或模型任务，不承诺60分。详见[v13复盘与v14阶段筛选](02_v9至v14复盘.md#record-6)。下方早期配置均为历史。

2026-09-19 报错修正：main.py已将imgsz同步为IMAGE_HW[0]=1920，保留宽1920、高1080；输出project改为按入口位置解析的绝对runs/detect，避免全局runs_dir重复拼接。其余v9配方和清洗数据不变，额外约200张人工复核仍待完成。只核对代码与差异，未运行测试或训练，不能声称已经通过实际开训验证。详见[v9实施说明](02_v9至v14复盘.md#record-1)。

代码结构（2026-09-19）：根目录只保留`main.py`训练和`predict.py`预测、保存及提交打包两个入口。共享模型/读图和训练组件放在`aic/`；清洗工具放在`tools/prepare_dataset.py`，运行`uv run python -m tools.prepare_dataset`。不再保留中文中转模块，详细迁移与旧权重说明见[代码结构与运行](../代码结构与运行.md)。

最新执行（2026-09-18）：用户已批准 **YOLO26l 门控融合 v9**，并要求训练和预测保留1920×1080内容。`main.py`已恢复原生`model.train(...)`分节风格：RGB预训练主干+红外/深度轻量分支，画布补齐为1920×1088，batch=2/有效16、120轮，第81轮完整画面收尾；`predict.py`同步v9 best.pt及相同尺寸。只做静态核对，未测试或启动任何模型任务；显存与分数未验证。详见 [v9融合训练实施](02_v9至v14复盘.md#record-1)。

使用五通道（RGB 3 + 红外 1 + 深度 1）检测官方12类，内部为三个真实参与学习的特征分支和同一检测器。历史YOLO与D-FINE预测后端保留；当前无引用的旧v5专用`训练优化.py`已删除，可从Git提交`08a4180`恢复。原三模态/融合模块已经按职责归并，旧D-FINE只保留预测支持，不再保留训练循环。

清洗数据继续使用`datasets`的1709 train / 291 val，保留2000组及已有57个标签文件修订。本轮不改数据，审计仍为`official_refresh_20260918_214843`。此前空间清理删除两个before中的images及code_cleanup备份；审计和历史标签保留，恢复图像须从官方源按清单重建，不能直接搬回完整旧目录。

v8线上53.5740、v7为52.4010、v6两份候选55.1490/55.1360；v4的55.6000仍为线上基线。v9尚无训练/赛事结果，不承诺超过60分。v8改变过验证划分，旧划分本地AP不能与新划分直接排名。以下v4–v8描述属于历史。

## 历史 v6 启动记录（2026-09-18）

v5 best.pt 已确认线上 **51.0830**，SHA256 与提交记录一致；v4 **55.6000** 仍是正式基线。main.py 已切换为 D-FINE-L Objects365 E25 五通道微调：1280、60 轮、3 轮预热、AdamW 主学习率 1e-4/骨干 1e-5、batch=2、有效批次16。关闭 v5 的重采样、定向裁剪与模态缺失。官方源码与权重、必要依赖已在本机准备，尚未启动新训练或运行模型测试，不能声称已提分或验证显存可用。

配置依据、类别初始化和离线复现见 [D-FINE三模态实施方案](01_早期训练与清洗.md#record-1)。下面 v5 的配置描述是历史，不是当前入口。

## 历史 YOLO v4/v5 记录

初始化说明（2026-09-18）：v5 已从 `orgin_models/yolo26l.pt` 的官方 COCO 预训练重新开始，未续训 v4；使用官方预训练仍可能过拟合。弱类改善不代表整体提高，训练分类损失下降本身不是问题。

v5_full 已完成 234 轮，最高 mAP50=0.59835（41 轮）、mAP50-95=0.40625（34 轮）。ball 从 v4 的 AP50=0 提升至约 0.423，但总体没有明显突破，后期过拟合。不要续训 last.pt；先使用 best.pt 进行用户自行决定的合规提交。详见 [完成复盘](01_早期训练与清洗.md#record-3)。以下“尚未训练”为历史状态。

最新线上反馈：v4 得分 55.6000，epoch50.pt 为 53.2540，保留 v4 为正式基线。下一轮已写入 main.py，运行名 AIC_RGBIRDepth_yolo26l_1280_v5_full；包含弱类重采样、原尺寸同步目标裁剪、RGB 增强、模态随机缺失和分层学习率，cls_pw=0、mosaic=0.25。第 161 轮起关闭 Mosaic/裁剪/模态缺失，patience=200。目标 60 分以上，不保证实际提分。详见 [v5 配方与 ball 诊断](01_早期训练与清洗.md#record-2)。尚未执行新配方训练或推理。

- 数据：官方 2000 组，train 1744 / val 256；使用新版标注的可追溯清洗副本，修改 57 个标签文件（64 个边缘框裁剪、1 行去重、1 处大客车类别修订），原始数据保留。
- 已完成 v2：第 79 轮最佳本地 mAP50-95=0.37746，第 179 轮早停；本地验证分数不等同官方测试榜单分数。
- v3 最佳 mAP50-95=0.37290；v4 已完成 200 轮，CSV 最高 mAP50=0.60632（42 轮），最高 mAP50-95=0.40370（100 轮），三次最终 best.pt 的 ball AP50 均为 0。
- 当前代码配置：1280、batch=4、workers=4、BF16、disk 缓存、AdamW、lr0=0.0001（骨干 0.00002）、nbs=16；5000 轮、patience=200，200 轮学习率衰减，第 161 轮开始完整模态收尾。epochs 仍影响 YOLO26 双头损失日程，不是纯训练上限。
- epoch50.pt 的本地 mAP50 更高，但用户报告线上更低，不能只按本地 mAP50 替换正式模型。当前已实施 v5，等待用户开训。

## 使用

本机沿用现有datasets、本地`orgin_models/yolo26l.pt`和已安装依赖。在项目根目录执行：

```powershell
uv run python main.py
```

当前运行名为`AIC_RGBIRDepth_yolo26l_1280_v14_backbone_guard`，重名由YOLO递增目录。best.pt按mAP50-95选取，另存best_map50.pt、last.pt和周期检查点。只恢复同配方中断任务；已完成/早停的last.pt拒绝恢复。新机器需自行准备同版本依赖和官方数据；不要无故升级本机已适配的框架。

v14训练完成后显式指定`--weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v14_backbone_guard/weights/best.pt --imgsz 1280 --height 1280 --output predict_v14`。预测默认保留已有v13最佳，不能只改输出名就当作v14提交。

`tools/prepare_dataset.py`保留清洗审计工具；日常训练无需再运行，不应以历史重建函数覆盖清洗版。训练组件在`aic/training.py`，共享读图在`aic/data.py`；资源释放由用户管理。

评估脚本、tests、config_checks已不在工作位置，其code_cleanup备份随后经用户授权删除。不会自动执行测试、复评、额外推理或训练。本轮没有安装或更新依赖，D-FINE依赖留作历史模型预测兼容。

## 本地目录

```text
datasets/
├── train/{images,infrared,depth,labels}
├── val/{images,infrared,depth,labels}
└── data.yaml
数据集/训练集/
├── AIC2026_Train_2000/{visible,infrared,depth,labels}
└── new_labels_2000/
orgin_models/                       # 本地预训练权重
vendor/D-FINE/                      # 固定版本官方源码（不入库）
runs/                              # 当次权重、日志与曲线
```

三种模态均位于 datasets 的对应划分内，以官方原图硬链接复用；标签是独立清洗副本。清洗备份与逐行记录在 `runs/dataset_cleaning/20260917_012026/`。硬链接图像不能原地改写。仅 datasets/data.yaml 入库，数据与运行产物不入库。

## 文档导航

- [项目知识索引](../项目知识索引.md)：统一当前状态、线上反馈、推理恢复、清洗事实和历史文档范围。

- [v8当前方案与数据清洗](01_早期训练与清洗.md#record-7)：新配置、数据备份、划分变化、启动与预测命令。
- [D-FINE历史实施](01_早期训练与清洗.md#record-1)：固定源码、预训练来源、类别迁移。

- [数据来源、问题与划分](04_数据与调参经验.md#record-1)：新版类别统计、ball 核验、边缘框、验证集不足。
- [训练配方与实验记录](04_数据与调参经验.md#record-2)：两次完成训练对比、球类退化诊断、双头损失日程及下一轮实验；历史配置单独保留。
- [参数参考](../参考/Ultralytics训练参数参考.md)、[目录规范](04_数据与调参经验.md#record-4)：区分框架默认值和项目设置。
- [训练通用经验](04_数据与调参经验.md#record-3)、[数据工作笔记](04_数据与调参经验.md#record-5)：历史示例不直接套用本赛题。
- [本机环境](05_旧运行与环境记录.md#record-1)、[历史资源释放记录](05_旧运行与环境记录.md#record-2)。
- [赛题要求摘录](05_旧运行与环境记录.md#record-3)、[官方资料](../比赛资料/官方赛题规则.md)：官方内容与项目核验结论分开维护。

## 预测与初赛提交

已恢复历史YOLO逐张原生单标签推理路径，使用：

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v4_clean_lr1e4/weights/best.pt --imgsz 1280 --yolo-profile v4 --output predict_v4_restored
```

YOLO默认选v4路径；现有批量自定义后处理用 `--yolo-profile current`。D-FINE使用独立原生后端，不能套用YOLO后处理，当前用户v7默认权重保持不变。历史来源和边界见 [预测文档](../预测与赛事提交.md)。

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_dfine_l_1280_v8/weights/best.pth --imgsz 1280 --batch 18 --output predict_v8
```

上述命令在 v8 训练产生权重后使用，若运行目录带时间戳须换成实际目录。predict.py 保留用户当前选择的 v7 best.pth 路径，不自动选择最新运行；历史五通道 YOLO .pt 仍可通过 --weights 使用，输出结构不变。

预测入口采用与 main.py 相同的 `PredictionConfig` 分节配置，实现也已合并在 `predict.py`。以下1536和batch=18是历史快照，当前v9默认1920×1080、batch=4、预取2批。当前用户入口为 FP32、1536、batch=18、8个读取线程、8个保存线程、预取10批；预测v8须如上显式切换权重和1280。可用 `--batch` 调整，不自动试跑探测显存。本轮未测试、未测速，实际吞吐量由正式预测结束时日志记录。

`predict.py` 自动配对官方初赛 1000 组三模态，生成 `predict/images` 带框图片、`predict/labels` 六列标签，以及 `predict/比赛提交内容/submission.zip`。**初赛只提交该 ZIP**，不上传图片或本地运行 JSON。已有输出目录会报错，不覆盖；重复预测使用 `--output predict_v3` 等新目录。

predict.py 的默认权重由用户手动维护；推荐显式传入要提交的单个模型。v5 训练完成后可执行：

```powershell
uv run python predict.py --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v5_full/weights/best.pt --output predict_v5
```

可选 `--multi-label` 保留同框多个类别候选，默认关闭；可能改善召回，也可能增加误检。独立候选使用新输出目录，不覆盖基准包。参数与权重选择见 [预测与赛事提交](../预测与赛事提交.md)。

五通道权重不能只传 RGB 图片调用通用预测命令；本脚本复用同名三模态预处理。赛事数据不上传公开平台，测试数据仅用于合规推理。
