# v14 一致性修复与执行记录

日期：2026-09-21。用户已批准上一轮审查方案，并明确授权相关代码修改、测试、训练、推理；未授权提交或推送。本次在当前工作区继续，不覆盖历史产物，不上传赛事数据。

后续追加：[v15短周期收尾](superpowers/plans/2026-09-21-v15-polish.md)已于2026-09-21 11:15完成9轮训练及一次独立复评，最佳第1轮，AP95=0.3849390289，比v14下降0.68486个百分点，保留v14。第5轮筛选通过，第9轮耐心早停，不是第10轮门槛误停。下面“下一候选尚未实施/启动”是v14交付当时的状态；v14权重与复评基线未改。下一步训练样本审阅需另行开展，不自动再训练。

## 设计与验收

保留 v4 的真实融合机制：官方 RGB 预训练、五通道首层、新增 IR/Depth 卷积切片零初始化且可训练。不是门控融合，不永久清零模态。固定现有清洗版 1709/291，禁止用曾见过新增验证图的 v4 权重续训后声称公平胜出。

统一当前训练验证与预测的 native_square 缩放、五通道 114 补边、单标签 NMS、真实内容区裁框。评估协议版本单独记录；先用已有 v13 best 在新协议下重新建立本地基线，不能直接把协议差异算作新训练收益。历史 v4 的旧验证数据和矩形协议保留为历史事实。

训练变量仍只有骨干第1–10层学习率降至 2e-5，首层/颈部/头保持 1e-4。保留完整入口参数、200轮上限及阶段预算门槛。原生轻增强收尾改为显式可选并验证有效，本次不启用；避免和学习率实验混合。早停可能先于第101轮关闭 Mosaic，不承诺完整收尾或 AP95 达到0.60。

## 执行清单

- [x] `tests/test_training_consistency.py`：10项回归覆盖原生预处理、边缘裁框、缓存关闭/失效、内容审计、可选收尾及早停禁用。
- [x] `aic/model.py`、`aic/training.py`、`predict.py`：共用输出几何处理并记录评估协议；保留历史推理兼容。
- [x] `aic/training.py`：缓存包含读图实现，标签内容加入缓存键；开训核对来源图像哈希；最佳轮图与指标单独保存，不追加模型前向。
- [x] 原生收尾参数不再静默无效；`patience=0`遵循禁用早停语义。
- [x] 两批 GPU 训练/验证、检查点重载及预测打包检查，以及已有 v13 的完整291张本地基线；不推理官方测试集。
- [x] 修正当前文档、参数注释及授权记录，保留真实测试结果和限制。
- [x] 正式 v14 已启动并完成55轮，最佳第15轮；独立重载复评确认相对v13有提升，未达到0.60。

## 资源与范围

开始时 RTX5080 显存占用约1.2GiB，无 Python 训练进程，可用内存约18GiB，D盘剩余约32GiB。保留 batch4、workers4、低预取、不写大体积NPY；不杀其他程序，不自动扩大批次。测试使用独立临时目录，报告放 runs 中。仅本地执行，不采用云训练技能的上传流程。

## 实测结果

1. `.venv/Scripts/python.exe -m pytest tests/test_training_consistency.py -q --tb=short`：最终10 passed，约2.5秒。裁框、缓存、原生收尾、早停及新快照接口均先观察到相应失败再修正。快照测试发现NumPy整数不能直接写JSON，已显式转换。
2. `runs/checks_20260921/main_check.py`：第一次已完成两批训练/验证，但检查脚本漏填PredictionConfig必填项，预测环节未完成。补齐检查脚本后复跑至`main_check-2`成功：2训练批、2验证批、2优化步，生成并重载best.pt，2张val图（PNG/JPG）预测和ZIP打包通过。峰值reserved显存11.88086GiB。测试产物不是正式训练成果。
3. RGB首层迁移逐值一致，额外IR/Depth切片初始为零、requires_grad=True，实际反向得到非零梯度。PNG/JPG验证输入与预测输入逐像素相等。没有测试全部历史D-FINE/门控模型或长期断点恢复。
4. `.venv/Scripts/python.exe -m tools.validate_local --weights runs/detect/AIC_RGBIRDepth_yolo26l_1280_v13_native_full/weights/best.pt --output runs/local_validation/v13_content_clip_v2`：291张val，AP95=0.3565488837，AP50=0.5577577474。修复标签缓存键后重新运行到`v13_content_clip_v2_fresh_cache`，标签重扫且两项结果完全相同。
5. 新协议v13基线不是新模型成绩；其与旧CSV AP95=0.35681接近，边缘裁框不是本地性能瓶颈。补边修复影响旧预测路径，不能由本次验证推导线上涨分。后续v14须与新协议0.35655比较，仍不等于当前验证划分上超过v4——v4旧AP来自另一划分，而且见过后来移入val的35张图。

评估完整元数据、逐类指标、PR曲线和混淆矩阵保存在`runs/local_validation/v13_content_clip_v2_fresh_cache/`。正式训练每次AP95新高保存`best_validation/metrics.json`与同轮曲线；根目录图只代表最近被绘制的验证轮次。

## 正式训练完成与独立复评（2026-09-21 追加）

2026-09-21 01:53:16后台启动，启动器PID为36464，约03:44结束，现在已无该训练进程。运行目录为`runs/detect/AIC_RGBIRDepth_yolo26l_1280_v14_backbone_guard/`，启动输出在`runs/checks_20260921/launch_v14/`。完成55轮，框架报告1.853小时；第15轮最佳后40轮没有AP95新高而正常早停，不是异常退出，也不是阶段门槛失败。第10/20/50轮筛选全部通过。

| 口径 | v13 best 第51轮 | v14 best 第15轮 | v14变化 |
|---|---:|---:|---:|
| 训练当时记录 AP50 | 0.56103 | 0.62932 | 不作为统一协议复评差值 |
| 训练当时记录 AP95 | 0.35681 | 0.39215 | 不作为统一协议复评差值 |
| 同协议重载权重 AP50，batch2 | 0.5577577474 | 0.6293876168 | +7.163个百分点 |
| 同协议重载权重 AP95，batch2 | 0.3565488837 | 0.3917876103 | +3.524个百分点 |

v14复评目录为`runs/local_validation/v14_best_content_clip_v2_batch2/`；固定291张、FP32、1280、单标签NMS、conf0.001/iou0.7/max_det100，与v13复评相同。v14权重SHA256为`b9dcf291dbe55857620b84b5f2f08b196c71bec6e129bfcd9bfb29a7ddcb557c`。评估工具补记batch/workers，不改变评估运算。

先按训练的batch1重载复评得到AP50=0.6289019752、AP95=0.3915779482，保存在`v14_best_content_clip_v2/`；随后按v13基线batch2对齐，以上表后两行为当前比较依据。框架保存EMA时转为FP16，重载FP32不能还原舍入前参数；本次也观察到batch1/2的微小数值差异。因此训练中最佳图/JSON与重载复评结果分别留存，不声称逐位一致，也不据此认定存在重大推理故障。

## 结论与下一步边界

- 本次真实进步来自一次受控训练对照，支持降低预训练骨干更新幅度的方向；仍只有单种子，不能宣称所有场景必然改善。来源图像与标签没有改动，不能把本次提高算成重新清洗的收益。
- 第15→55轮train/box_loss从1.26023降至1.07916，但val/box_loss从1.27950升至1.36625，val/cls_loss从1.12098升至1.19295，AP95降至0.35687。仍有后期过拟合，增加轮数不是已证明的解决办法。
- 第一轮AP95仅0.24671，后面仍达到0.39215，再次说明首轮低于0.4不能判定配方无效。
- 第101轮才关闭Mosaic，实际第55轮已停，所以本轮从未进入无Mosaic收尾。这是现有日程与早停的交互，不应靠放宽门槛掩盖。下一候选是保留v14作为基线，从其best做独立的短周期低学习率、关闭Mosaic收尾；须另建运行并记录新初始化/优化器，不能把已完成的last当中断resume。该候选尚未编写或启动，不自动扩展为多模型搜索。
- 逐类AP95有10类提高，boat与ball回退。boat只有2张/7框，不能仅凭该类波动自动改标签或加强重采样。下一轮数据复核优先查训练集boat/ball，以及light的框边界和三模态对齐；不改验证标签以追分，不根据预测自动删难例。此前约200张人工复核仍未完成。
- 不把v4旧划分AP95=0.40370和当前结果直接排胜负；v4见过当前val中的35张图。没有赛事新分数，本地0.60目标尚未达到。当前可用候选是v14第15轮best.pt，不是第55轮last.pt。
- 本轮未提交或推送Git，未运行官方测试集推理、未删除历史权重，也没有启动第二次正式训练。main.py保留可复现的v14配方，predict.py原有v13默认选择不擅自覆盖；使用v14时显式指定权重。
